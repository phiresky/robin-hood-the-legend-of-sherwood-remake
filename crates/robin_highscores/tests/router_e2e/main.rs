//! Real-router end-to-end scenarios grouped by feature. The shared rig,
//! request builders and manifest fixtures live in `support`.
#![forbid(unsafe_code)]

mod boards;
mod campaign;
mod preflight;
mod storage;
mod submission;
mod support;
