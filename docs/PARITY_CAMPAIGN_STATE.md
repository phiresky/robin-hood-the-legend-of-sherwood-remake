# Parity campaign — full session state dump (2026-08-06, ~14:10 Berlin)

Written when the session ran out of credits (limit resets 18:30 Berlin; earlier reset was 13:30 so it may be a rolling window). This file is the complete resume point — a fresh session should be able to continue the campaign from here with NO other context.

## Standing orders (user directives, all still in force)

- **"keep going until ALL replays are 100% parity. until then do not stop."** Proper general fixes aligned with `original-code/` C++ — no per-trace hacks. See memory `project_parity_campaign`.
- This session is already running on the remote server: do **not** SSH. All builds and sweeps run directly in this sandboxed checkout.
- Prefer debug for local investigation, but individual long replays may use release when the longer link is cheaper than debug execution; decide per repro.
- Never `git stash`. Never pipe cargo output through filters. No clippy in worktree agents. Preserve all unrelated tracked and untracked worktree state.
- Agents validate 2-4 key repros + focused controls; the local release full-universe sweep is the wide regression check.
- Consult `docs/SNAPSHOT_INPUT_AUDIT.md` for the determinism/ownership contract.

## Score

- Current authoritative universe: **4,761 traces**, generated by `scripts/build_parity_full_snapshot.sh` (2,283 retained old 60s captures + 1,350 schema-14 replacements + 399 schema-12 + 729 random-save). `parity-save-replays/` schema-11 is permanently retired; old captures replaced by schema-14 are listed in tracked `docs/PARITY_RETIRED_SAVES.txt`, while twelve individually malformed or current-Original-inconsistent records are listed in `docs/PARITY_RETIRED_TRACES.txt`. The historical 5,164 denominator below predates these canonical retirement manifests.
- Full-universe discovery sweep for frozen runner `e806655f8` completed across all 4,761 traces: **2,583 exact passes (54.3%)**, 0 timeouts, 59 classified panics, and 1 other error. Classification is `output/parity-audits/full-sweep-e806655f8/classification.json`; largest clusters are actor.motion_state 126, actor.animation 116, visibility_queries.length 114, position_goal_map.x 94, and direction_goal 84. This runner predates tasks #116 onward and therefore materially understates current parity.
- Current full-universe sweep for frozen runner `d7d414c1e` completed across all 4,761 traces: **2,865 exact passes (60.2%)**, 1,547 state-divergence exits, 349 exit-101/RNG or invariant failures, and 0 timeouts. Classification is `output/parity-audits/full-sweep-d7d414c1e/classification.json`; largest live clusters are actor.animation 115, visibility_queries.length 101, AiRandomValueRectangle 76, VipIdleRemark 73, direction_goal 73, direction 69, and movement_map.x 63. This is the authoritative post-task-158 baseline and confirms both task-36 controls remain in the passing set.
- Full-universe sweep 14 completed for frozen runner `7675ff084` across its then-current 4,773-record snapshot: **1,942 pass, 2,477 ordinary divergence exits, 354 RNG/panic exits, and 0 timeouts**. Classification is `output/parity-audits/full-sweep-14-7675ff084/classification.json`. This is a discovery baseline rather than the current score: it predates tasks #71–#79 and includes the seven records now retired individually.
- Full-universe baseline `resweep-feefc57f5` completed locally: **2,400 pass, 1,972 state divergence, 394 RNG divergence, 7 contention timeouts** across all 4,773 traces. Runner SHA-256: `d7e423d3dd89f3e51ef0e79f231e165d06d98f09c36202823093c0bb5bfd7300`; frozen snapshot SHA-256: `84fbe90b42200b21cbb4fb37bc92675b8f6dbb3767ca359e2a04fcdbe83adcb4`. Classification is `output/parity-audits/resweep-feefc57f5/classification.json`. This runner predates #31 and tasks #28/#34/#38/#40–#45, so it is a discovery baseline, not the current score. All seven controls were rerun concurrently against frozen current runner `36acefeb4` with 1,200-second limits and all seven again exited 124. Tasks #52, #53, and #57 subsequently proved every control was the same EnterSwordfight translation-loop family; current main clears all seven former timeouts.
- Last complete sweep (re-sweep 12, runner db67cc978): **2,390 passing (46.3%)**, 2,418 failing. Ledger entry committed in `docs/ORIGINAL_PARITY_REPLAY.md`.
- Failing-trace depth profile (sampled n=99): traces are ~1,384 frames; failures diverge at mean 630 frames = **mean 46.4% / median 41.4% progress**; 15% of failures get past 80%.
- **Re-sweep 13 DONE** (finished after the dump; results fetched + classified + ledger-committed): 31 passes, 1,948 state-div, 431 rng-panic, 8 timeouts → **cumulative 2,421/5,164 (46.9%)**. Classification: `output/parity-audits/resweep-69df41aeb/classification.json` (top: actor.animation 113, rng:VipIdleRemark 80 [mislabeled swordfight — task #38], direction_goal 79, position_goal_map.x 73, movement_map.x 71, motion_state 70, detectables.length 69).
  - Sweep 13 measured main @69df41aeb (has #12,#14,#18,#26,#29,#32). Merges AFTER it: #33,#27,#25,#30,#19 → need re-sweep 14.
  - **Re-sweep 14 MUST be full-universe (all 5,164)** — failure-only manifests are blind to regressions on passing traces, and we have a confirmed regression (task #36).

## Main branch state

main @ HEAD includes today's merged fixes (all suite-green, `cargo test -p robin_engine --lib` = 2629/0):
- #12 corpse-walk arrival predicate (committed-step + rebuilt-increment dot-product vs `dist<=speed` prediction) — merge of wt-fix-corpsewalk cc3737b28. Six traces to exact EOF.
- #13 (earlier) + #29 seek/stop-transition arms + #32 FaceOpponent determinant-form Angle + #33 seek-refresh queued-effect drop — branch wt-fix-orderlag (5cdc8d639, bccb27806, 94e32d620, 708abb56e).
- #14 ladder-transition pick + #27 ladder-fall flight kinematics — branch worktree-agent-a43843b306107ea87 (91be78873, 349502f46).
- #18 battle_decisions (us-list PCs, per-iteration swap commits, avenger fallback, detection.rs camp bug) — branch worktree-agent-a7bd1cc12fe038a73 (37e5149b1..b50a7b200).
- #25 hero forbidden-expression timer aging moved post-hourglass + set_focus(0)→unfocus — branch worktree-agent-acf2bcdcdeebac468 (ba78cf865).
- #26 test suite fully repaired (2629/0) — branch worktree-agent-a4ead08f9f8668510 (2495ac7a6, 18c810f10, 097eda962). ~176 tests fixed; production fixes: WorldState creation-order map `any_key_map_sized` serde adapter; "command level must stay at 2" was a BORN-BROKEN fixture, not a regression.
- #19 reach-point half: five Think-dispatch sites live-read animation instead of latched installed_order (mpOrder mirror) — deletion fix, branch wt-fix-noisetiming (da65a5097).
- #30 TakeCorpse: invented face-the-corpse snap deleted from begin_carry + FarmerCarry wrongly selecting LittleJohn carried-animation set — branch worktree-agent-ac0fca79adc9b7209 (497eb4ad0, 62ea32191). Merge had one trivial comment conflict in abilities.rs, resolved.

## Task #36 seek-RNG regression — resolved locally, pending re-sweep 14

Four traces that passed at `db67cc978` regressed before this fix with seek-RNG draw-count storms:
- Cyrdach_001 replay-009 (f1653, SeekPointSelection storm, draws 5251..5813)
- Nescafe_001 replay-009 (f1589, same)
- linux2/Profile_002/Savegame_000/replay-014 (f5379, SeekPointAcceptance)
- linux2/Profile_002/Continue/replay-013 (f7581, SeekPointAcceptance)
All were confirmed on the pre-fix HEAD by two independent A/B tests (not caused by #25 or #30).

The exact boundary is `69df41aeb` (both controls pass) to `4cfae5aa0`, whose only topic change is #33 (`708abb56e`). Dropping the stop transition's queued `Waiting` effect is faithful, but exposed an older staging error: Rust applied the resulting `RefreshSeek` after line-crossing callbacks, whereas Original `PerformSeek` refreshes synchronously before `RHElementActor::Hourglass` calls `CheckForLineCrossing`. The fix applies the replacement before crossing resolution and retains the already-committed crossing segment on the walking refresh path. Cyrdach replay-009 and Nescafe replay-009 both match exact EOF in the patched debug runner. Re-sweep 14 must still be full-universe to verify the passing set broadly.

## KILLED agents — session terminated by user; agents are NOT resumable. Respawn fresh.

The whole session (coordinator + subagents) was killed on 2026-08-06 ~14:20. The old session's agent transcripts and task store are gone for practical purposes, BUT everything needed is archived in-repo:

- **`docs/parity-task-archive/*.json`** — full copy of the task store (all 40 tasks with their accumulated per-task analysis in the descriptions). Read these instead of TaskGet; recreate open tasks in the new session's board from them.
- **`docs/parity-task-archive/wip-fix-jump-task20.patch`** (363 lines) — fix-jump's UNCOMMITTED mid-diagnosis work on task #20 (engine/jump.rs + sprite.rs, in worktree agent-a2b48d6a0f1b3ebb0 at stale base 3d42ce0ad). Mostly instrumentation + partial fix; treat as reference/head-start, re-derive on current main. It was last dumping frames 790-800 of its repro to compare Rust step-entry sequencing.
- **`docs/parity-task-archive/wip-fix-ladderpick2-task21.patch`** (47 lines) — fix-ladderpick2's uncommitted start on task #21 (movement.rs + tick.rs, worktree agent-a43843b306107ea87, base = merged main 9423d267a).
- **fix-seekregress (task #36, URGENT)**: worktree agent-ae66cda91de4ba9d5 is CLEAN at base 3d42ce0ad — the bisect produced no commits and no report. Nothing to salvage; restart #36 from the task description (it has all 4 repro traces + suspect list). This is the highest-priority item.
- **Task #39 completed**: the retained FIFO was already correctly ordered `CallAlert -> CallLookThere -> EventHear`; the actual mismatch was an invented Rust receiver-side gate that rejected `CALL_LOOKTHERE` after the alert changed state. Original handles delivery unconditionally after sender selection. Both controls clear the heard-steps substate/view failure; later direction-goal residuals are separate.

Old worktrees under .claude/worktrees/ (agent-* and fix-*) whose branches are fully merged can be `git worktree remove --force`d to free disk; keep the two with WIP patches until their tasks land. All merged branch names appear in `git log --oneline --merges` on main.

## Task board (open items)

- #9 completed: reciprocal archer/shield links now clear synchronously like Original; both f762/f794 boundaries clear and advance to independent residuals.
- #20 completed: retained Original's Execute-time airborne increment and synchronous post-jump `motion_state=InProgress`; primary linux2 schema-14 replay-004 exact EOF. SuN Aborted-vs-Terminated signatures are a separate non-jump MoveOk lifecycle family.
- #21 completed as superseded/no-fix: four archived elevation/projectile controls reach exact EOF on task-36 main; remaining symptoms route to arrow lifecycle, shield-command, and movement families.
- #23 completed: `ForecastDestinationForIA` now preserves the independent direct-passage latch after the live door clears; both primary Seek RNG boundaries advance to separate later door/building residuals.
- #24 completed as source-verified no-op: shipped proto loading never registers `RHSectorAssociated` (both Original lift `AddSector` calls are commented out); Rust already discards the dead click polygon.
- #28 completed: lateral strikes seed Original's ground-space direction/full-3D range, and observation attack-opportunity checks use Original's isometric target direction. The latter clears the extra CombatObserveSideStep draw in both controls; replay-002's later f14266 frontier is #46.
- #31 completed: threaded owner-boundary `Position(enemy)` into `EVENT_VIEW` and made `SeekingCombatAlert` ignore `EventTimer`; the two archived list-us symptoms were separate timing/position causes, not a shared 360-LOS composition bug.
- #34 completed: Strangle translation now preserves the Moving action state from synchronous post-seek arrival, matching Original's append-only PC::Translate path and clearing nicouzouf/S069 r005 f457. Its next f463 motion/sprite residual is tracked separately as #42.
- #35 completed: Rust-only LeaveSpy/LeaveTree commands inherit Original CROUCH_UP's Preference priority; Original appends the cape/tree transition order to that same posture-toggle element.
- #37 completed: retired the old nicouzouf_061 save family in `docs/PARITY_RETIRED_SAVES.txt` and the local `tmp/regen14-saves.txt`; all 15 schema-14 replacement traces already exist (the 11 affected old-schema captures lacked `hero_refused_action` records and were unfixable as recorded; original-code commit d494273).
- #38 completed: Smalltalk back-hit now uses Original's isometric facing vector, restoring ReceiveSwordDamage and its protection draws. Representative controls for both UpdateSwordfightDistance offsets, EvaluateSwordfight, FindPositionForTableSwordfight, and protection calls all reach exact EOF; those labels were downstream topology symptoms of the same diagonal-facing bug.
- #39 completed: removed invented receiver-side `CALL_LOOKTHERE` state gate; Original delivery is unconditional after sender selection. Both controls clear the heard-steps substate/view failure and expose a separate direction-goal residual.
- #40 completed: The16thFrame now reads ActorData::installed_order after synchronous detection callbacks instead of using the NPC owner path's pre-detection snapshot, matching Original's live GetAnimation boundary.
- #41 completed: opening a scroll now forces the live BonusThree sprite at the shared SetStatus boundary, clearing the repeated Scroll(111) row/frame family found in 25 completed baseline controls.
- #42 completed: Strangle's two pre-action TurnFast short-circuit arms now retain Original's due victim virgin increment, clearing the real Soldier197 frame skew; the comparator also excludes only out-of-range uninitialized Original motion telemetry. The next f465 visibility/combat frontier is #45.
- #43 completed: legacy sprite adoption now preserves Original's profile-switch-before-position ordering, including the transient row computed from the constructed mission direction when the saved alternate-profile flag differs.
- #44 completed: final point-target Seek arrivals launch attached post-seek actions, and DropAle now uses Original's authored animation/DONE lifecycle and exact bottle placement/initialization. Three cross-family controls reach EOF.
- #45 completed: removed the invented Attacking-state gate from BeginSwordfight, restoring synchronous EVENT_GOTHIT retaliation at f465. The later frame1299 RNG frontier is #47.
- #46 completed: smalltalk strikes now route to the bespoke Human animation executor instead of the specialized melee owner, restoring the missing action-DONE damage/protection pair; the exact replay reaches EOF.
- #47 completed: fighter snapshots now use Original's committed gate-side position during door passage, clearing the extra SwordStrikeSelection draw at frame 1299; the next unrelated frontier is Soldier 187's AI substate at frame 1332.
- #48 completed: `CMD_CHANGE_WAY` now preserves Original's explicit second BreakMacro + ReturnToDuty tail after AssignNewPatrolPath's synchronous callback, restoring the duplicate path request in 39 controls.
- #49 completed: the complete fighter registry retains dead PCs for held Original pointers, and ShootArrowAt no longer invents a dead-target admission guard. The frame-1515 panic and command/animation mismatch clear; the same-frame extra post-shot seek RNG is #50.
- #50 completed: queued bow shots retain active human targets that died while aiming, matching Original's ShootArrowAt and clearing the extra SeekArea/SeekPointSelection batch. The next projectile-position frontier is #54.
- #51 completed: forced battle decisions persist after use, matching Original's never-cleared `mforcedNextBattleDecision`; nicouzouf/S069 replay-005 reaches exact EOF.
- #52 completed with its pending-query interpretation superseded by #63: the sequence manager retains the translating actor selection through synchronous condolence bookkeeping, but that actor pointer is not an entry in Original's launch list. The faithful recursion fix is the direct rebalance model in #63.
- #53 completed as superseded by #52: Restart replay-008 had the identical unbounded EnterSwordfight loop at frame 1423 (Soldier 80); current main clears it and reaches a normal visibility-query frontier. Savegame_054 replay-008 also now clears its former timeout and reaches a frame-1696 visibility frontier.
- #57 completed as superseded by #52: all four older-schema timeout controls were the same EnterSwordfight loop. Current main clears all seven baseline timeout controls; three expose ordinary state frontiers and one advances to a later frame-47910 RNG frontier.
- #54 completed: ground impact evaluation now mirrors Original's mixed-precision dominant-axis equation (`FLOAT` coefficients with `DOUBLE` secondary-coordinate evaluation), clearing Projectile192's exact-bit residual and every later frame. A pre-existing frame-776 visibility-query count remains as #56.
- #55 completed: an entry movement that becomes `Impossible` now preserves Original's authoritative `Aborted` Execute result across a synchronous condolence-installed successor; the representative clears frame 835.
- #58 completed: the sequence phase now records actual accepted Actor instruction boundaries, including zero-order elements that terminate synchronously, preserving Original's `InProgress` edge at frame 973.
- #59 completed: redundant `EnterSwordfight` preserves the already-running `WaitingSword` order across its terminal callback and transfers sprite execute identity to the replacement Wait; the frame-2141 animation/frame-2142 sprite lag clears with exact RNG.
- #60 completed: `AssertPosition` now exposes its pointer-changing `Skip` barrier, so Actor::Instruct skips the accepted-motion/order epilogue after Translate-time `SetState`, clearing the frame-1073 civilian motion frontier.
- #61 completed: decision-local swordfight target multiplicity now saturates as occupancy instead of stacking duplicate nearby-friend claims, clearing Soldier 115's hidden target-choice divergence and the frame-1162 visibility frontier.
- #62 completed: transition-distance movement now refreshes forecasted displacement through the same post-motion rule as ordinary walking, restoring bow target lead at frame 1216; the replay advances to an independent `VipIdleRemark` RNG frontier after frame 1600.
- #64 completed, promotion interpretation superseded by #68: task #67's corrected Stop callback ordering restores the frame-1600 ReconsiderSwordfight/ProposeGoodSwordStrike path without synchronously promoting the reciprocal Enter. Original non-WAIT `LaunchSequenceElement` leaves that element on the manager FIFO.
- #63 completed: `SequenceElementIsAboutToBeLaunched` no longer treats the translating actor pointer as queued, and `ReconsiderSwordfight` rebalancing now calls the direct engagement path instead of launching a recursive Enter command. The real visibility call is preserved, both controls clear their reported frontiers, and the former loop does not recur.
- #65 completed: human `PlayAnim*` translation now reaches the ordinary accepted Actor::Instruct epilogue instead of returning a `Skip` barrier, restoring the `InProgress` motion edge. The retained SuN replay-014 matches every recorded frame through EOF.
- #66 completed: direct `ReconsiderSwordfight` entry bypasses command-only opponent preparation, and terminal already-satisfied Enter translation now exposes a `Skip` barrier before the accepted-motion epilogue. The linux3 Profile_001 Savegame_013 replay-006 control matches every recorded frame.
- #67 completed: PrepareToEnterSwordFight now closes the selected Stop callback before snapshotting pending work, then stops each captured entry with its synchronous callback before continuing. Frame 1601 state and reciprocal links match, and the replay advances exactly to an independent frame-1837 PC motion frontier.
- #68 completed: `CalculateOpponentOfMauriceWhoIsNearestToRene` now compares Original's `UWORD`-truncated raw map-position MaxNorm distances, preserving first-opponent order on fractional ties. This prevents Soldier 84 from taking the wrong direct-rebalance branch and clears the frame-866 ReconsiderSwordfight RNG deficit; the exact replay advances to an independent frame-920 Soldier 80 position-goal frontier. The source-wrong reciprocal-Enter promotion from #64 was removed; #64's control still clears its former frame-1601 RNG frontier and advances to the independent frame-1837 PC motion frontier. Both task-36 replay-009 controls remain exact through EOF.
- #69 completed: orphaned sword movement now registers a plain deferred `QuitSwordfight` from Execute, leaving its lowering translation and `InProgress` projection to the later manager Instruct boundary. The linux3 Profile_001 Continue replay-001 control now matches every recorded frame through EOF.
- #70 completed: synchronous owner dispatch now propagates the original Actor::Instruct pointer-change barrier through movement, Seek, QuitSwordfight, smalltalk, turn/attention/wait, parry, position assertion, swordfight entry, and damage translators. Source-explicit terminal exits skip the shared epilogue, while accepted work publishes `mpOrder` and stamps motion `InProgress` even when an empty translated order terminates immediately afterward; fifteen focused synchronous regressions and the manager-phase edge tests pass.
- #71 completed: redundant EnterSwordfight retention is now discarded when it remains unconsumed at the end of SequenceManager::Hourglass. A retained WaitingSword can bridge only to a re-entrant actor-Hourglass lazy Wait, as in #59; manager-dispatched work is already past every actor slot, and Original's terminal condolence leaves `mpOrder` null when no replacement steals selection. This clears the broad Cyrdach/Nescafe actor.animation frontier without weakening the consumed sprite-identity handoff.
- #72 completed: accepted damage translations that produce no order now reproduce Original Actor::Instruct's epilogue ordering: stamp motion `InProgress`, detach `mpSequenceElement`, then terminate the element. A successful second parry can therefore resume postponed movement without its terminal condolence clearing that movement's live goal. The linux3 Profile_003 Restart replay-004 control clears Soldier 80's frame-920 position-goal frontier and advances to an independent frame-968 movement precision mismatch.
- #74 completed: recorded group moves now translate Original sparse sector identities through the retained topology map instead of requiring the recorded waypoint to lie inside the mapped polygon. Door/jump overlays still use semantic spatial selection; three Cyrdach/Nescafe schema-12 controls reach exact EOF.
- #77 completed: WAKING_UP DONE now dispatches synchronous NPC `EVENT_FITAGAIN` only when non-forced concussion clearing actually wakes the target. Original intentionally leaves tied, carried, or script-locked NPCs unconscious while still issuing the unconditional follow-up Wait; the linux2 Profile_002 Savegame_008 replay-006 invariant panic is cleared.
- #73 completed as source-verified no-fix: four retained schema-12 Cyrdach controls omit PC creation-order 89's required entry into Sherwood deployment zone 14. At the exact `(1546.740845,400.692596) -> (1549.510132,399.538757)` movement, current Original `SBGeoPolygon2DArray::IsInside` and Rust both change outside-to-inside. Original `SBGeoSegment2D::IsIntersecting` also necessarily accepts the active script edge `(1771,648.00037) -> (1526,375.00034)`: endpoint determinants are `+632.35547/-406.35156`, the reciprocal pair is `-943.62518/-95.08084`, and the old-position determinant is nonzero. `RHelementactor.cpp::CheckForLineCrossing` therefore dispatches `RHSectorScript::Enter`, contrary to the archived traces. Disabling the schema-12 beam-PC identity remap did not change the divergence. A proposed layer/motion-sector guard was rejected because Original uses `IsReallyInside` only for bulk initialization and post-flight reconciliation, not ordinary line crossing. These four records are excluded individually by `docs/PARITY_RETIRED_TRACES.txt` until they can be re-recorded with the current Original build/assets; no Rust trace-specific suppression was added.
- #79 completed as corpus hygiene: three old-corpus linux3 recordings contain valid zstd streams but truncated JSONL records and panic the reader with EOF mid-value. A per-trace retirement manifest excludes only those malformed recordings (plus the four #73 source-inconsistent records) without retiring their otherwise-valid neighboring save/replay families.
- #75 completed as source-verified capture-required/no-fix: the schema-12 Nescafe H12 `RuntimeBuildingExitWait` cohort lacks the macro timer, cursor/remaining, and busy-state fields needed to distinguish the recorded dormant state from a timer that should resume. In the representative Profile_001 Restart replay-002, Rust Civilian 85 enters sector 166, arms `WAIT 250` and then `WAIT 50`, and consumes the extra exit-wait pair before frame 508; Original Civilian 85 remains inactive in `Wait`/`DefaultInMacro`, while Original's pair at frame 526 belongs to Civilian 95 as it changes to `WaitTimer`/`DefaultEnroute`. Current Original source ticks inactive actors, always arms `CMD_WAIT` timers, and polls macro timers, so it provides no basis for an inactive-actor or timer-suppression exception. A fresh schema-14 Original capture is required before any production change; the available Original executable is i386 and this host currently lacks `/lib/ld-linux.so.2` plus its 32-bit SDL/freetype/theora/vorbis/ogg/crypto/C++ runtime dependencies. Retire or replace these old controls only after that recapture.
- #80 completed: combat-flight displacement now runs at the falling actor's live creation slot instead of the later global melee batch, matching Original `ExecuteFallingHit`/`PerformFlight` ordering. In Nescafe Profile_001 Savegame_001 replay-008, unconscious Soldier 177 (creation order 208) now commits `(1468.115479,2764.299805)` before Soldier 179's detection slot (creation order 210), so the body visibility endpoint and resulting direction 11 match at frame 382; the trace stays exact through frame 1105. Nescafe Profile_002 Savegame_001 replay-015 also clears its frame-1518 visibility destination and advances to an independent frame-1603 AI lifecycle frontier. Terminal landing reconciliation remains after synchronous completion callbacks to retain the already-exact final `NewMove` delta; a focused test verifies owner-scoped flight advances only the selected creation slot.
- #82 completed: generic per-Think data now populates sleeping-enemy candidates from the live, creation-ordered opposing fighter registry, matching Original `KillNearbySleepingEnemies` even on timer-driven `BattleDecisions` without a detection aggregate. The SuN1Sh1nE Profile_004 Savegame_034 replay-014 control clears its frame-1036 missing sleeping target and compensating MacroRand divergence, advancing to an independent PC motion-state frontier at frame 1438; both task-36 replay-009 controls remain exact through EOF.
- #84 completed: observation side-step crowd scoring now builds the soldier's facing vector with Original's `SetSector0to15(direction, ASPECT_RATIO)` geometry before taking the perpendicular and truncating scalar products to `SWORD`. The previous ordinary unit-circle vector reversed Soldier 178's chosen side-step in Nescafe Profile_001 Savegame_001 replay-008 at frame 1106 despite an exact RNG cursor. The corrected debug runner matches every recorded frame through EOF; all 84 focused enemy-AI tests pass.
- #86 completed: a soldier reaching a nearby officer now preserves Original's synchronous `SeekingRunningToOfficer -> SeekingRunningToOfficerSeen -> Think(EVENT_REACHPOINT)` lifecycle instead of substituting a one-frame timer. In Nescafe Profile_002 Savegame_001 replay-015, the target officer was already back in `Default`, so Original immediately returned Soldier 131 to duty at frame 1603 while Rust exposed the transient Seen/Yellow state. Reentrant self-stimulus delivery now performs the source-authored same-frame callback; the debug replay matches every recorded frame through EOF and all 85 focused enemy-AI tests pass.
- #88 completed: anti-collision snapshots now read the target exemption directly from `PositionInterface`, matching Original's serialized `RHPositionInterface::mpTargetElement`, and every newly initialized sprite motion copies its order antagonist there. Friday cleanup may legitimately remove an interrupted sequence while Rust's derived `active_movement` latch still names it; snapshots no longer dereference that stale latch. The linux3 Profile_001 Savegame_045 replay-008 control clears its frame-44032 panic and advances to an independent detection-list frontier at frame 44569; focused anti-collision and motion-initialization tests pass.
- #91 completed: finishing a PC's beggar-entry transition now invokes Original's `AddBeggarForAllIntelligentSeekingSoldiers` side effect after toggling nearby coin flags. The ordered scan appends a unique Beggar detectable only to Lacklandist soldiers with IQ at least 30 whose live substate belongs to `_ANY_SEEK_AREA_SUBSTATE_`. In linux3 Profile_001 Savegame_045 replay-008, PC 344's frame-44569 DONE edge now registers with seeking Soldier 118, clearing the detectable-list mismatch and advancing to an independent command/animation frontier at frame 44976; a focused eligibility and uniqueness test passes.
- #93 completed: reaching a beggar now launches Original's single two-level identification sequence: level 1 `TurnFast` with the live beggar direction, followed by level 2 `StartMenace` for melee soldiers or `EquipBow` for archers. Rust previously emitted a separate face order plus two independent command sequences, so `StartMenace` interrupted both turns during the same drain. The linux3 Profile_001 Savegame_045 replay-008 control clears Soldier 118's frame-44976 command/animation mismatch and advances to an independent Civilian 63 BeggarShowFace frontier at frame 45007; focused melee and archer ordering/direction/timer coverage passes.
- #95 completed: NPC `BeggarShowingFace` now falls back to the `Rolling` sprite animation when the current profile lacks the authored showing-face animation, exactly matching `RHElementActorNPC::Execute`. The order remains `BeggarShowingFace` so its posture and completion dispatch is unchanged. The linux3 Profile_001 Savegame_045 replay-008 control clears Civilian 63's frame-45007 abort and advances to an independent Soldier 119 turn-direction frontier at frame 45041; focused present/missing-animation coverage passes.
- #97 completed: downward leaning-out bow shots now initialize their facing from the shooter and live antagonist `PositionMap`, matching the soldier override instead of reusing the Human arm's `PositionGround` sampling. The selected bow owner already performs the source-authored per-tick `Turn()` and freezes the shooting animation's first frame while turning; feeding it the correct map-space goal makes Soldier 119 advance from direction 14 to 13 toward goal 10. The linux3 Profile_001 Savegame_045 replay-008 control clears frame 45041 and advances to an independent projectile spawn-Y frontier at frame 45043; focused goal/turn/row/frozen-frame coverage passes.
- #100 completed: downward leaning-out bow launch points now use Original's isometric `GetDirectionVector` geometry for their 20-unit forward shift. The former unit-circle vector left diagonal projectile Y off by 4.65 units while X happened to match. The linux3 Profile_001 Savegame_045 replay-008 control clears frame 45043 and advances to an independent StopMenace/path frontier at frame 45056; focused diagonal coordinate coverage passes.
- #102 completed: AI `GoTo`/`GoNear`/`GoToSpeed` issued from `ActionState::Menacing` now preserve Original's single ordered sequence with level-1 `StopMenace` before the movement. Rust previously emitted a standalone stop channel followed by an independent move launch in the same drain, allowing the move to preempt the menace-exit transition and queue a path immediately. The serialized movement intent now carries the prefix through both local and gate-path builders, alongside the existing quit-swordfight/lower-shield prefixes; the standalone channel remains available to non-GoTo producers. The linux3 Profile_001 Savegame_045 replay-008 control clears Soldier 118's frame-45056 command/path mismatch and advances to an independent Soldier 140 motion-state frontier at frame 45294; the task-36 Cyrdach and Nescafe replay-009 controls remain exact through EOF, and focused intent round-trip/exact level-ordering tests pass.
- #92 completed: the current actor's anti-collision snapshot now refreshes its target exemption immediately after `PerformMotion` initializes a new order, matching Original's serial `mpTargetElement` update before the same-slot repulsion query. A transition order with no antagonist previously inherited the preceding seek order's target for one extra Rust frame, suppressing the target's repulsion and choosing direction 10 instead of 12. Both Cyrdach Profile_156 replay-006 controls (Restart and Savegame_000) now match every recorded frame.
- #89 completed as source/assets-verified no-fix: three old nicouzouf Savegame_051 captures record newly spawned arrows on layer/sector 0, but their exact current-asset landing points `(590.03455,564.7571)`, `(632.18756,520.73846)`, and `(591.2044,534.9738)` lie inside motion obstacle sector 18 as well as ground area 0. Current Original `ComputeTrajectory` iterates both sectors, first assigns area 0, then clears the sector and returns on the non-area obstacle before `SetLayer`, leaving layer `0xFFFF`/sector null exactly as Rust does. The arrow's recorded first-frame position and movement match Rust bit-for-bit, so the source-relevant trajectory is not divergent. The three contradictory captures are retired individually pending recapture; no production suppression was added.
- #78 completed: AI `Focus` now retains Original's generic-element semantics instead of incorrectly asserting that every target is human. This permits the Ale handler's legitimate focus on a Bonus object while still panicking for a missing raw entity slot. SuN1Sh1nE Profile_004 Savegame_034 replay-014 clears the frame-1008 invariant panic and advances to an independent frame-1035 `VipIdleRemark` RNG frontier; a focused object-follow regression covers the boundary.
- #81 completed: Human's retained PC shoot list now re-admits a `Terminated` element as the retail Original does. Original serializes/restores the raw pointer and guards it only with a debug assertion in `Actor::Instruct`; Rust's ordinary manager terminal-state rejection remains intact, while the narrowly scoped held-shoot admission proceeds without rewriting the terminal state first. linux3 Profile_001 Savegame_022 replay-012 clears its invariant panic and advances to a frame-738 sprite frame-count residual.
- #101 completed: deferred attentive-mode requests now preserve the first request for an unchanged target, matching Original's synchronous `will_be_attentive` update. The 12-trace linux3 Savegame_016 officer-leave cluster clears frame 7670 and advances to the independent frame-7728 visibility-query frontier; both task-36 schema-14 replay-009 controls remain exact through EOF.
- #83 completed, refining #81: retail does bypass `Actor::Instruct`'s entry assertion for a retained terminal shoot pointer, but its explicit post-`GenerateTransition` terminal guard still rejects the element before arbitration/translation. Rust now preserves that guard, the existing command-Wait/AimingWithBow order, and the referenced terminal backing allocation across Friday cleanup. linux3 Profile_001 Savegame_022 replay-012 clears the frame-738 restart and frame-768 cleanup boundaries, advancing to an unrelated frame-999 PC position-goal frontier; both task-36 replay-009 controls remain exact through EOF.
- #85 completed: Human HIT translation now mirrors Original's synchronous `Think(EVENT_STOP)` callback when the antagonist is moving, after installing the Hitting order and before action initialization. The callback halts the victim, enters `SeekingGotStopEvent`, and blinks its enemy detection. That substate now also preserves Original's fallthrough from the no-op attentive-mode arm into `SetAlertStatus(ALERT_YELLOW)`. Nescafe Profile_002 Savegame_001 replay-002 clears its frame-686 Soldier181 state/animation/visibility/alert boundary and advances to an unrelated Soldier180 attentive-mode frontier at frame 1651; both task-36 replay-009 controls remain exact through EOF.
- #87 completed: standalone `IsDetecting180Degrees` now builds its viewer and target endpoints from the actors' raw element anchors, matching Original's `ComputeEyesPoint`/`ComputeDetectionPoint`, rather than the AI-facing `Position()` that may substitute a door endpoint or carrier. This restores Soldier180's clear frame-1651 ray to PC252, keeps `TooProudToAttackApproach`, and queues the next Move instead of entering attentive overview. Nescafe Profile_002 Savegame_001 replay-002 and both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #90 completed: `HeyFolksLookThere` now evaluates recipients' live AI state/range in Original creation order at the synchronous owner boundary, rather than preselecting from the top-of-tick snapshot. Patrol subordinates also preserve Original's direct call to the chief's `DispatchStimulusToWholePatrol`: an already-Attacking chief rejects the delegation without receiving an invented `Think(CALL_LOOKTHERE)`, while delivery remains unconditional after a recipient is selected as established by #39. The nicouzouf Profile_001 Savegame_067 replay-008 frame-761 double-Wondering frontier clears and advances to an independent frame-815 PC107/Soldier72 EnterSwordfight frontier; both task-36 schema-14 replay-009 controls remain exact through EOF.
- #94 completed: `IsTooProudToAttack` now uses Original's full-world, isometric-Y `MaxNormDistance` for its maximal-sword-range gate instead of a raw 2D map max norm. At the nicouzouf Profile_001 Savegame_067 replay-008 frame-815 geometry, that restores Soldier72's `AttackingTooProudToAttackRetire` decision and prevents the incorrect PC107/Soldier72 swordfight links. The replay advances to an independent frame-862 Soldier61 path-goal frontier; both task-36 schema-14 replay-009 controls remain exact through EOF.
- #96 completed as source-verified no-fix: nicouzouf Profile_001 Savegame_067 replay-008's frame-862 `(725,539)` seek goal contradicts the current fullgame_linux HOLE data and shipped `SeekArea`. The exact frame-701 center/direction makes point 29 `(665,677)` the mandatory primary directional point; point 62 `(725,539)` is only a fallback. No seek lock, door teleport, mutable-position write, or path-goal adjustment can produce the archived choice. Replay-009 from the same save records the same impossible point-62 choice at frame 723. Both stale traces are retired pending re-recording with the current Original build/assets; no Rust workaround was added.
- #99 completed: Enemy suspect cooldown now follows Original's `uwSumOfSharpnesses == 0` condition instead of requiring that no enemy be visible. An already-latched visible target contributes no fresh sharpness, so Savegame_linux2 Profile_002 Savegame_018 replay-011 frame 8660 now cools Soldier65's Enemy suspicion from 40 to 39 and the replay matches every recorded frame through EOF. Both task-36 schema-14 replay-009 controls remain exact through EOF.
- #103 completed: `AddBeggarForAllIntelligentSeekingSoldiers` now tests Original's difficulty-modified `GetIQ()` instead of raw profile IQ. On Hard, Savegame_linux2 Profile_002 Savegame_016 replay-013 frame 23146's Soldier104 reaches the threshold with base IQ 15 and registers newly disguised PC172 in its Beggar detectable list, restoring the missing 67th entry. The representative and both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #104 completed: the `OwnerActionBarrier::Skip` handling added in #76 for `ParrySword`, `ParrySwordLow`, and `StopParrySword` also clears linux3 Profile_001 Savegame_045 replay-008's later frame-45294 motion-state frontier. Soldier 140's parry-exit sprite correctly returns `Start`; a same-frame redundant `ParrySword` generates its exit prefix and terminates during `Translate`, and the barrier prevents the accepted-`Instruct` epilogue from overwriting that edge with `InProgress`. A focused manager-path regression now covers the exact generated-prefix lifecycle. The representative and both task-36 replay-009 controls match exact EOF.
- #105 completed, superseding #75's capture-required disposition: a fresh schema-14 recapture exposed the earlier source-faithful boundary. Outside-Think multi-point patrol-macro completion now applies its outer flag cleanup after the synchronous already-on-point `EventReachPoint` recursion, preserving the nested WAIT deadline but clearing both macro-running flags. The recapture reaches exact EOF; the frozen representative clears frame 508 and advances to frame 1495, the secondary AiRandomValueRectangle sample clears frame 507 and advances to frame 634, and both task-36 controls remain exact.
- #106 completed: combat-position damage estimation now uses the defender's live facing for localized sword protection, matching `RHSword::GetProtection`; the candidate-adjusted facing remains limited to Original's from-behind heuristic. This removes a false sector-12/sector-13 score tie for SuN1Sh1nE Profile_004 Savegame_036 replay-002 at frame 672, and the replay advances to an independent frame-898 command/substate frontier. Three neighboring direction-goal controls retain their known later unrelated frontiers.
- #107 completed: legacy-save lateral sword sweeps now lazily rehydrate executable `ActorData::sweep_state` from the serialized human sweep mirror and synchronize consumed victims and angles back into that mirror. SuN1Sh1nE Profile_004 Savegame_009 replay-001 and both task-36 schema-14 replay-009 controls match every recorded frame through EOF; sibling replays 005 and 013 also clear the former Soldier134 animation frontier.
- #109 completed: patrol-wide `EVENT_GET_ARROW` handling now preserves Original's unconditional `Focus(mpInterestingObject)` call. A null interesting object therefore clears the temporary point Stare installed by a nested `CALL_LOOKTHERE`, restoring LookForward for each later patrol recipient while leaving `SeekingArrowReactiontime` intact. SuN1Sh1nE Profile_004 Savegame_011 replay-001 matches every recorded frame through EOF.
- #108 completed: final building `Move|SEEK` sequence tails now retain Original's `RefreshingSeek` non-animation instead of taking the ordinary interior-move termination path. The explicit hold unconditionally calls `RefreshSeek` on the following owner slot, allowing `SEEK_IN_BUILDINGS` to snap to the target and launch its post-seek action. linux2 Profile_002 Savegame_031 replay-002 clears the frame-11906 Move/animation frontier and advances to an independent frame-13016 Soldier58 position-goal mismatch; both task-36 schema-14 replay-009 controls remain exact through EOF.
- #110 completed: the manager Move/Seek dispatcher now honors its pointer-changing `OwnerActionBarrier::Skip`, matching Actor::Instruct's return immediately after Translate changes `mpSequenceElement`. A building-interior Move that synchronously terminates no longer runs the accepted-Instruct epilogue and overwrites an earlier actor `Done` edge with `InProgress`. SuN1Sh1nE Profile_004 Savegame_002 replay-001 and both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #111 completed: AI RaiseShield no longer restores the movement goal that the preceding synchronous `StopAll`/`Halt` condolence cleared. The restoration flag was an obsolete workaround from before Halt delivered its selected movement cleanup at the source-authored barrier; Original clears `PositionGoalMap` before the non-directional RaiseShield sequence takes ownership. linux2 Profile_002 Savegame_031 replay-002 and both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #112 completed: climb-lift translation now preserves an explicitly authored `RunningUpright` action even when the movement element does not carry `RHMOVE_FAST`. Original passes the action directly to `RHSectorLift`, whose ladder mapping selects `RHNONANIMATION_CLIMBING_LADDER_UP_FAST`; Rust had instead downgraded the action from the flag before mapping it. SuN1Sh1nE Profile_004 Savegame_021 replay-009 clears Soldier99's frame-518 animation-35/296 frontier and advances to an independent frame-594 direction/sprite-row frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #113 completed: tower-guard `EVENT_DONE` now suspends its trailing `BattleDecisions` until every synchronous `CALL_TOWER_GUARD_ALERT` and `CALL_TOWER_GUARD_CALLS_ME` recipient has returned. Original's direct calls update nearby soldiers' alert status before the guard can enter `SeekArea`; Rust previously planned the seek inside the borrowed handler before draining those calls, reducing the nearby-friend multiplier and consuming only 15 of Original's roughly 112 seek-point selection pairs. Nescafe Profile_001 Restart replay-008 clears the frame-297 `SeekPointSelection` frontier and advances to an independent frame-307 Soldier225 attentive-mode frontier; Profile_003 advances to the same frontier, while Profile_002 clears the seek-point count and exposes its pre-existing schema-12 `RuntimeBuildingExitWait` pair. Both task-36 schema-14 replay-009 controls match exact EOF.
- #114 completed: PC concussion handling now resolves coma status through the serialized campaign-description identity instead of the unrelated actor/UI list index. Same-profile PCs can occupy different campaign slots; borrowing the UI slot's coma bit caused a zero-concussion arrow hit to knock out the wrong PC and dissolve both combat opponent lists. SuN1Sh1nE Profile_004 Savegame_017 replay-007 and both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #115 completed: lift traversal preserves inherited facing at all three Original lifecycle boundaries. Lift direction is applied only while a climb order initializes; completing `PassDoor` changes topology without setting a new direction goal; and fallback `Wait` normalizes lift posture/Freezing without snapping direction. SuN1Sh1nE Profile_004 Savegame_021 replay-009 clears the frame-594/604/605 ladder-facing chain and advances to an independent frame-614 AI/visibility frontier; both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #116 completed: tower-guard runner fallback now preserves Original's two coupled selection bugs. The hearing-list size bounds a scan of the first N camp-registry soldiers rather than the hearing list itself, and every candidate is compared with the fixed far-officer distance, so the last qualifying prefix actor wins instead of the nearest hearer. Nescafe Profile_001 Restart replay-008 no longer sends `CALL_TOWER_GUARD_CALLS_ME` to Soldier225 and matches exact EOF; Profile_003 clears the same frame-307 frontier and advances to an independent frame-396 Soldier114 group/list-us and MoveWaiting frontier. Both task-36 schema-14 replay-009 controls match exact EOF.
- #117 completed: gate-route authorization now checks exact live building capacity for direct NPC entry during both seed and neighbor expansion, matching `RHDoor::IsActorAutorized`. Immutable AI routing receives a canonical sector-to-authorization snapshot and panics on missing building state instead of inventing capacity. Nescafe Profile_002 Continue replay-008 clears the frame-310 `RuntimeBuildingExitWait` pair and advances to an independent frame-465 RNG frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #118 completed: near-enemy sight reactions now use Original's world-space 3D `MaxNormDistance`, including stretched world Y and elevation, instead of raw map Y that already contains elevation projection. SuN1Sh1nE Profile_004 Savegame_021 replay-009 clears Soldier97's frame-614 turn-versus-bow-decision frontier and advances through frame 853 to an independent frame-854 seek RNG frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #120 completed: reaching a `SeekingCombatAlert` hint now starts Original's plain lost-enemy `SeekArea` around the stored point instead of running a fast battle overview and reacquiring an enemy. Nescafe Profile_002 Continue replay-008 and both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #121 completed: failed attacking approaches now fall through to Original's `CouldntReachpointEmergencyRoutine` and `GetBattleOverview` instead of an invented branch that rebuilt combat groups and replanned movement. Nescafe Profile_003 Restart replay-008 and both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #119 completed: same-position `GoTo`/`GoToSpeed` now observes an Original-synchronous `Halt` queued earlier in the same Think, including actor effects suspended into a state-change owner-work prefix. SuN1Sh1nE Profile_004 Savegame_021 replay-009 clears the frame-854 seek acceptance/movement frontier and advances through frame 896 to an independent frame-897 direction-shuffle RNG frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #122 completed: civilian friend detectables now preserve Original's soldier-registry iteration order instead of nondeterministic entity-view `HashMap` order, with strict live-view resolution. Nescafe Profile_001 Restart replay-009 clears the old frame-251 visibility and frame-286 list-order frontiers, advancing to an independent frame-441 missing path event; both task-36 schema-14 replay-009 controls match exact EOF.
- #123 completed: conscious `EVENT_GOTHIT` now drains its queued eye-status write at the synchronous damage boundary, matching Original's inline `SetViewStatus(EYES_DIE_OR_GET_UNCONSCIOUS)`. Nescafe Profile_001 Savegame_007 replay-002 and both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #126 completed: shield-bearer `CALL_COORDINATE` is now ignored during `AttackingBowAiming`, `AttackingBowLoading`, and `AttackingBowObservingLoading`, matching Original's switches; only the shooting substate handles it. Cyrdach Profile_156 Restart replay-014 clears the frame-1418 bow-transition frontier and advances to an independent frame-1480 combat-decision frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #124 completed: deferred same-point `GoTo` now projects pending Halt multiplicity through Original's `StopMovement` action families instead of treating every queued Halt as immediately idle. SuN1Sh1nE Profile_004 Savegame_021 replay-009 clears the frame-854 and frame-897 seek frontiers and advances to an independent frame-977 PC life-point frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #127 completed: archers reject cover behind a shield bearer whose primary target is null, matching Original's failed `ComputePositionBehindMyShieldBearer` and Shoot/Observe fallback instead of inventing a cover-view position. Cyrdach Profile_156 Restart replay-014 clears the frame-1480 frontier and advances to an independent frame-1481 phalanx-timer RNG frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #125 completed: deferred AI GoTo route construction now preserves Original's call-time source topology, while moving-fast EventView distance reads the actor's literal live position instead of its door-side AI forecast. Nescafe Profile_001 Restart replay-009 clears frame 441 and advances to an independent frame-451 order-transition frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #129 completed: arrow and stone hits on ladder/wall occupants now apply Original's piercing damage before translating the ladder-fall reaction. SuN1Sh1nE Profile_004 Savegame_021 replay-009 clears the frame-977 life mismatch and advances to an independent frame-978 motion edge; both task-36 schema-14 replay-009 controls match exact EOF.
- #128 completed: combat-neighbour teardown now clears reciprocal links, while preserving Original's incoming-substate line-mode quirk when entering a phalanx. Cyrdach Profile_156 Restart replay-014 and both task-36 schema-14 replay-009 controls match exact EOF.
- #130 completed: ladder-fall arrival now returns and latches Original's terminal motion edge instead of being overwritten by the earlier sprite Start result. SuN1Sh1nE Profile_004 Savegame_021 replay-009 clears frame 978 and advances to an independent frame-1302 lethal/coma frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #132 completed: goto-post arrival and completion now dispatch through the enemy virtual `SetState` tail, preserving Original's Face/posture ordering while emitting LeaveAttentiveMode and Green alert. nicouzouf Profile_001 Savegame_065 replay-001 clears frame 440 and advances to an independent frame-688 group movement frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #133 completed: `GetKilledAtBottom` now closes Original's virtual PC wounded boundary, allowing VIP amulet coma saves before terminal translation. SuN1Sh1nE Profile_004 Savegame_021 replay-009 and both task-36 schema-14 replay-009 controls match exact EOF.
- #131 completed: Observe now reads the live persistent primary target and launches its replacement `GoNear` before `SetState`, matching Original's synchronous actor-effect staging. Nescafe Profile_001 Restart replay-009 clears frame 451 and advances to an independent frame-505 animation frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #135 completed: owner-local script `StopActor` rewrites that reseed a walking order after Execute Start no longer masquerade as successor advancement. SuN1Sh1nE Profile_004 Savegame_004 replay-005 clears frame 16287 and advances to an independent frame-16440 motion frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #136 completed: `FORCE_SWORD_MOVEMENT` is now limited to Original's already-translated sword-move Execute guard and no longer converts a postponed ordinary walk after swordfight teardown. Nescafe Profile_001 Restart replay-009 and both task-36 schema-14 replay-009 controls match exact EOF.
- #137 completed: the StopMovement Start-edge safeguard now distinguishes a newly reseeded in-place order from an older prebuilt transition successor. SuN1Sh1nE Profile_004 Savegame_004 replay-005 clears frame 16440 and advances to an independent frame-16482 speech-request panic; both task-36 schema-14 replay-009 controls match exact EOF.
- #138 completed: `NearbyCiviliansPanic` now retains Original's visibility work for active outdoor civilians regardless of death or unconsciousness. SuN1Sh1nE Profile_004 Savegame_002 replay-009 clears frame 36195 and advances to an independent frame-36627 target-list frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #139 completed: beggar identification now queues Original's civilian self-identification remark immediately after the show-face command through the strict target speech path. SuN1Sh1nE Profile_004 Savegame_004 replay-005 clears frame 16482 and advances to an independent frame-16513 movement/goal frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #134 completed: battle target multiplicity now follows Original's UWORD stacking and `ProposeShotTarget` reset/bow-only rebuild across failed Shoot fallback, using strict live primary targets. The Task61 linux3 Profile_001 Continue replay-001 control clears frame 1162 and advances to an unrelated frame-1506 goal frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #141 completed: targets appended after BattleDecisions' personal-list reset retain the shared live multiplicity before source-authorized increments. SuN1Sh1nE Profile_004 Savegame_002 replay-009 and both task-36 schema-14 replay-009 controls match exact EOF.
- #140 completed: non-push sword damage that amulet-saves a PC into a coma now terminates while selected and returns the pointer-change Skip barrier, clearing the interrupted goal while preserving the prior Execute motion edge. SuN1Sh1nE Profile_004 Savegame_004 replay-005 and both task-36 schema-14 replay-009 controls match exact EOF.
- #143 completed: arrow translation now reads the post-wound posture after piercing damage, matching Original's lethal VIP amulet/coma boundary and allowing selected-element cleanup to clear the interrupted movement goal. The linux3 Profile_001 Continue replay-001 representative and both task-36 schema-14 replay-009 controls match exact EOF.
- #145 completed: building PassDoor translation now uses the movement element's stamped posture/action and canonical sword/shield categories, preserving `MovingFastSword` as `RunningWithSword` even when the actor's live state has already changed. SuN1Sh1nE Profile_004 Savegame_001 replay-001 and both task-36 schema-14 replay-009 controls match exact EOF.
- #147 completed as already-fixed: Cyrdach Profile_156 Savegame_001 replay-011's historical missing phalanx visibility query was already corrected by `daeacac810`, which performs Original's 180-degree query before rejecting a dead or unconscious target. The current runner matches exact EOF; no new production change was needed.
- #148 completed as already-fixed: Nescafe Profile_001 Savegame_001 replay-006's historical retained RaiseShield movement goal was already corrected by `ac00ab0c9` / task #111. The current runner clears the goal and matches exact EOF; no new production change was needed.
- #142 completed as stale-capture retirement: current Original and the archived nicouzouf Profile_001 Savegame_065 replay-001 first disagree at frame 486 despite identical inputs, and no longer share the alleged frame-712 frontier. Fresh current Rust matches fresh current Original for all 685 frames, so no trace-specific workaround was added; the archived recording must be replaced before a full-universe sweep.
- #146 completed: combat primary-target multiplicity now mirrors Original's nonserialized, owner-ordered per-human UWORD scratch counters across BattleDecisions, combat-position preparation, observation reconsideration, and shot fallback. The within-call `IsTooProudToAttack` re-pick reads the locally mutated counters rather than an owner-start snapshot. The SuN Task61/134/141 controls and both task-36 controls match exact EOF; the SuN direction-goal representative clears frames 14540/14716 and advances to an independent frame-15016 sound FIFO frontier.
- #149 completed: far-opponent pruning now preserves the initiating actor's cached relative fighting ability while invoking authoritative reciprocal `DeleteOpponent` recomputation, initiative, and quit side effects, matching Original's asymmetric raw-delete lifecycle. The Cyrdach representative clears its frame-1073 missing `MeleeInitiative` draw and advances to an independent frame-1458 RaiseShield/Wait frontier; both task-36 controls match exact EOF.
- #150 completed: redundant QuitSwordfight manager dispatch now preserves its terminal Skip edge instead of applying the generic InProgress epilogue, and post-completion projection uses the actor's installed-order/mpOrder mirror rather than manager-resident fallback work. SuN1Sh1nE Profile_004 Savegame_028 replay-012 and both task-36 controls match exact EOF.
- #152 completed: RaiseShield translation no longer rejects a live sword/bow action after `GenerateTransition` has authored its exit transition. Matching Original, Rust retains the lowering-sword order and appends RaisingShield unless the actor is already holding/moving with a shield. Cyrdach Profile_156 Savegame_001 replay-001 and both task-36 controls match exact EOF.
- #154 completed: jump-line movement execution now applies Original's posture eligibility gate while cursor preview remains posture-agnostic. Rust previously admitted a helper-required line for upright Pc174 and projected its direct approach onto the wrong source-line geometry. linux2 Profile_002 Savegame_015 replay-010 and both task-36 controls match exact EOF.
- #151 completed: corpse-removal intersection scans now preserve Original's live `marrayActors` mutation visibility. Rust previously captured only initially-small neighbours before recursively re-evaluating them, so a later corpse changed from large to small by an earlier recursive call was never revisited. The removal path snapshots only creation-ordered human identities and re-checks current radius flag, posture, topology, and distance before each recursion. Savegame_linux2 Profile_002 Savegame_003 replay-010 clears its frame-14693 movement and frame-15016 sound FIFO frontiers and advances to an independent frame-15133 Soldier140 command/animation boundary; both task-36 controls remain exact EOF.
- #155 completed: actor work issued before a trailing synchronous `StopAll` now closes through an ordered owner FIFO barrier. This preserves Original's `GoTo`-then-Halt statement order in defensive swordfight-observation fallthrough: the active stronger QuitSwordfight survives while its following Todo Move is interrupted, instead of Rust halting the older sequence and launching the new Move afterward. Savegame_linux2 Profile_002 Savegame_003 replay-010 clears frame 15133 and advances to an independent frame-15171 RNG frontier; both task-36 controls remain exact EOF.
- #153 completed: selected PC swordfight entry now forwards Original's `MSG_SELECT_ACTION(NO_ACTION)` before `DisableAllActionsTemp`, preventing a previously armed Purse action from being saved and resurrected after the final opponent is removed. Savegame_linux2 Profile_002 Savegame_001 replay-001 clears the old frame-1765 movement-stop/animation frontier and advances to an independent frame-2147 difference; both task-36 controls remain exact EOF.
- #156 completed as a source-proven focused mismatch: `EnableAllActionsTemp` now restores `saved_action` only when an exact newly re-enabled authored profile slot matches it, and forwards restoration through selected-PC action semantics instead of directly assigning `current_action`. Focused tests cover matching and nonmatching slots, full-selection side effects, selected swordfight exit, and the unselected no-op; no dedicated replay frontier was claimed.

- #157 completed: the heard-steps reaction-point handler now follows Original into `SeekArea(Position(mpMe), 0, SEEK_LOCATION_FIRST | SEEK_WALKING)` instead of entering an invented just-watching timer shortcut. Savegame_linux2 Profile_002 Savegame_002 replay-015 and both task-36 schema-14 replay-009 controls match exact EOF.
- #158 completed: all four `ReconsiderEnemyApproach` movement branches now preserve Original's GoNear/GoTo → already-on-point check → SetState statement order. Nescafe Profile_001 Savegame_001 replay-013 clears the missing frame-777 RunningUpright path and matches exact EOF; both task-36 schema-14 replay-009 controls remain exact EOF.
- #159 completed: AI entity views now expose a logically live alert path from `detached_patrol_path_status` while its Rust `PatrolPath` is temporarily unmaterialized. This restores Original's synchronous `InitializeFriendCheck` path scan and preserves the following macro cursor/timer until `EVENT_SEES_CHARLY`. Cyrdach Profile_156 Savegame_001 replay-014 and both task-36 schema-14 replay-009 controls match exact EOF.
- #160 completed as already-fixed: the frozen SuN1Sh1nE Profile_004 ExQuickSave replay-010 visibility representative formerly had 21 Original queries versus 20 Rust after frame 36672; the newest existing runner now matches every recorded frame through exact EOF at frame 36960. No production change was needed. The sampled leading schema-14 RuntimeBuildingExitWait representative also clears its old frontier and advances to the independent task-161 frame-44565 RNG boundary.
- #161 completed as current-Original-inconsistent/no-fix: the archived linux3 Profile_001 Savegame_045 replay-011 records Soldier 105 entering ReturnToDuty at frame 44566, but a direct current-Original probe with the exact save and seeds matches Rust's watchdog cadence and counter (0 at f44465, 1 at f44529, 2 through f44577), with no ReturnToDuty or ReturnToDutyCommonStuff call. The next applicable watchdog slot is f44593. Retire the stale control pending recapture; no Rust workaround was added.
- #163 completed: `EVENT_SEES_BEGGAR` no longer requeues the beggar already under examination, matching Original's explicit `pHuman != mpBeggarToExamine` gate around the queue and coupled effects. linux2 Profile_002 Savegame_003 replay-013 clears frame 14509 and advances to an independent frame-14874 sword-strike frontier; both task-36 controls remain exact EOF.
- #162 completed: BattleDecisions now measures its owner-to-primary-target reference with Original's literal 3D `SquareDistance` (stretched world Y, included Z, ULONG truncation) while retaining committed door-side raw-map norms for friend comparisons. linux2 Profile_002 Savegame_001 replay-001 clears frame 2147 and advances to an independent frame-2150 visibility-query-only frontier; both task-36 controls remain exact EOF.
- #164 completed: `EvaluateSwordfight` now treats incompatible elevation and sector as Original's immediate reciprocal teardown and returns before the later distance/LOS block. linux2 Profile_002 Savegame_029 replay-014 clears the extra frame-4442 visibility query and advances to an independent frame-4692 parry/damage frontier; both task-36 controls remain exact EOF.
- #166 completed: lateral sword sweeps now retain only the victims admitted by Original's authored angular scan; Rust no longer recovers an out-of-arc interaction antagonist through generic range checks. linux2 Profile_002 Savegame_003 replay-010 and both task-36 controls match exact EOF.
- #167 completed: tower-alert fallback selection now applies Original's hearing count to the complete stable camp-registry prefix, including the tower owner, while normal hearing eligibility remains self-excluding. Squared distances also truncate to ULONG before comparison. Focused owner-prefix and truncation regressions pass; both task-36 controls remain exact EOF.
- #165 completed: DropAle DONE now survives the Actor::Hourglass completion seam, and anti-collision snapshots include live objects in engine-slot order like Original's all-element scan. SuN1Sh1nE Profile_004 Savegame_028 replay-004 clears frame 1543 and advances to an independent frame-1738 direction frontier; both task-36 controls remain exact EOF.
- #170 completed: `CanEnterSwordfightWith` no longer treats an active door pass through ordinary sectors as being inside a building. linux2 Profile_002 Savegame_003 replay-013 clears frame 14874 and advances to an independent frame-15126 RNG frontier; both task-36 controls remain exact EOF.
- #169 completed: smalltalk DONE back-hit classification now builds its relative vector from Original's ground positions rather than elevation-projected map positions, while retaining the isometric facing vector. linux2 Profile_002 Savegame_029 replay-014 and both task-36 controls match exact EOF.
- #171 completed: generic human/PC `StandingUpSword` now refreshes its direction goal from the live principal opponent after sprite playback and then turns unconditionally, while the Soldier override remains sprite-only. SuN1Sh1nE Profile_004 Savegame_028 replay-004 and both task-36 controls match exact EOF.
- #172 completed: low-level `GoTo` now honors replayed `GOTO_NEAR` flags with Original's same-layer squared-distance early exit and stored stop tolerance. linux2 Profile_002 Savegame_003 replay-013 and both task-36 controls match exact EOF.
- #173 completed: non-soldier forced sword movement with no live opponent now selects `WalkingSword` directly instead of encoding a fake self-facing target that can become a tiny strafe vector. linux2 Profile_002 Savegame_008 replay-014 and both task-36 controls match exact EOF.
- #175 completed: archery friendly-fire checks now use Original's determinant/dot `SBGeoVector2D::Angle`, including PI for a coincident zero vector instead of Rust's `atan2(0,0)=0`. linux2 Profile_002 Savegame_010 replay-003 and both task-36 controls match exact EOF.
- #168 completed: `ReconsiderSwordfight` now evaluates its facing guard from Original's ground positions rather than elevation-projected map positions. linux2 Profile_002 Savegame_001 replay-001 and both task-36 controls match exact EOF.
- #174 completed: phalanx 180-degree detection now performs Original's cached `ComputeViewRadius` boundary before its final target LOS, restoring ordered night/fog light-sector barycentre queries. linux2 Profile_002 Savegame_030 replay-005 clears frame 6153 and advances to an independent frame-6314 projectile topology frontier; both task-36 controls remain exact EOF.

Standing clusters not yet tasked: actor.animation 133 (top cluster, resweep-db67cc978 classification), direction_goal 76, position_goal_map.x 72, motion_state 70, MoveOk→MoveWaiting 57, BoredAnimationChoice 56, AiRandomValueRectangle 55, path_events.length 54, RuntimeBuildingExitWait 51, visibility_queries.length 45 (+43 ai.list_them etc.). Full breakdown: `output/parity-audits/resweep-db67cc978/classification.json`.

## Wave loop (the process to continue)

1. Merge reported agent branches into main (verify suite 2629/0 + debug build).
2. When agents free up / finish: assign next task from board, or spawn fresh (`Agent` tool, `isolation: "worktree"`, prompt template: symlink setup + TaskGet + debug-only + narrow validation + faithful-C++ rules — copy any recent spawn prompt from this file's history).
3. After a batch of merges: rsync source to remote, release-build, freeze runner as `original_parity_replay-<commit>`, make manifest, launch 11-shard tmux sweep (`scripts/run_parity_release_sweep.sh CORPUS AUDIT RUNNER SHARD SHARDS`, wrapper `run-parity-*.sh` pattern), watcher on SWEEP-DONE, rsync back, classify, update ledger, dispatch.
4. **Next sweep (14) must be FULL-UNIVERSE** — run `scripts/build_parity_full_snapshot.sh <audit>/traces.snapshot`; it includes every current corpus, excludes schema-11 (`parity-save-replays/`), and replaces old captures listed in `docs/PARITY_RETIRED_SAVES.txt` with their schema-14 recordings. The passing set must be re-verified because of the #36 regression.

## Key mechanics reference (hard-won, do not relearn)

- Remote sweep: `ssh atlasbio-robin-cpu4`; corpora + datadirs + original-code + rust-src at `~/robinhood/`; build `cd ~/robinhood/rust-src && cargo build --release --example original_parity_replay`; sweep script reads `<audit>/traces.snapshot`, writes per-trace `status/<key>.status` (0 pass, 1 state-div, 101 rng-panic, 124 timeout) and `logs/<key>.log`; key = trace path with `/`→`__`.
- classify_parity_failures.py groups by first divergence boundary; output classification.json.
- Substates in dumps: C++ enum = Rust index + 1. Aim previews burn creation-order in Original, not in Rust (see memory project_creation_order_preview_gaps).
- RNG contract: Rust consumes recorded draw VALUES; "replay exhausted" = Rust drew MORE than Original; extra/missing draws → compare gate conditions at the named site.
- `--dump-jsonl` ~500KB/frame — narrow windows only, delete after.
- Entity mapping in dumps: original pc:N ↔ Rust Pc(PcId(N−1)) sometimes (creation-order shift) — verify entity identity before trusting a dump diff (bit fix-endfacing twice).
- Debug replays under multi-agent load: 25-85 min for 15k+ frame traces; timeout 1800-3600s.
- The Bash tool auto-spills long output; never filter cargo.

## Recovery checklist for the NEW session (do these in order)

1. Read this file fully. Also load memories (MEMORY.md points here).
2. Recreate the open task board from `docs/parity-task-archive/*.json` (open items: #9, #20, #21, #23, #24, #28, #31, #34–#40; the JSON descriptions carry the accumulated analysis — copy them verbatim into new TaskCreate calls).
3. **First dispatch: task #36 (seek-RNG regression)** — spawn a fresh worktree agent with the task JSON's content; 4 shallow repros make it a fast bisect over `git log --oneline --merges db67cc978..78a792ed6`.
4. Spawn agents for #20 (attach wip patch as head-start), #21 (ditto), #39, and further board items as slots allow. Spawn template: Agent tool with isolation:"worktree"; prompt = symlink setup block (original-code, datadirs, corpora → absolute paths into this repo) + task text + rules (debug builds only; never --release/clippy/filtered cargo; faithful C++ only, no invented guards; validate 2-4 repros + 2-3 short controls; cargo fmt; commit on own branch; report worktree+branch+root cause+validation).
5. After merging a batch: release build + freeze runner + **FULL-UNIVERSE re-sweep 14** via `scripts/build_parity_full_snapshot.sh`. Failure-only sweeps are banned until a clean post-#36 full-universe result verifies the passing set.
6. Continue the wave loop (previous section) until 100% parity. Do not stop.
