# Parity campaign — full session state dump (2026-08-06, ~14:10 Berlin)

Written when the session ran out of credits (limit resets 18:30 Berlin; earlier reset was 13:30 so it may be a rolling window). This file is the complete resume point — a fresh session should be able to continue the campaign from here with NO other context.

## Standing orders (user directives, all still in force)

- **"keep going until ALL replays are 100% parity. until then do not stop."** Proper general fixes aligned with `original-code/` C++ — no per-trace hacks. See memory `project_parity_campaign`.
- This session is already running on the remote server: do **not** SSH. All builds and sweeps run directly in this sandboxed checkout.
- Prefer debug for local investigation, but individual long replays may use release when the longer link is cheaper than debug execution; decide per repro.
- Never `git stash`. Never pipe cargo output through filters. No clippy in worktree agents. Preserve all unrelated tracked and untracked worktree state.
- Agents validate 2-4 key repros + focused controls; the local release full-universe sweep is the wide regression check.
- Consult `docs/SNAPSHOT_INPUT_AUDIT.md` for the determinism/ownership contract.

## Score

- Current authoritative universe: **4,680 traces**, generated by `scripts/build_parity_full_snapshot.sh` (2,221 retained old 60s captures + 1,350 schema-14 replacements + 386 schema-12 + 723 random-save). `parity-save-replays/` schema-11 is permanently retired; old captures replaced by schema-14 are listed in tracked `docs/PARITY_RETIRED_SAVES.txt`, while 93 individually malformed, current-Original-inconsistent, presentation-contaminated, or incomplete records are listed in `docs/PARITY_RETIRED_TRACES.txt`. The historical 5,164 denominator below predates these canonical retirement manifests.
- Historical pre-lanes-21–30 sweep for frozen runner `7890a5983`, evaluated against the then-current 4,713-trace universe: **3,732 exact EOF (79.1852%)** and 981 non-EOF traces. It is retained only as a reproducible discovery baseline; its denominator and runner predate the current authoritative universe and integrated fixes.
- Full-universe discovery sweep for frozen runner `e806655f8` completed across all 4,761 traces: **2,583 exact passes (54.3%)**, 0 timeouts, 59 classified panics, and 1 other error. Classification is `output/parity-audits/full-sweep-e806655f8/classification.json`; largest clusters are actor.motion_state 126, actor.animation 116, visibility_queries.length 114, position_goal_map.x 94, and direction_goal 84. This runner predates tasks #116 onward and therefore materially understates current parity.
- Current full-universe sweep for frozen runner `d7d414c1e` completed across all 4,761 traces: **2,865 exact passes (60.2%)**, 1,547 state-divergence exits, 349 exit-101/RNG or invariant failures, and 0 timeouts. Classification is `output/parity-audits/full-sweep-d7d414c1e/classification.json`; largest live clusters are actor.animation 115, visibility_queries.length 101, AiRandomValueRectangle 76, VipIdleRemark 73, direction_goal 73, direction 69, and movement_map.x 63. This is the authoritative post-task-158 baseline and confirms both task-36 controls remain in the passing set.
- Full-universe sweep 14 completed for frozen runner `7675ff084` across its then-current 4,773-record snapshot: **1,942 pass, 2,477 ordinary divergence exits, 354 RNG/panic exits, and 0 timeouts**. Classification is `output/parity-audits/full-sweep-14-7675ff084/classification.json`. This is a discovery baseline rather than the current score: it predates tasks #71–#79 and includes the seven records now retired individually.
- Full-universe baseline `resweep-feefc57f5` completed locally: **2,400 pass, 1,972 state divergence, 394 RNG divergence, 7 contention timeouts** across all 4,773 traces. Runner SHA-256: `d7e423d3dd89f3e51ef0e79f231e165d06d98f09c36202823093c0bb5bfd7300`; frozen snapshot SHA-256: `84fbe90b42200b21cbb4fb37bc92675b8f6dbb3767ca359e2a04fcdbe83adcb4`. Classification is `output/parity-audits/resweep-feefc57f5/classification.json`. This runner predates #31 and tasks #28/#34/#38/#40–#45, so it is a discovery baseline, not the current score. All seven controls were rerun concurrently against frozen current runner `36acefeb4` with 1,200-second limits and all seven again exited 124. Tasks #52, #53, and #57 subsequently proved every control was the same EnterSwordfight translation-loop family; current main clears all seven former timeouts.
- Last complete sweep (re-sweep 12, runner db67cc978): **2,390 passing (46.3%)**, 2,418 failing. Ledger entry committed in `docs/ORIGINAL_PARITY_REPLAY.md`.
- Failing-trace depth profile (sampled n=99): traces are ~1,384 frames; failures diverge at mean 630 frames = **mean 46.4% / median 41.4% progress**; 15% of failures get past 80%.
- **Re-sweep 13 DONE** (finished after the dump; results fetched + classified + ledger-committed): 31 passes, 1,948 state-div, 431 rng-panic, 8 timeouts → **cumulative 2,421/5,164 (46.9%)**. Classification: `output/parity-audits/resweep-69df41aeb/classification.json` (top: actor.animation 113, rng:VipIdleRemark 80 [mislabeled swordfight — task #38], direction_goal 79, position_goal_map.x 73, movement_map.x 71, motion_state 70, detectables.length 69).
  - Sweep 13 measured main @69df41aeb (has #12,#14,#18,#26,#29,#32). Merges AFTER it: #33,#27,#25,#30,#19 → need re-sweep 14.
  - **Re-sweep 14 MUST be full-universe (all 5,164)** — failure-only manifests are blind to regressions on passing traces, and we have a confirmed regression (task #36).

## Main branch state

main @ HEAD includes today's merged fixes (all suite-green, `cargo test -p robin_engine --lib` = 2629/0):
- #12 corpse-walk arrival predicate (committed-step + rebuilt-increment dot-product vs `dist<=speed` prediction) — merge of wt-fix-corpsewalk cc3737b28. Six traces to exact EOF.
- #13 (earlier) + #29 seek/stop-transition arms + #32 FaceOpponent determinant-form Angle + #33 seek-refresh queued-effect drop — branch wt-fix-orderlag (5cdc8d639, bccb27806, 94e32d620, 708abb56e).
- #14 ladder-transition pick + #27 ladder-fall flight kinematics — branch worktree-agent-a43843b306107ea87 (91be78873, 349502f46).
- #18 battle_decisions (us-list PCs, per-iteration swap commits, avenger fallback, detection.rs camp bug) — branch worktree-agent-a7bd1cc12fe038a73 (37e5149b1..b50a7b200).
- #25 hero forbidden-expression timer aging moved post-hourglass + set_focus(0)→unfocus — branch worktree-agent-acf2bcdcdeebac468 (ba78cf865).
- #26 test suite fully repaired (2629/0) — branch worktree-agent-a4ead08f9f8668510 (2495ac7a6, 18c810f10, 097eda962). ~176 tests fixed; production fixes: WorldState creation-order map `any_key_map_sized` serde adapter; "command level must stay at 2" was a BORN-BROKEN fixture, not a regression.
- #19 reach-point half: five Think-dispatch sites live-read animation instead of latched installed_order (mpOrder mirror) — deletion fix, branch wt-fix-noisetiming (da65a5097).
- #30 TakeCorpse: invented face-the-corpse snap deleted from begin_carry + FarmerCarry wrongly selecting LittleJohn carried-animation set — branch worktree-agent-ac0fca79adc9b7209 (497eb4ad0, 62ea32191). Merge had one trivial comment conflict in abilities.rs, resolved.

## Task #36 seek-RNG regression — resolved locally, pending re-sweep 14

Four traces that passed at `db67cc978` regressed before this fix with seek-RNG draw-count storms:
- Cyrdach_001 replay-009 (f1653, SeekPointSelection storm, draws 5251..5813)
- Nescafe_001 replay-009 (f1589, same)
- linux2/Profile_002/Savegame_000/replay-014 (f5379, SeekPointAcceptance)
- linux2/Profile_002/Continue/replay-013 (f7581, SeekPointAcceptance)
All were confirmed on the pre-fix HEAD by two independent A/B tests (not caused by #25 or #30).

The exact boundary is `69df41aeb` (both controls pass) to `4cfae5aa0`, whose only topic change is #33 (`708abb56e`). Dropping the stop transition's queued `Waiting` effect is faithful, but exposed an older staging error: Rust applied the resulting `RefreshSeek` after line-crossing callbacks, whereas Original `PerformSeek` refreshes synchronously before `RHElementActor::Hourglass` calls `CheckForLineCrossing`. The fix applies the replacement before crossing resolution and retains the already-committed crossing segment on the walking refresh path. Cyrdach replay-009 and Nescafe replay-009 both match exact EOF in the patched debug runner. Re-sweep 14 must still be full-universe to verify the passing set broadly.

## KILLED agents — session terminated by user; agents are NOT resumable. Respawn fresh.

The whole session (coordinator + subagents) was killed on 2026-08-06 ~14:20. The old session's agent transcripts and task store are gone for practical purposes, BUT everything needed is archived in-repo:

- **`docs/parity-task-archive/*.json`** — full copy of the task store (all 40 tasks with their accumulated per-task analysis in the descriptions). Read these instead of TaskGet; recreate open tasks in the new session's board from them.
- **`docs/parity-task-archive/wip-fix-jump-task20.patch`** (363 lines) — fix-jump's UNCOMMITTED mid-diagnosis work on task #20 (engine/jump.rs + sprite.rs, in worktree agent-a2b48d6a0f1b3ebb0 at stale base 3d42ce0ad). Mostly instrumentation + partial fix; treat as reference/head-start, re-derive on current main. It was last dumping frames 790-800 of its repro to compare Rust step-entry sequencing.
- **`docs/parity-task-archive/wip-fix-ladderpick2-task21.patch`** (47 lines) — fix-ladderpick2's uncommitted start on task #21 (movement.rs + tick.rs, worktree agent-a43843b306107ea87, base = merged main 9423d267a).
- **fix-seekregress (task #36, URGENT)**: worktree agent-ae66cda91de4ba9d5 is CLEAN at base 3d42ce0ad — the bisect produced no commits and no report. Nothing to salvage; restart #36 from the task description (it has all 4 repro traces + suspect list). This is the highest-priority item.
- **Task #39 completed**: the retained FIFO was already correctly ordered `CallAlert -> CallLookThere -> EventHear`; the actual mismatch was an invented Rust receiver-side gate that rejected `CALL_LOOKTHERE` after the alert changed state. Original handles delivery unconditionally after sender selection. Both controls clear the heard-steps substate/view failure; later direction-goal residuals are separate.

Old worktrees under .claude/worktrees/ (agent-* and fix-*) whose branches are fully merged can be `git worktree remove --force`d to free disk; keep the two with WIP patches until their tasks land. All merged branch names appear in `git log --oneline --merges` on main.

## Task board (open items)

- #9 completed: reciprocal archer/shield links now clear synchronously like Original; both f762/f794 boundaries clear and advance to independent residuals.
- #20 completed: retained Original's Execute-time airborne increment and synchronous post-jump `motion_state=InProgress`; primary linux2 schema-14 replay-004 exact EOF. SuN Aborted-vs-Terminated signatures are a separate non-jump MoveOk lifecycle family.
- #21 completed as superseded/no-fix: four archived elevation/projectile controls reach exact EOF on task-36 main; remaining symptoms route to arrow lifecycle, shield-command, and movement families.
- #23 completed: `ForecastDestinationForIA` now preserves the independent direct-passage latch after the live door clears; both primary Seek RNG boundaries advance to separate later door/building residuals.
- #24 completed as source-verified no-op: shipped proto loading never registers `RHSectorAssociated` (both Original lift `AddSector` calls are commented out); Rust already discards the dead click polygon.
- #28 completed: lateral strikes seed Original's ground-space direction/full-3D range, and observation attack-opportunity checks use Original's isometric target direction. The latter clears the extra CombatObserveSideStep draw in both controls; replay-002's later f14266 frontier is #46.
- #31 completed: threaded owner-boundary `Position(enemy)` into `EVENT_VIEW` and made `SeekingCombatAlert` ignore `EventTimer`; the two archived list-us symptoms were separate timing/position causes, not a shared 360-LOS composition bug.
- #34 completed: Strangle translation now preserves the Moving action state from synchronous post-seek arrival, matching Original's append-only PC::Translate path and clearing nicouzouf/S069 r005 f457. Its next f463 motion/sprite residual is tracked separately as #42.
- #35 completed: Rust-only LeaveSpy/LeaveTree commands inherit Original CROUCH_UP's Preference priority; Original appends the cape/tree transition order to that same posture-toggle element.
- #37 completed: retired the old nicouzouf_061 save family in `docs/PARITY_RETIRED_SAVES.txt` and the local `tmp/regen14-saves.txt`; all 15 schema-14 replacement traces already exist (the 11 affected old-schema captures lacked `hero_refused_action` records and were unfixable as recorded; original-code commit d494273).
- #38 completed: Smalltalk back-hit now uses Original's isometric facing vector, restoring ReceiveSwordDamage and its protection draws. Representative controls for both UpdateSwordfightDistance offsets, EvaluateSwordfight, FindPositionForTableSwordfight, and protection calls all reach exact EOF; those labels were downstream topology symptoms of the same diagonal-facing bug.
- #39 completed: removed invented receiver-side `CALL_LOOKTHERE` state gate; Original delivery is unconditional after sender selection. Both controls clear the heard-steps substate/view failure and expose a separate direction-goal residual.
- #40 completed: The16thFrame now reads ActorData::installed_order after synchronous detection callbacks instead of using the NPC owner path's pre-detection snapshot, matching Original's live GetAnimation boundary.
- #41 completed: opening a scroll now forces the live BonusThree sprite at the shared SetStatus boundary, clearing the repeated Scroll(111) row/frame family found in 25 completed baseline controls.
- #42 completed: Strangle's two pre-action TurnFast short-circuit arms now retain Original's due victim virgin increment, clearing the real Soldier197 frame skew; the comparator also excludes only out-of-range uninitialized Original motion telemetry. The next f465 visibility/combat frontier is #45.
- #43 completed: legacy sprite adoption now preserves Original's profile-switch-before-position ordering, including the transient row computed from the constructed mission direction when the saved alternate-profile flag differs.
- #44 completed: final point-target Seek arrivals launch attached post-seek actions, and DropAle now uses Original's authored animation/DONE lifecycle and exact bottle placement/initialization. Three cross-family controls reach EOF.
- #45 completed: removed the invented Attacking-state gate from BeginSwordfight, restoring synchronous EVENT_GOTHIT retaliation at f465. The later frame1299 RNG frontier is #47.
- #46 completed: smalltalk strikes now route to the bespoke Human animation executor instead of the specialized melee owner, restoring the missing action-DONE damage/protection pair; the exact replay reaches EOF.
- #47 completed: fighter snapshots now use Original's committed gate-side position during door passage, clearing the extra SwordStrikeSelection draw at frame 1299; the next unrelated frontier is Soldier 187's AI substate at frame 1332.
- #48 completed: `CMD_CHANGE_WAY` now preserves Original's explicit second BreakMacro + ReturnToDuty tail after AssignNewPatrolPath's synchronous callback, restoring the duplicate path request in 39 controls.
- #49 completed: the complete fighter registry retains dead PCs for held Original pointers, and ShootArrowAt no longer invents a dead-target admission guard. The frame-1515 panic and command/animation mismatch clear; the same-frame extra post-shot seek RNG is #50.
- #50 completed: queued bow shots retain active human targets that died while aiming, matching Original's ShootArrowAt and clearing the extra SeekArea/SeekPointSelection batch. The next projectile-position frontier is #54.
- #51 completed: forced battle decisions persist after use, matching Original's never-cleared `mforcedNextBattleDecision`; nicouzouf/S069 replay-005 reaches exact EOF.
- #52 completed with its pending-query interpretation superseded by #63: the sequence manager retains the translating actor selection through synchronous condolence bookkeeping, but that actor pointer is not an entry in Original's launch list. The faithful recursion fix is the direct rebalance model in #63.
- #53 completed as superseded by #52: Restart replay-008 had the identical unbounded EnterSwordfight loop at frame 1423 (Soldier 80); current main clears it and reaches a normal visibility-query frontier. Savegame_054 replay-008 also now clears its former timeout and reaches a frame-1696 visibility frontier.
- #57 completed as superseded by #52: all four older-schema timeout controls were the same EnterSwordfight loop. Current main clears all seven baseline timeout controls; three expose ordinary state frontiers and one advances to a later frame-47910 RNG frontier.
- #54 completed: ground impact evaluation now mirrors Original's mixed-precision dominant-axis equation (`FLOAT` coefficients with `DOUBLE` secondary-coordinate evaluation), clearing Projectile192's exact-bit residual and every later frame. A pre-existing frame-776 visibility-query count remains as #56.
- #55 completed: an entry movement that becomes `Impossible` now preserves Original's authoritative `Aborted` Execute result across a synchronous condolence-installed successor; the representative clears frame 835.
- #58 completed: the sequence phase now records actual accepted Actor instruction boundaries, including zero-order elements that terminate synchronously, preserving Original's `InProgress` edge at frame 973.
- #59 completed: redundant `EnterSwordfight` preserves the already-running `WaitingSword` order across its terminal callback and transfers sprite execute identity to the replacement Wait; the frame-2141 animation/frame-2142 sprite lag clears with exact RNG.
- #60 completed: `AssertPosition` now exposes its pointer-changing `Skip` barrier, so Actor::Instruct skips the accepted-motion/order epilogue after Translate-time `SetState`, clearing the frame-1073 civilian motion frontier.
- #61 completed: decision-local swordfight target multiplicity now saturates as occupancy instead of stacking duplicate nearby-friend claims, clearing Soldier 115's hidden target-choice divergence and the frame-1162 visibility frontier.
- #62 completed: transition-distance movement now refreshes forecasted displacement through the same post-motion rule as ordinary walking, restoring bow target lead at frame 1216; the replay advances to an independent `VipIdleRemark` RNG frontier after frame 1600.
- #64 completed, promotion interpretation superseded by #68: task #67's corrected Stop callback ordering restores the frame-1600 ReconsiderSwordfight/ProposeGoodSwordStrike path without synchronously promoting the reciprocal Enter. Original non-WAIT `LaunchSequenceElement` leaves that element on the manager FIFO.
- #63 completed: `SequenceElementIsAboutToBeLaunched` no longer treats the translating actor pointer as queued, and `ReconsiderSwordfight` rebalancing now calls the direct engagement path instead of launching a recursive Enter command. The real visibility call is preserved, both controls clear their reported frontiers, and the former loop does not recur.
- #65 completed: human `PlayAnim*` translation now reaches the ordinary accepted Actor::Instruct epilogue instead of returning a `Skip` barrier, restoring the `InProgress` motion edge. The retained SuN replay-014 matches every recorded frame through EOF.
- #66 completed: direct `ReconsiderSwordfight` entry bypasses command-only opponent preparation, and terminal already-satisfied Enter translation now exposes a `Skip` barrier before the accepted-motion epilogue. The linux3 Profile_001 Savegame_013 replay-006 control matches every recorded frame.
- #67 completed: PrepareToEnterSwordFight now closes the selected Stop callback before snapshotting pending work, then stops each captured entry with its synchronous callback before continuing. Frame 1601 state and reciprocal links match, and the replay advances exactly to an independent frame-1837 PC motion frontier.
- #68 completed: `CalculateOpponentOfMauriceWhoIsNearestToRene` now compares Original's `UWORD`-truncated raw map-position MaxNorm distances, preserving first-opponent order on fractional ties. This prevents Soldier 84 from taking the wrong direct-rebalance branch and clears the frame-866 ReconsiderSwordfight RNG deficit; the exact replay advances to an independent frame-920 Soldier 80 position-goal frontier. The source-wrong reciprocal-Enter promotion from #64 was removed; #64's control still clears its former frame-1601 RNG frontier and advances to the independent frame-1837 PC motion frontier. Both task-36 replay-009 controls remain exact through EOF.
- #69 completed: orphaned sword movement now registers a plain deferred `QuitSwordfight` from Execute, leaving its lowering translation and `InProgress` projection to the later manager Instruct boundary. The linux3 Profile_001 Continue replay-001 control now matches every recorded frame through EOF.
- #70 completed: synchronous owner dispatch now propagates the original Actor::Instruct pointer-change barrier through movement, Seek, QuitSwordfight, smalltalk, turn/attention/wait, parry, position assertion, swordfight entry, and damage translators. Source-explicit terminal exits skip the shared epilogue, while accepted work publishes `mpOrder` and stamps motion `InProgress` even when an empty translated order terminates immediately afterward; fifteen focused synchronous regressions and the manager-phase edge tests pass.
- #71 completed: redundant EnterSwordfight retention is now discarded when it remains unconsumed at the end of SequenceManager::Hourglass. A retained WaitingSword can bridge only to a re-entrant actor-Hourglass lazy Wait, as in #59; manager-dispatched work is already past every actor slot, and Original's terminal condolence leaves `mpOrder` null when no replacement steals selection. This clears the broad Cyrdach/Nescafe actor.animation frontier without weakening the consumed sprite-identity handoff.
- #72 completed: accepted damage translations that produce no order now reproduce Original Actor::Instruct's epilogue ordering: stamp motion `InProgress`, detach `mpSequenceElement`, then terminate the element. A successful second parry can therefore resume postponed movement without its terminal condolence clearing that movement's live goal. The linux3 Profile_003 Restart replay-004 control clears Soldier 80's frame-920 position-goal frontier and advances to an independent frame-968 movement precision mismatch.
- #74 completed: recorded group moves now translate Original sparse sector identities through the retained topology map instead of requiring the recorded waypoint to lie inside the mapped polygon. Door/jump overlays still use semantic spatial selection; three Cyrdach/Nescafe schema-12 controls reach exact EOF.
- #77 completed: WAKING_UP DONE now dispatches synchronous NPC `EVENT_FITAGAIN` only when non-forced concussion clearing actually wakes the target. Original intentionally leaves tied, carried, or script-locked NPCs unconscious while still issuing the unconditional follow-up Wait; the linux2 Profile_002 Savegame_008 replay-006 invariant panic is cleared.
- #73 completed as source-verified no-fix: four retained schema-12 Cyrdach controls omit PC creation-order 89's required entry into Sherwood deployment zone 14. At the exact `(1546.740845,400.692596) -> (1549.510132,399.538757)` movement, current Original `SBGeoPolygon2DArray::IsInside` and Rust both change outside-to-inside. Original `SBGeoSegment2D::IsIntersecting` also necessarily accepts the active script edge `(1771,648.00037) -> (1526,375.00034)`: endpoint determinants are `+632.35547/-406.35156`, the reciprocal pair is `-943.62518/-95.08084`, and the old-position determinant is nonzero. `RHelementactor.cpp::CheckForLineCrossing` therefore dispatches `RHSectorScript::Enter`, contrary to the archived traces. Disabling the schema-12 beam-PC identity remap did not change the divergence. A proposed layer/motion-sector guard was rejected because Original uses `IsReallyInside` only for bulk initialization and post-flight reconciliation, not ordinary line crossing. These four records are excluded individually by `docs/PARITY_RETIRED_TRACES.txt` until they can be re-recorded with the current Original build/assets; no Rust trace-specific suppression was added.
- #79 completed as corpus hygiene: three old-corpus linux3 recordings contain valid zstd streams but truncated JSONL records and panic the reader with EOF mid-value. A per-trace retirement manifest excludes only those malformed recordings (plus the four #73 source-inconsistent records) without retiring their otherwise-valid neighboring save/replay families.
- #75 completed as source-verified capture-required/no-fix: the schema-12 Nescafe H12 `RuntimeBuildingExitWait` cohort lacks the macro timer, cursor/remaining, and busy-state fields needed to distinguish the recorded dormant state from a timer that should resume. In the representative Profile_001 Restart replay-002, Rust Civilian 85 enters sector 166, arms `WAIT 250` and then `WAIT 50`, and consumes the extra exit-wait pair before frame 508; Original Civilian 85 remains inactive in `Wait`/`DefaultInMacro`, while Original's pair at frame 526 belongs to Civilian 95 as it changes to `WaitTimer`/`DefaultEnroute`. Current Original source ticks inactive actors, always arms `CMD_WAIT` timers, and polls macro timers, so it provides no basis for an inactive-actor or timer-suppression exception. A fresh schema-14 Original capture is required before any production change; the available Original executable is i386 and this host currently lacks `/lib/ld-linux.so.2` plus its 32-bit SDL/freetype/theora/vorbis/ogg/crypto/C++ runtime dependencies. Retire or replace these old controls only after that recapture.
- #80 completed: combat-flight displacement now runs at the falling actor's live creation slot instead of the later global melee batch, matching Original `ExecuteFallingHit`/`PerformFlight` ordering. In Nescafe Profile_001 Savegame_001 replay-008, unconscious Soldier 177 (creation order 208) now commits `(1468.115479,2764.299805)` before Soldier 179's detection slot (creation order 210), so the body visibility endpoint and resulting direction 11 match at frame 382; the trace stays exact through frame 1105. Nescafe Profile_002 Savegame_001 replay-015 also clears its frame-1518 visibility destination and advances to an independent frame-1603 AI lifecycle frontier. Terminal landing reconciliation remains after synchronous completion callbacks to retain the already-exact final `NewMove` delta; a focused test verifies owner-scoped flight advances only the selected creation slot.
- #82 completed: generic per-Think data now populates sleeping-enemy candidates from the live, creation-ordered opposing fighter registry, matching Original `KillNearbySleepingEnemies` even on timer-driven `BattleDecisions` without a detection aggregate. The SuN1Sh1nE Profile_004 Savegame_034 replay-014 control clears its frame-1036 missing sleeping target and compensating MacroRand divergence, advancing to an independent PC motion-state frontier at frame 1438; both task-36 replay-009 controls remain exact through EOF.
- #84 completed: observation side-step crowd scoring now builds the soldier's facing vector with Original's `SetSector0to15(direction, ASPECT_RATIO)` geometry before taking the perpendicular and truncating scalar products to `SWORD`. The previous ordinary unit-circle vector reversed Soldier 178's chosen side-step in Nescafe Profile_001 Savegame_001 replay-008 at frame 1106 despite an exact RNG cursor. The corrected debug runner matches every recorded frame through EOF; all 84 focused enemy-AI tests pass.
- #86 completed: a soldier reaching a nearby officer now preserves Original's synchronous `SeekingRunningToOfficer -> SeekingRunningToOfficerSeen -> Think(EVENT_REACHPOINT)` lifecycle instead of substituting a one-frame timer. In Nescafe Profile_002 Savegame_001 replay-015, the target officer was already back in `Default`, so Original immediately returned Soldier 131 to duty at frame 1603 while Rust exposed the transient Seen/Yellow state. Reentrant self-stimulus delivery now performs the source-authored same-frame callback; the debug replay matches every recorded frame through EOF and all 85 focused enemy-AI tests pass.
- #88 completed: anti-collision snapshots now read the target exemption directly from `PositionInterface`, matching Original's serialized `RHPositionInterface::mpTargetElement`, and every newly initialized sprite motion copies its order antagonist there. Friday cleanup may legitimately remove an interrupted sequence while Rust's derived `active_movement` latch still names it; snapshots no longer dereference that stale latch. The linux3 Profile_001 Savegame_045 replay-008 control clears its frame-44032 panic and advances to an independent detection-list frontier at frame 44569; focused anti-collision and motion-initialization tests pass.
- #91 completed: finishing a PC's beggar-entry transition now invokes Original's `AddBeggarForAllIntelligentSeekingSoldiers` side effect after toggling nearby coin flags. The ordered scan appends a unique Beggar detectable only to Lacklandist soldiers with IQ at least 30 whose live substate belongs to `_ANY_SEEK_AREA_SUBSTATE_`. In linux3 Profile_001 Savegame_045 replay-008, PC 344's frame-44569 DONE edge now registers with seeking Soldier 118, clearing the detectable-list mismatch and advancing to an independent command/animation frontier at frame 44976; a focused eligibility and uniqueness test passes.
- #93 completed: reaching a beggar now launches Original's single two-level identification sequence: level 1 `TurnFast` with the live beggar direction, followed by level 2 `StartMenace` for melee soldiers or `EquipBow` for archers. Rust previously emitted a separate face order plus two independent command sequences, so `StartMenace` interrupted both turns during the same drain. The linux3 Profile_001 Savegame_045 replay-008 control clears Soldier 118's frame-44976 command/animation mismatch and advances to an independent Civilian 63 BeggarShowFace frontier at frame 45007; focused melee and archer ordering/direction/timer coverage passes.
- #95 completed: NPC `BeggarShowingFace` now falls back to the `Rolling` sprite animation when the current profile lacks the authored showing-face animation, exactly matching `RHElementActorNPC::Execute`. The order remains `BeggarShowingFace` so its posture and completion dispatch is unchanged. The linux3 Profile_001 Savegame_045 replay-008 control clears Civilian 63's frame-45007 abort and advances to an independent Soldier 119 turn-direction frontier at frame 45041; focused present/missing-animation coverage passes.
- #97 completed: downward leaning-out bow shots now initialize their facing from the shooter and live antagonist `PositionMap`, matching the soldier override instead of reusing the Human arm's `PositionGround` sampling. The selected bow owner already performs the source-authored per-tick `Turn()` and freezes the shooting animation's first frame while turning; feeding it the correct map-space goal makes Soldier 119 advance from direction 14 to 13 toward goal 10. The linux3 Profile_001 Savegame_045 replay-008 control clears frame 45041 and advances to an independent projectile spawn-Y frontier at frame 45043; focused goal/turn/row/frozen-frame coverage passes.
- #100 completed: downward leaning-out bow launch points now use Original's isometric `GetDirectionVector` geometry for their 20-unit forward shift. The former unit-circle vector left diagonal projectile Y off by 4.65 units while X happened to match. The linux3 Profile_001 Savegame_045 replay-008 control clears frame 45043 and advances to an independent StopMenace/path frontier at frame 45056; focused diagonal coordinate coverage passes.
- #102 completed: AI `GoTo`/`GoNear`/`GoToSpeed` issued from `ActionState::Menacing` now preserve Original's single ordered sequence with level-1 `StopMenace` before the movement. Rust previously emitted a standalone stop channel followed by an independent move launch in the same drain, allowing the move to preempt the menace-exit transition and queue a path immediately. The serialized movement intent now carries the prefix through both local and gate-path builders, alongside the existing quit-swordfight/lower-shield prefixes; the standalone channel remains available to non-GoTo producers. The linux3 Profile_001 Savegame_045 replay-008 control clears Soldier 118's frame-45056 command/path mismatch and advances to an independent Soldier 140 motion-state frontier at frame 45294; the task-36 Cyrdach and Nescafe replay-009 controls remain exact through EOF, and focused intent round-trip/exact level-ordering tests pass.
- #92 completed: the current actor's anti-collision snapshot now refreshes its target exemption immediately after `PerformMotion` initializes a new order, matching Original's serial `mpTargetElement` update before the same-slot repulsion query. A transition order with no antagonist previously inherited the preceding seek order's target for one extra Rust frame, suppressing the target's repulsion and choosing direction 10 instead of 12. Both Cyrdach Profile_156 replay-006 controls (Restart and Savegame_000) now match every recorded frame.
- #89 completed as source/assets-verified no-fix: three old nicouzouf Savegame_051 captures record newly spawned arrows on layer/sector 0, but their exact current-asset landing points `(590.03455,564.7571)`, `(632.18756,520.73846)`, and `(591.2044,534.9738)` lie inside motion obstacle sector 18 as well as ground area 0. Current Original `ComputeTrajectory` iterates both sectors, first assigns area 0, then clears the sector and returns on the non-area obstacle before `SetLayer`, leaving layer `0xFFFF`/sector null exactly as Rust does. The arrow's recorded first-frame position and movement match Rust bit-for-bit, so the source-relevant trajectory is not divergent. The three contradictory captures are retired individually pending recapture; no production suppression was added.
- #78 completed: AI `Focus` now retains Original's generic-element semantics instead of incorrectly asserting that every target is human. This permits the Ale handler's legitimate focus on a Bonus object while still panicking for a missing raw entity slot. SuN1Sh1nE Profile_004 Savegame_034 replay-014 clears the frame-1008 invariant panic and advances to an independent frame-1035 `VipIdleRemark` RNG frontier; a focused object-follow regression covers the boundary.
- #81 completed: Human's retained PC shoot list now re-admits a `Terminated` element as the retail Original does. Original serializes/restores the raw pointer and guards it only with a debug assertion in `Actor::Instruct`; Rust's ordinary manager terminal-state rejection remains intact, while the narrowly scoped held-shoot admission proceeds without rewriting the terminal state first. linux3 Profile_001 Savegame_022 replay-012 clears its invariant panic and advances to a frame-738 sprite frame-count residual.
- #101 completed: deferred attentive-mode requests now preserve the first request for an unchanged target, matching Original's synchronous `will_be_attentive` update. The 12-trace linux3 Savegame_016 officer-leave cluster clears frame 7670 and advances to the independent frame-7728 visibility-query frontier; both task-36 schema-14 replay-009 controls remain exact through EOF.
- #83 completed, refining #81: retail does bypass `Actor::Instruct`'s entry assertion for a retained terminal shoot pointer, but its explicit post-`GenerateTransition` terminal guard still rejects the element before arbitration/translation. Rust now preserves that guard, the existing command-Wait/AimingWithBow order, and the referenced terminal backing allocation across Friday cleanup. linux3 Profile_001 Savegame_022 replay-012 clears the frame-738 restart and frame-768 cleanup boundaries, advancing to an unrelated frame-999 PC position-goal frontier; both task-36 replay-009 controls remain exact through EOF.
- #85 completed: Human HIT translation now mirrors Original's synchronous `Think(EVENT_STOP)` callback when the antagonist is moving, after installing the Hitting order and before action initialization. The callback halts the victim, enters `SeekingGotStopEvent`, and blinks its enemy detection. That substate now also preserves Original's fallthrough from the no-op attentive-mode arm into `SetAlertStatus(ALERT_YELLOW)`. Nescafe Profile_002 Savegame_001 replay-002 clears its frame-686 Soldier181 state/animation/visibility/alert boundary and advances to an unrelated Soldier180 attentive-mode frontier at frame 1651; both task-36 replay-009 controls remain exact through EOF.
- #87 completed: standalone `IsDetecting180Degrees` now builds its viewer and target endpoints from the actors' raw element anchors, matching Original's `ComputeEyesPoint`/`ComputeDetectionPoint`, rather than the AI-facing `Position()` that may substitute a door endpoint or carrier. This restores Soldier180's clear frame-1651 ray to PC252, keeps `TooProudToAttackApproach`, and queues the next Move instead of entering attentive overview. Nescafe Profile_002 Savegame_001 replay-002 and both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #90 completed: `HeyFolksLookThere` now evaluates recipients' live AI state/range in Original creation order at the synchronous owner boundary, rather than preselecting from the top-of-tick snapshot. Patrol subordinates also preserve Original's direct call to the chief's `DispatchStimulusToWholePatrol`: an already-Attacking chief rejects the delegation without receiving an invented `Think(CALL_LOOKTHERE)`, while delivery remains unconditional after a recipient is selected as established by #39. The nicouzouf Profile_001 Savegame_067 replay-008 frame-761 double-Wondering frontier clears and advances to an independent frame-815 PC107/Soldier72 EnterSwordfight frontier; both task-36 schema-14 replay-009 controls remain exact through EOF.
- #94 completed: `IsTooProudToAttack` now uses Original's full-world, isometric-Y `MaxNormDistance` for its maximal-sword-range gate instead of a raw 2D map max norm. At the nicouzouf Profile_001 Savegame_067 replay-008 frame-815 geometry, that restores Soldier72's `AttackingTooProudToAttackRetire` decision and prevents the incorrect PC107/Soldier72 swordfight links. The replay advances to an independent frame-862 Soldier61 path-goal frontier; both task-36 schema-14 replay-009 controls remain exact through EOF.
- #96 completed as source-verified no-fix: nicouzouf Profile_001 Savegame_067 replay-008's frame-862 `(725,539)` seek goal contradicts the current fullgame_linux HOLE data and shipped `SeekArea`. The exact frame-701 center/direction makes point 29 `(665,677)` the mandatory primary directional point; point 62 `(725,539)` is only a fallback. No seek lock, door teleport, mutable-position write, or path-goal adjustment can produce the archived choice. Replay-009 from the same save records the same impossible point-62 choice at frame 723. Both stale traces are retired pending re-recording with the current Original build/assets; no Rust workaround was added.
- #99 completed: Enemy suspect cooldown now follows Original's `uwSumOfSharpnesses == 0` condition instead of requiring that no enemy be visible. An already-latched visible target contributes no fresh sharpness, so Savegame_linux2 Profile_002 Savegame_018 replay-011 frame 8660 now cools Soldier65's Enemy suspicion from 40 to 39 and the replay matches every recorded frame through EOF. Both task-36 schema-14 replay-009 controls remain exact through EOF.
- #103 completed: `AddBeggarForAllIntelligentSeekingSoldiers` now tests Original's difficulty-modified `GetIQ()` instead of raw profile IQ. On Hard, Savegame_linux2 Profile_002 Savegame_016 replay-013 frame 23146's Soldier104 reaches the threshold with base IQ 15 and registers newly disguised PC172 in its Beggar detectable list, restoring the missing 67th entry. The representative and both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #104 completed: the `OwnerActionBarrier::Skip` handling added in #76 for `ParrySword`, `ParrySwordLow`, and `StopParrySword` also clears linux3 Profile_001 Savegame_045 replay-008's later frame-45294 motion-state frontier. Soldier 140's parry-exit sprite correctly returns `Start`; a same-frame redundant `ParrySword` generates its exit prefix and terminates during `Translate`, and the barrier prevents the accepted-`Instruct` epilogue from overwriting that edge with `InProgress`. A focused manager-path regression now covers the exact generated-prefix lifecycle. The representative and both task-36 replay-009 controls match exact EOF.
- #105 completed, superseding #75's capture-required disposition: a fresh schema-14 recapture exposed the earlier source-faithful boundary. Outside-Think multi-point patrol-macro completion now applies its outer flag cleanup after the synchronous already-on-point `EventReachPoint` recursion, preserving the nested WAIT deadline but clearing both macro-running flags. The recapture reaches exact EOF; the frozen representative clears frame 508 and advances to frame 1495, the secondary AiRandomValueRectangle sample clears frame 507 and advances to frame 634, and both task-36 controls remain exact.
- #106 completed: combat-position damage estimation now uses the defender's live facing for localized sword protection, matching `RHSword::GetProtection`; the candidate-adjusted facing remains limited to Original's from-behind heuristic. This removes a false sector-12/sector-13 score tie for SuN1Sh1nE Profile_004 Savegame_036 replay-002 at frame 672, and the replay advances to an independent frame-898 command/substate frontier. Three neighboring direction-goal controls retain their known later unrelated frontiers.
- #107 completed: legacy-save lateral sword sweeps now lazily rehydrate executable `ActorData::sweep_state` from the serialized human sweep mirror and synchronize consumed victims and angles back into that mirror. SuN1Sh1nE Profile_004 Savegame_009 replay-001 and both task-36 schema-14 replay-009 controls match every recorded frame through EOF; sibling replays 005 and 013 also clear the former Soldier134 animation frontier.
- #109 completed: patrol-wide `EVENT_GET_ARROW` handling now preserves Original's unconditional `Focus(mpInterestingObject)` call. A null interesting object therefore clears the temporary point Stare installed by a nested `CALL_LOOKTHERE`, restoring LookForward for each later patrol recipient while leaving `SeekingArrowReactiontime` intact. SuN1Sh1nE Profile_004 Savegame_011 replay-001 matches every recorded frame through EOF.
- #108 completed: final building `Move|SEEK` sequence tails now retain Original's `RefreshingSeek` non-animation instead of taking the ordinary interior-move termination path. The explicit hold unconditionally calls `RefreshSeek` on the following owner slot, allowing `SEEK_IN_BUILDINGS` to snap to the target and launch its post-seek action. linux2 Profile_002 Savegame_031 replay-002 clears the frame-11906 Move/animation frontier and advances to an independent frame-13016 Soldier58 position-goal mismatch; both task-36 schema-14 replay-009 controls remain exact through EOF.
- #110 completed: the manager Move/Seek dispatcher now honors its pointer-changing `OwnerActionBarrier::Skip`, matching Actor::Instruct's return immediately after Translate changes `mpSequenceElement`. A building-interior Move that synchronously terminates no longer runs the accepted-Instruct epilogue and overwrites an earlier actor `Done` edge with `InProgress`. SuN1Sh1nE Profile_004 Savegame_002 replay-001 and both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #111 completed: AI RaiseShield no longer restores the movement goal that the preceding synchronous `StopAll`/`Halt` condolence cleared. The restoration flag was an obsolete workaround from before Halt delivered its selected movement cleanup at the source-authored barrier; Original clears `PositionGoalMap` before the non-directional RaiseShield sequence takes ownership. linux2 Profile_002 Savegame_031 replay-002 and both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #112 completed: climb-lift translation now preserves an explicitly authored `RunningUpright` action even when the movement element does not carry `RHMOVE_FAST`. Original passes the action directly to `RHSectorLift`, whose ladder mapping selects `RHNONANIMATION_CLIMBING_LADDER_UP_FAST`; Rust had instead downgraded the action from the flag before mapping it. SuN1Sh1nE Profile_004 Savegame_021 replay-009 clears Soldier99's frame-518 animation-35/296 frontier and advances to an independent frame-594 direction/sprite-row frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #113 completed: tower-guard `EVENT_DONE` now suspends its trailing `BattleDecisions` until every synchronous `CALL_TOWER_GUARD_ALERT` and `CALL_TOWER_GUARD_CALLS_ME` recipient has returned. Original's direct calls update nearby soldiers' alert status before the guard can enter `SeekArea`; Rust previously planned the seek inside the borrowed handler before draining those calls, reducing the nearby-friend multiplier and consuming only 15 of Original's roughly 112 seek-point selection pairs. Nescafe Profile_001 Restart replay-008 clears the frame-297 `SeekPointSelection` frontier and advances to an independent frame-307 Soldier225 attentive-mode frontier; Profile_003 advances to the same frontier, while Profile_002 clears the seek-point count and exposes its pre-existing schema-12 `RuntimeBuildingExitWait` pair. Both task-36 schema-14 replay-009 controls match exact EOF.
- #114 completed: PC concussion handling now resolves coma status through the serialized campaign-description identity instead of the unrelated actor/UI list index. Same-profile PCs can occupy different campaign slots; borrowing the UI slot's coma bit caused a zero-concussion arrow hit to knock out the wrong PC and dissolve both combat opponent lists. SuN1Sh1nE Profile_004 Savegame_017 replay-007 and both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #115 completed: lift traversal preserves inherited facing at all three Original lifecycle boundaries. Lift direction is applied only while a climb order initializes; completing `PassDoor` changes topology without setting a new direction goal; and fallback `Wait` normalizes lift posture/Freezing without snapping direction. SuN1Sh1nE Profile_004 Savegame_021 replay-009 clears the frame-594/604/605 ladder-facing chain and advances to an independent frame-614 AI/visibility frontier; both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #116 completed: tower-guard runner fallback now preserves Original's two coupled selection bugs. The hearing-list size bounds a scan of the first N camp-registry soldiers rather than the hearing list itself, and every candidate is compared with the fixed far-officer distance, so the last qualifying prefix actor wins instead of the nearest hearer. Nescafe Profile_001 Restart replay-008 no longer sends `CALL_TOWER_GUARD_CALLS_ME` to Soldier225 and matches exact EOF; Profile_003 clears the same frame-307 frontier and advances to an independent frame-396 Soldier114 group/list-us and MoveWaiting frontier. Both task-36 schema-14 replay-009 controls match exact EOF.
- #117 completed: gate-route authorization now checks exact live building capacity for direct NPC entry during both seed and neighbor expansion, matching `RHDoor::IsActorAutorized`. Immutable AI routing receives a canonical sector-to-authorization snapshot and panics on missing building state instead of inventing capacity. Nescafe Profile_002 Continue replay-008 clears the frame-310 `RuntimeBuildingExitWait` pair and advances to an independent frame-465 RNG frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #118 completed: near-enemy sight reactions now use Original's world-space 3D `MaxNormDistance`, including stretched world Y and elevation, instead of raw map Y that already contains elevation projection. SuN1Sh1nE Profile_004 Savegame_021 replay-009 clears Soldier97's frame-614 turn-versus-bow-decision frontier and advances through frame 853 to an independent frame-854 seek RNG frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #120 completed: reaching a `SeekingCombatAlert` hint now starts Original's plain lost-enemy `SeekArea` around the stored point instead of running a fast battle overview and reacquiring an enemy. Nescafe Profile_002 Continue replay-008 and both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #121 completed: failed attacking approaches now fall through to Original's `CouldntReachpointEmergencyRoutine` and `GetBattleOverview` instead of an invented branch that rebuilt combat groups and replanned movement. Nescafe Profile_003 Restart replay-008 and both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #119 completed: same-position `GoTo`/`GoToSpeed` now observes an Original-synchronous `Halt` queued earlier in the same Think, including actor effects suspended into a state-change owner-work prefix. SuN1Sh1nE Profile_004 Savegame_021 replay-009 clears the frame-854 seek acceptance/movement frontier and advances through frame 896 to an independent frame-897 direction-shuffle RNG frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #122 completed: civilian friend detectables now preserve Original's soldier-registry iteration order instead of nondeterministic entity-view `HashMap` order, with strict live-view resolution. Nescafe Profile_001 Restart replay-009 clears the old frame-251 visibility and frame-286 list-order frontiers, advancing to an independent frame-441 missing path event; both task-36 schema-14 replay-009 controls match exact EOF.
- #123 completed: conscious `EVENT_GOTHIT` now drains its queued eye-status write at the synchronous damage boundary, matching Original's inline `SetViewStatus(EYES_DIE_OR_GET_UNCONSCIOUS)`. Nescafe Profile_001 Savegame_007 replay-002 and both task-36 schema-14 replay-009 controls match every recorded frame through EOF.
- #126 completed: shield-bearer `CALL_COORDINATE` is now ignored during `AttackingBowAiming`, `AttackingBowLoading`, and `AttackingBowObservingLoading`, matching Original's switches; only the shooting substate handles it. Cyrdach Profile_156 Restart replay-014 clears the frame-1418 bow-transition frontier and advances to an independent frame-1480 combat-decision frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #124 completed: deferred same-point `GoTo` now projects pending Halt multiplicity through Original's `StopMovement` action families instead of treating every queued Halt as immediately idle. SuN1Sh1nE Profile_004 Savegame_021 replay-009 clears the frame-854 and frame-897 seek frontiers and advances to an independent frame-977 PC life-point frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #127 completed: archers reject cover behind a shield bearer whose primary target is null, matching Original's failed `ComputePositionBehindMyShieldBearer` and Shoot/Observe fallback instead of inventing a cover-view position. Cyrdach Profile_156 Restart replay-014 clears the frame-1480 frontier and advances to an independent frame-1481 phalanx-timer RNG frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #125 completed: deferred AI GoTo route construction now preserves Original's call-time source topology, while moving-fast EventView distance reads the actor's literal live position instead of its door-side AI forecast. Nescafe Profile_001 Restart replay-009 clears frame 441 and advances to an independent frame-451 order-transition frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #129 completed: arrow and stone hits on ladder/wall occupants now apply Original's piercing damage before translating the ladder-fall reaction. SuN1Sh1nE Profile_004 Savegame_021 replay-009 clears the frame-977 life mismatch and advances to an independent frame-978 motion edge; both task-36 schema-14 replay-009 controls match exact EOF.
- #128 completed: combat-neighbour teardown now clears reciprocal links, while preserving Original's incoming-substate line-mode quirk when entering a phalanx. Cyrdach Profile_156 Restart replay-014 and both task-36 schema-14 replay-009 controls match exact EOF.
- #130 completed: ladder-fall arrival now returns and latches Original's terminal motion edge instead of being overwritten by the earlier sprite Start result. SuN1Sh1nE Profile_004 Savegame_021 replay-009 clears frame 978 and advances to an independent frame-1302 lethal/coma frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #132 completed: goto-post arrival and completion now dispatch through the enemy virtual `SetState` tail, preserving Original's Face/posture ordering while emitting LeaveAttentiveMode and Green alert. nicouzouf Profile_001 Savegame_065 replay-001 clears frame 440 and advances to an independent frame-688 group movement frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #133 completed: `GetKilledAtBottom` now closes Original's virtual PC wounded boundary, allowing VIP amulet coma saves before terminal translation. SuN1Sh1nE Profile_004 Savegame_021 replay-009 and both task-36 schema-14 replay-009 controls match exact EOF.
- #131 completed: Observe now reads the live persistent primary target and launches its replacement `GoNear` before `SetState`, matching Original's synchronous actor-effect staging. Nescafe Profile_001 Restart replay-009 clears frame 451 and advances to an independent frame-505 animation frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #135 completed: owner-local script `StopActor` rewrites that reseed a walking order after Execute Start no longer masquerade as successor advancement. SuN1Sh1nE Profile_004 Savegame_004 replay-005 clears frame 16287 and advances to an independent frame-16440 motion frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #136 completed: `FORCE_SWORD_MOVEMENT` is now limited to Original's already-translated sword-move Execute guard and no longer converts a postponed ordinary walk after swordfight teardown. Nescafe Profile_001 Restart replay-009 and both task-36 schema-14 replay-009 controls match exact EOF.
- #137 completed: the StopMovement Start-edge safeguard now distinguishes a newly reseeded in-place order from an older prebuilt transition successor. SuN1Sh1nE Profile_004 Savegame_004 replay-005 clears frame 16440 and advances to an independent frame-16482 speech-request panic; both task-36 schema-14 replay-009 controls match exact EOF.
- #138 completed: `NearbyCiviliansPanic` now retains Original's visibility work for active outdoor civilians regardless of death or unconsciousness. SuN1Sh1nE Profile_004 Savegame_002 replay-009 clears frame 36195 and advances to an independent frame-36627 target-list frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #139 completed: beggar identification now queues Original's civilian self-identification remark immediately after the show-face command through the strict target speech path. SuN1Sh1nE Profile_004 Savegame_004 replay-005 clears frame 16482 and advances to an independent frame-16513 movement/goal frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #134 completed: battle target multiplicity now follows Original's UWORD stacking and `ProposeShotTarget` reset/bow-only rebuild across failed Shoot fallback, using strict live primary targets. The Task61 linux3 Profile_001 Continue replay-001 control clears frame 1162 and advances to an unrelated frame-1506 goal frontier; both task-36 schema-14 replay-009 controls match exact EOF.
- #141 completed: targets appended after BattleDecisions' personal-list reset retain the shared live multiplicity before source-authorized increments. SuN1Sh1nE Profile_004 Savegame_002 replay-009 and both task-36 schema-14 replay-009 controls match exact EOF.
- #140 completed: non-push sword damage that amulet-saves a PC into a coma now terminates while selected and returns the pointer-change Skip barrier, clearing the interrupted goal while preserving the prior Execute motion edge. SuN1Sh1nE Profile_004 Savegame_004 replay-005 and both task-36 schema-14 replay-009 controls match exact EOF.
- #143 completed: arrow translation now reads the post-wound posture after piercing damage, matching Original's lethal VIP amulet/coma boundary and allowing selected-element cleanup to clear the interrupted movement goal. The linux3 Profile_001 Continue replay-001 representative and both task-36 schema-14 replay-009 controls match exact EOF.
- #145 completed: building PassDoor translation now uses the movement element's stamped posture/action and canonical sword/shield categories, preserving `MovingFastSword` as `RunningWithSword` even when the actor's live state has already changed. SuN1Sh1nE Profile_004 Savegame_001 replay-001 and both task-36 schema-14 replay-009 controls match exact EOF.
- #147 completed as already-fixed: Cyrdach Profile_156 Savegame_001 replay-011's historical missing phalanx visibility query was already corrected by `daeacac810`, which performs Original's 180-degree query before rejecting a dead or unconscious target. The current runner matches exact EOF; no new production change was needed.
- #148 completed as already-fixed: Nescafe Profile_001 Savegame_001 replay-006's historical retained RaiseShield movement goal was already corrected by `ac00ab0c9` / task #111. The current runner clears the goal and matches exact EOF; no new production change was needed.
- #142 completed as stale-capture retirement: current Original and the archived nicouzouf Profile_001 Savegame_065 replay-001 first disagree at frame 486 despite identical inputs, and no longer share the alleged frame-712 frontier. Fresh current Rust matches fresh current Original for all 685 frames, so no trace-specific workaround was added; the archived recording must be replaced before a full-universe sweep.
- #146 completed: combat primary-target multiplicity now mirrors Original's nonserialized, owner-ordered per-human UWORD scratch counters across BattleDecisions, combat-position preparation, observation reconsideration, and shot fallback. The within-call `IsTooProudToAttack` re-pick reads the locally mutated counters rather than an owner-start snapshot. The SuN Task61/134/141 controls and both task-36 controls match exact EOF; the SuN direction-goal representative clears frames 14540/14716 and advances to an independent frame-15016 sound FIFO frontier.
- #149 completed: far-opponent pruning now preserves the initiating actor's cached relative fighting ability while invoking authoritative reciprocal `DeleteOpponent` recomputation, initiative, and quit side effects, matching Original's asymmetric raw-delete lifecycle. The Cyrdach representative clears its frame-1073 missing `MeleeInitiative` draw and advances to an independent frame-1458 RaiseShield/Wait frontier; both task-36 controls match exact EOF.
- #150 completed: redundant QuitSwordfight manager dispatch now preserves its terminal Skip edge instead of applying the generic InProgress epilogue, and post-completion projection uses the actor's installed-order/mpOrder mirror rather than manager-resident fallback work. SuN1Sh1nE Profile_004 Savegame_028 replay-012 and both task-36 controls match exact EOF.
- #152 completed: RaiseShield translation no longer rejects a live sword/bow action after `GenerateTransition` has authored its exit transition. Matching Original, Rust retains the lowering-sword order and appends RaisingShield unless the actor is already holding/moving with a shield. Cyrdach Profile_156 Savegame_001 replay-001 and both task-36 controls match exact EOF.
- #154 completed: jump-line movement execution now applies Original's posture eligibility gate while cursor preview remains posture-agnostic. Rust previously admitted a helper-required line for upright Pc174 and projected its direct approach onto the wrong source-line geometry. linux2 Profile_002 Savegame_015 replay-010 and both task-36 controls match exact EOF.
- #151 completed: corpse-removal intersection scans now preserve Original's live `marrayActors` mutation visibility. Rust previously captured only initially-small neighbours before recursively re-evaluating them, so a later corpse changed from large to small by an earlier recursive call was never revisited. The removal path snapshots only creation-ordered human identities and re-checks current radius flag, posture, topology, and distance before each recursion. Savegame_linux2 Profile_002 Savegame_003 replay-010 clears its frame-14693 movement and frame-15016 sound FIFO frontiers and advances to an independent frame-15133 Soldier140 command/animation boundary; both task-36 controls remain exact EOF.
- #155 completed: actor work issued before a trailing synchronous `StopAll` now closes through an ordered owner FIFO barrier. This preserves Original's `GoTo`-then-Halt statement order in defensive swordfight-observation fallthrough: the active stronger QuitSwordfight survives while its following Todo Move is interrupted, instead of Rust halting the older sequence and launching the new Move afterward. Savegame_linux2 Profile_002 Savegame_003 replay-010 clears frame 15133 and advances to an independent frame-15171 RNG frontier; both task-36 controls remain exact EOF.
- #153 completed: selected PC swordfight entry now forwards Original's `MSG_SELECT_ACTION(NO_ACTION)` before `DisableAllActionsTemp`, preventing a previously armed Purse action from being saved and resurrected after the final opponent is removed. Savegame_linux2 Profile_002 Savegame_001 replay-001 clears the old frame-1765 movement-stop/animation frontier and advances to an independent frame-2147 difference; both task-36 controls remain exact EOF.
- #156 completed as a source-proven focused mismatch: `EnableAllActionsTemp` now restores `saved_action` only when an exact newly re-enabled authored profile slot matches it, and forwards restoration through selected-PC action semantics instead of directly assigning `current_action`. Focused tests cover matching and nonmatching slots, targeted multi-selection collapse, selected swordfight exit, and the unselected no-op; no dedicated replay frontier was claimed.

- #157 completed: the heard-steps reaction-point handler now follows Original into `SeekArea(Position(mpMe), 0, SEEK_LOCATION_FIRST | SEEK_WALKING)` instead of entering an invented just-watching timer shortcut. Savegame_linux2 Profile_002 Savegame_002 replay-015 and both task-36 schema-14 replay-009 controls match exact EOF.
- #158 completed: all four `ReconsiderEnemyApproach` movement branches now preserve Original's GoNear/GoTo → already-on-point check → SetState statement order. Nescafe Profile_001 Savegame_001 replay-013 clears the missing frame-777 RunningUpright path and matches exact EOF; both task-36 schema-14 replay-009 controls remain exact EOF.
- #159 completed: AI entity views now expose a logically live alert path from `detached_patrol_path_status` while its Rust `PatrolPath` is temporarily unmaterialized. This restores Original's synchronous `InitializeFriendCheck` path scan and preserves the following macro cursor/timer until `EVENT_SEES_CHARLY`. Cyrdach Profile_156 Savegame_001 replay-014 and both task-36 schema-14 replay-009 controls match exact EOF.
- #160 completed as already-fixed: the frozen SuN1Sh1nE Profile_004 ExQuickSave replay-010 visibility representative formerly had 21 Original queries versus 20 Rust after frame 36672; the newest existing runner now matches every recorded frame through exact EOF at frame 36960. No production change was needed. The sampled leading schema-14 RuntimeBuildingExitWait representative also clears its old frontier and advances to the independent task-161 frame-44565 RNG boundary.
- #161 completed as current-Original-inconsistent/no-fix: the archived linux3 Profile_001 Savegame_045 replay-011 records Soldier 105 entering ReturnToDuty at frame 44566, but a direct current-Original probe with the exact save and seeds matches Rust's watchdog cadence and counter (0 at f44465, 1 at f44529, 2 through f44577), with no ReturnToDuty or ReturnToDutyCommonStuff call. The next applicable watchdog slot is f44593. Retire the stale control pending recapture; no Rust workaround was added.
- #163 completed: `EVENT_SEES_BEGGAR` no longer requeues the beggar already under examination, matching Original's explicit `pHuman != mpBeggarToExamine` gate around the queue and coupled effects. linux2 Profile_002 Savegame_003 replay-013 clears frame 14509 and advances to an independent frame-14874 sword-strike frontier; both task-36 controls remain exact EOF.
- #162 completed: BattleDecisions now measures its owner-to-primary-target reference with Original's literal 3D `SquareDistance` (stretched world Y, included Z, ULONG truncation) while retaining committed door-side raw-map norms for friend comparisons. linux2 Profile_002 Savegame_001 replay-001 clears frame 2147 and advances to an independent frame-2150 visibility-query-only frontier; both task-36 controls remain exact EOF.
- #164 completed: `EvaluateSwordfight` now treats incompatible elevation and sector as Original's immediate reciprocal teardown and returns before the later distance/LOS block. linux2 Profile_002 Savegame_029 replay-014 clears the extra frame-4442 visibility query and advances to an independent frame-4692 parry/damage frontier; both task-36 controls remain exact EOF.
- #166 completed: lateral sword sweeps now retain only the victims admitted by Original's authored angular scan; Rust no longer recovers an out-of-arc interaction antagonist through generic range checks. linux2 Profile_002 Savegame_003 replay-010 and both task-36 controls match exact EOF.
- #167 completed: tower-alert fallback selection now applies Original's hearing count to the complete stable camp-registry prefix, including the tower owner, while normal hearing eligibility remains self-excluding. Squared distances also truncate to ULONG before comparison. Focused owner-prefix and truncation regressions pass; both task-36 controls remain exact EOF.
- #165 completed: DropAle DONE now survives the Actor::Hourglass completion seam, and anti-collision snapshots include live objects in engine-slot order like Original's all-element scan. SuN1Sh1nE Profile_004 Savegame_028 replay-004 clears frame 1543 and advances to an independent frame-1738 direction frontier; both task-36 controls remain exact EOF.
- #170 completed: `CanEnterSwordfightWith` no longer treats an active door pass through ordinary sectors as being inside a building. linux2 Profile_002 Savegame_003 replay-013 clears frame 14874 and advances to an independent frame-15126 RNG frontier; both task-36 controls remain exact EOF.
- #169 completed: smalltalk DONE back-hit classification now builds its relative vector from Original's ground positions rather than elevation-projected map positions, while retaining the isometric facing vector. linux2 Profile_002 Savegame_029 replay-014 and both task-36 controls match exact EOF.
- #171 completed: generic human/PC `StandingUpSword` now refreshes its direction goal from the live principal opponent after sprite playback and then turns unconditionally, while the Soldier override remains sprite-only. SuN1Sh1nE Profile_004 Savegame_028 replay-004 and both task-36 controls match exact EOF.
- #172 completed: low-level `GoTo` now honors replayed `GOTO_NEAR` flags with Original's same-layer squared-distance early exit and stored stop tolerance. linux2 Profile_002 Savegame_003 replay-013 and both task-36 controls match exact EOF.
- #173 completed: non-soldier forced sword movement with no live opponent now selects `WalkingSword` directly instead of encoding a fake self-facing target that can become a tiny strafe vector. linux2 Profile_002 Savegame_008 replay-014 and both task-36 controls match exact EOF.
- #175 completed: archery friendly-fire checks now use Original's determinant/dot `SBGeoVector2D::Angle`, including PI for a coincident zero vector instead of Rust's `atan2(0,0)=0`. linux2 Profile_002 Savegame_010 replay-003 and both task-36 controls match exact EOF.
- #168 completed: `ReconsiderSwordfight` now evaluates its facing guard from Original's ground positions rather than elevation-projected map positions. linux2 Profile_002 Savegame_001 replay-001 and both task-36 controls match exact EOF.
- #174 completed: phalanx 180-degree detection now performs Original's cached `ComputeViewRadius` boundary before its final target LOS, restoring ordered night/fog light-sector barycentre queries. linux2 Profile_002 Savegame_030 replay-005 clears frame 6153 and advances to an independent frame-6314 projectile topology frontier; both task-36 controls remain exact EOF.
- #176 completed: sword movement now retains a live opponent's literal ground-space facing vector when the fighters are co-located. Original's zero-vector angle selects `WalkingBackwardsSword`; Rust no longer substitutes the actor heading and selects a strafe. linux2 Profile_002 Savegame_010 replay-014 and both task-36 controls match exact EOF.
- #178 completed: projectile trajectory results now distinguish an exact bare-ground impact from an unknown obstacle. Null-obstacle impacts use Original's layer-zero-only sector search instead of adopting overlapping upper-layer projection membership. linux2 Profile_002 Savegame_030 replay-005 clears frame 6314 and advances to an independent frame-6316 projectile-active frontier; both task-36 controls remain exact EOF.
- #180 completed: successful projectile human/FX/shield hits now return before applying an unused future trajectory's disappearance flag, preserving Original's stopped active frame until `Arrow::Refresh`. linux2 Profile_002 Savegame_030 replay-005 and both task-36 controls match exact EOF.
- #179 completed after correcting the invalid first attempt: the actual PC Execute completion seam now synchronizes the carried body's terminal drop frame, computes outdoor placement from the live animation hotspot, queues Original's delayed outdoor position, unlinks the pair, and only then advances to the following order. linux2 Profile_002 Savegame_012 replay-013 and both task-36 controls match exact EOF under ordinary fail-fast validation.
- #181 completed: hits on an already-dead actor retain authored protection/concussion work but now stop at Original's `SetLifePoints` dead guard instead of repeating the virtual `Kill` cascade. linux2 Profile_002 Savegame_017 replay-013 clears frame 587 and advances to an independent frame-598 path-tolerance frontier; both task-36 controls remain exact EOF.
- #183 completed: shoulder-damage translation now leaves `HelpingToClimb`/shoulder posture intact until `FallingBackUpright` executes on the actor's next slot. linux2 Profile_002 Savegame_034 replay-001 clears frame 34733 and advances to an independent frame-34735 damage/parry animation frontier; both task-36 controls remain exact EOF.
- #184 completed: moving-fast `EVENT_VIEW` now computes `GoNear` tolerance from Original's stretched 3D world-position distance, including elevation, instead of raw map X/Y. linux2 Profile_002 Savegame_017 replay-013 clears frame 598 and advances to an independent frame-1440 AI/turn frontier; both task-36 controls remain exact EOF.
- #177 completed: `SeekingOfficerWaitForInstructedSoldier` no longer invents a 30-frame timer relaunch after a missed soldier. This removes the repeated officer detection chain and its extra night/fog rays. linux2 Profile_002 Savegame_034 replay-001 clears frame 34552 and advances to an independent frame-34733 PC posture frontier; both task-36 controls remain exact EOF.
- #186 completed: AI `GoTo` now projects a resolved movement-to-wait sprite action that has reached its authored action-done boundary as already idle, matching Original's Actor::Hourglass retirement before timer-driven BattleDecisions. linux2 Profile_002 Savegame_017 replay-013 clears frame 1440 and matches exact EOF; both task-36 controls remain exact EOF.
- #185 completed: parried authored push strikes now reach Original's TranslatePushDamage path, while ReadyForTakeOff is deferred to the falling-pushed order's first Execute and samples live geometry. The Start edge preserves zero displacement and obstacle installation preserves material. linux2 Profile_002 Savegame_034 replay-001 clears the former frame-34731 through frame-34752 push-flight frontiers and advances to an independent sound FIFO frontier; both task-36 controls remain exact EOF.
- #187 completed: EVENT_VIEW distance gates and AttackingReactiontimeEnemyNearTest now use literal live actor positions during door passage, while forecast positions remain limited to the Original AI Position consumers. linux2 Profile_002 Savegame_034 replay-011 clears both door-pass swordfight-entry frontiers and matches exact EOF; both task-36 controls remain exact EOF.
- #188 completed: push SayOuch and sword-damage combat informs now follow Original virtual dispatch and translation gates, while PC hurt speech compares actual post-protection life loss at SetLifePoints semantics. linux2 Profile_002 Savegame_034 replay-001 clears frame 34770 and advances to an independent frame-34806 movement frontier; both task-36 controls remain exact EOF.
- #189 completed: direct recursive BreakPhalanx now seeds and commits the shared view-radius cache like typed Think dispatch, preserving Soldier53's computed ground radius instead of repeating two night/fog light rays. linux3 Profile_001 Savegame_024 replay-008 clears frame 8921 and advances to an independent frame-9629 attentive-state frontier; both task-36 controls remain exact EOF.
- #191 completed: pushed flights now apply their first displacement on the falling order's Start Execute, matching Original PerformFlight rather than lagging one owner slot. linux2 Profile_002 Savegame_034 replay-001 clears frame 34806 and advances to a small terminal movement residue at frame 34823; both task-36 controls remain exact EOF.
- #192 completed: SeekingBody EventReachPoint now restores Original's distance, rider/dead, tied/unconscious, and nearby-recovered target branches. linux3 Profile_001 Savegame_024 replay-008 clears frame 9629 and matches exact EOF; both task-36 controls remain exact EOF.
- #193 completed: the bow fighter running behind a shield bearer now faces its primary target through Original's elevation-aware entity overload. linux2 Profile_002 Savegame_035 replay-015 clears frame 38574 and advances to the separately tracked Rolling family; both task-36 controls remain exact EOF.
- #190 completed: anti-collision restoration now occurs at authored landing retirement for up/long/sword-long jumps only; down-jump landing and its trailing crouch-up preserve Original's disabled state. linux2 Profile_002 Savegame_003 replay-012 clears frame 15186 and matches exact EOF; both task-36 controls remain exact EOF.
- #194 completed: Rolling now executes owner-locally through PerformMotion WALK anti-collision, using initialization-before-row ordering, the live primary move box, and live order/map-goal retargeting instead of synthetic flight. Savegame_032 replay-002 clears frame 17463 and Savegame_035 replay-015 clears frame 38677; both task-36 controls remain exact EOF.
- #195 completed: completed zero-frame pushed flights now terminal-snap after their falling order retires, preserving Original's tiny near-goal movement residue. linux2 Profile_002 Savegame_034 replay-001 clears frame 34823 and advances to an independent RNG frontier; both task-36 controls remain exact EOF.
- #196 completed: UpdateRoll rejection now materializes Original's lazy zero map increment after setting the goal to the current position, rather than exposing stale raw storage. linux2 Profile_002 Savegame_032 replay-002 clears frame 17471 and advances to a Rolling retirement snap frontier; both task-36 controls remain exact EOF.
- #198 completed: RunningToPhalanx EventDone now preserves an existing soldier neighbour's null primary target and enters BattleDecisions instead of selecting a replacement target. linux2 Profile_002 Savegame_035 replay-015 clears frame 38768 and advances to an independent RNG frontier; both task-36 controls remain exact EOF.
- #199 completed: Rolling arrival now snaps to PositionInterface's live map goal rather than the stale authored order destination, preserving an UpdateRoll stop-at-current-position rewrite. linux2 Profile_002 Savegame_032 replay-002 clears frame 17473 and advances to an independent swordfight/path frontier; both task-36 controls remain exact EOF.
- #197 completed: AlertOfficer now selects candidates with Original's truncated stretched 3D world MaxNormDistance and successful LookForHelp resumes its remark only after synchronous GoNear route construction. linux2 Profile_002 Savegame_034 replay-001 clears frame 34827 and matches exact EOF; both task-36 controls remain exact EOF.
- #202 completed: door-pass movement selection now preserves a stamped Crouched posture before authored-action normalization, so direct stairs passes retain WalkingCrouched. linux3 Profile_001 Savegame_010 replay-011 clears frame 32908 and advances to an independent AI/path frontier; both task-36 controls remain exact EOF.
- #203 completed: reciprocal phalanx-neighbour updates now drain synchronously after each AI Think, matching Original's immediate four-step `UpdateLeftCombatNeighbour`/`UpdateRightCombatNeighbour` mutations. linux2 Profile_002 Savegame_034 replay-009 clears its frame-34182 extra `PhalanxAdvance` draw and advances 111 frames to an independent Soldier53/Soldier58 movement frontier after frame 34293; both task-36 controls remain exact EOF.
- #204 completed: `ReconsiderSwordfight` now performs its facing guard against the refreshed opponent's literal live ground position, using the live snapshot only when its target identity still matches. Nescafe Profile_003 Savegame_001 replay-012 and both task-36 controls match exact EOF.
- #205 completed: ordinary cross-gate swordfight interactions now retain Original's entity Seek lowering, including the 25-tick refresh, victim propagation through gate approaches, and actor-owned post-seek `EnterSwordfight`, without group-move arrival extras. linux3 Profile_001 Savegame_010 replay-011 clears frame 33298 and advances 171 frames to an independent frame-33469 animation frontier; both task-36 controls remain exact EOF.
- #206 completed: `ReconsiderPhalanx` now uses Original's aspect-aware vector normalization and normal construction for its forward/right formation advance. linux2 Profile_002 Savegame_034 replay-009 clears frame 34293 and advances 183 frames to an independent visibility-query-only frontier after frame 34476; both task-36 controls remain exact EOF.
- #207 completed: in-place Make* posture rewrites now update the matching detached installed-order action immediately, mirroring Original's shared `mpOrder` object while retaining order-identity safety. linux3 Profile_001 Savegame_010 replay-011 and both task-36 controls match exact EOF.
- #209 completed as already exact: all four reported schema-14 controls match exact EOF at 1500/1500 frames on commit `07daeee8d`; no production change was needed.
- #210 completed: direct reciprocal combat-neighbour setters now drain after each registry-ordered owner slot. Soldier52 clears Soldier53's stale left link before Soldier53 executes, restoring the missing PhalanxReinitializeThemList visibility query. Both task-36 controls match exact EOF; linux2 Profile_002 Savegame_034 replay-009 advances to the independent frame-34689 Projectile177 movement frontier.
- #211 completed: SeekingBody EVENT_REACHPOINT no longer invents a Face operation, preserving Original's live facing. Nescafe Profile_003 Restart replay-014 and both task-36 controls match exact EOF.
- #212 completed: BattleDecisions reserve selection now uses Original's stretched 3D position distance with Z and ULONG truncation. linux3 Profile_003 Savegame_010 replay-003 clears frame 19649 and advances to an independent frame-19740 detectable-list frontier; both task-36 controls remain exact EOF.
- #213 completed: a selected door pass now rebinds its mirrored action to the concrete distance-motion successor after a speed transition retires. Nescafe Profile_001 Savegame_001 replay-003 clears frame 1090 and advances to the independent frame-1636 Wait-to-HitCmd handoff.
- #214 completed: an otherwise lethal sword hit on an already-comatose PC is ignored at Original's GetWounded boundary, while sublethal coma damage and all protection/result behavior remain intact. SuN1Sh1nE Profile_004 ExQuickSave replay-009 clears frame 35951 and matches exact EOF.
- #215 completed: Original's literal 16-direction float tables and plane-evaluated solid-obstacle impact Z replace rounded-angle trigonometry and parametric Z reconstruction. linux2 Profile_002 Savegame_034 replay-009 clears Projectile177 at frame 34689 and advances to an independent Soldier81 frontier at frame 34820; both task-36 controls remain exact EOF.
- #216 completed: terminal copied stop transitions now retain actor-owned entity-seek identity for post-seek Hit validity, preserving Original's ground-facing side effect and suppressing an out-of-range Hit before it becomes visible. Nescafe Profile_001 Savegame_001 replay-003 matches exact EOF; Profile_003 replay-008 advances from frame 353 to an independent frame-1427 AI frontier; both task-36 controls remain exact EOF.
- #217 completed: instructed-soldier EVENT_MYTALK_2 now snapshots the officer's live detected body before CALL_YOURTALK_2 and appends it through the existing detectable drain. linux3 Profile_003 Savegame_010 replay-003 clears Soldier89's frame-19740 missing body and matches exact EOF; both task-36 controls remain exact EOF.
- #218 completed: retained civilian stimuli drained during EVENT_AFTER_SCRIPT_GO_ON now rebuild their human-specific antagonist context. linux3 Profile_001 Savegame_008 replay-015 clears frame 4056 and matches exact EOF; both task-36 controls remain exact EOF.
- #219 completed: the eight non-hard PerformFlight falling orders now disable anti-collision and clear deviation on START, then restore anti-collision on TERMINATED. SuN1Sh1nE Profile_004 Savegame_038 replay-005 clears PC342's frame-290 direction frontier and matches exact EOF; both task-36 controls remain exact EOF.
- #220 completed: return-to-duty restores Enemy SetState's leaving-Menacing reciprocal guarded-PC cleanup before later NPC owner slots execute. linux2 Profile_002 Savegame_018 replay-003 clears its frame-8883 missing blocked LOS query and matches exact EOF; both task-36 controls remain exact EOF.
- #221 completed: schema adoption refreshes DoorSeekInfo NPC-villain authorization from each restored canonical door. linux2 Profile_002 Savegame_034 replay-009 clears Soldier81's frame-34820 locked-building route and advances to independent visibility geometry at frame 34860; both task-36 controls remain exact EOF.
- #222 completed: explicit QuitSwordfight dispatch no longer rewrites an independently cross-postponed sword movement; only EvaluateOpponents mutates a live movement element as Original does. linux3 Profile_002 Savegame_000 replay-003 clears PC126's frame-905 animation frontier and matches exact EOF; both task-36 controls remain exact EOF.
- #223 completed: RECEIVE_HIT_DAMAGE sends EVENT_GOTHIT only for conscious NPCs, matching Original's IsNPC gate. SuN1Sh1nE Profile_004 Savegame_032 replay-007 clears the frame-333 Little John AI-controller panic and advances to an independent frame-370 sound FIFO frontier; both task-36 controls remain exact EOF.
- #227 completed: a PC pinch abort cancels the matching terminal order-pop queued by nested PerformMotion before marking the entry-latched element Impossible. linux3 Profile_001 Savegame_022 replay-012 and both task-36 controls match exact EOF.
- #228 completed: ordinary SeekNextPoint preserves the semantic noise/search center instead of replacing it with the current route point; the authored beggar special case remains intact. Nescafe Profile_002 Savegame_001 replay-010 and both task-36 controls match exact EOF.
- #229 completed: projectile impact stimuli retain Original's full saved trajectory-origin Position, including sector and layer, instead of fabricating position metadata from the victim. linux3 Profile_001 Savegame_024 replay-009 clears frame 8578 and advances to an independent frame-9768 macro-state frontier; both task-36 controls remain exact EOF.
- #231 completed: InitializePatrol uses literal stored 3D actor positions for its admission ray while retaining gate-side AI positions for sorting and formation. linux2 Profile_002 Savegame_034 replay-009 clears frame 34860 and advances to an independent frame-34864 visibility frontier; both task-36 controls remain exact EOF.
- #232 completed: resumed PC movement normalizes stale WalkingWithSword/RunningWithSword tokens to upright walk/run after QuitSwordfight lowered the weapon, matching Original's base waiting-state movement animation. The representative Nescafe Profile_002 Savegame_016 replay-006 and both task-36 controls match exact EOF.
- #233 completed: the soldier-from-officer CALL_ALERT arm queues actor Halt without AI StopAll, preserving Original's active macro and timer. linux3 Profile_001 Savegame_024 replay-009 and both task-36 controls match exact EOF.
- #230 completed: reactive parade/counterstrike evaluation uses Original's virtual difficulty-adjusted soldier fighting ability. SuN1Sh1nE Profile_004 Savegame_024 replay-013 clears frame 139 and advances to independent frame-169 visibility geometry; both task-36 controls remain exact EOF.
- #234 completed: RefreshPatrol missed-member visibility uses literal actor endpoints instead of gate-side AI positions. linux2 Profile_002 Savegame_034 replay-009 clears frame 34864 and advances to an independent frame-34886 turn lifecycle frontier; both task-36 controls remain exact EOF.
- #235 completed: TranslateHitDamage suppresses the inherited-empty PC SayOuch call, removing an invented HERO_HURT FIFO entry when no life points changed. SuN1Sh1nE Profile_004 Savegame_032 replay-007 clears frame 371 and advances to an independent frame-536 AI frontier; both task-36 controls remain exact EOF.
- #237 completed: terminal combat flights snap to PositionGoal at the actor owner boundary before the NPC derived tail. SuN1Sh1nE Profile_004 Savegame_024 replay-013 clears frame 169 and advances to an independent frame-209 RNG frontier; both task-36 controls remain exact EOF.
- #239 completed: rolling termination commits Dead/Lying posture before postponed movement resumes. SuN1Sh1nE Profile_004 Savegame_002 replay-012 and both task-36 controls match exact EOF.
- #236 completed: DefaultGotoChief arrival faces the live chief entity with Original's elevation-aware overload, restoring the postponed Turn. linux2 Profile_002 Savegame_034 replay-009 and both task-36 controls match exact EOF.
- #240 completed: reactive counterstrikes use the full StopAll/Halt barrier so an interrupted old parry condolence cannot reset the new SpecialStrike state. SuN1Sh1nE Profile_004 Savegame_032 replay-007 clears frame 536 and advances to an independent frame-760 direction frontier; both task-36 controls remain exact EOF.
- #238 completed: sword-state PassDoor translation preserves authored running speed instead of consulting the door element's empty FAST flags. SuN1Sh1nE Profile_004 QuickSave replay-009 clears frame 36381 and advances to an independent frame-36915 position-goal frontier; both task-36 controls remain exact EOF.
- #241 completed: cross-sequence postponed ShootBow re-instruction clears Rust's execution-only ActiveShot latch only for the exact same sequence element, matching Original's sequence-owned restart and preventing the resumed shot from rejecting itself as concurrent. SuN1Sh1nE Profile_004 Savegame_024 replay-013 clears the frame-209 extra BattleCourage draw and advances to an independent frame-499 detection-alert frontier; both task-36 controls remain exact EOF.
- #243 completed: replacement circle strikes preserve an interrupted strike's retained geometry until their own exact action point, and true-circle hold uses Original's exact order/frame/counter predicate rather than a one-tick-early forecast. SuN1Sh1nE Profile_004 Savegame_032 replay-007 clears Soldier197's frame-760 direction and frame-771 motion timing frontiers and advances to an independent Soldier198 frame-776 movement frontier; both task-36 controls remain exact EOF.
- #244 completed without a Rust change: all 34 identified LowerBow-to-Wait captures were produced by historical Original undefined behavior that read an uninitialized `ShootType` after an out-of-range result. Independent per-trace audits reconstructed or diagnostically observed each target classification and proved every result out of range before the undefined read. Current Original and Rust correctly reject those invalid classifications, so the full identified cohort is retired pending deterministic Original recapture.
- #245 completed: damage-element NPC death preserves Original's split forced-attentive alert semantics: music drops to Green with the instant-change flag, while a forced-attentive soldier's view remains Yellow. SuN1Sh1nE Profile_004 Savegame_024 replay-013 clears Soldier52's frame-499 detection-alert frontier and advances to independent RNG work after frame 517; both task-36 controls remain exact EOF.
- #246 completed: the periodic reachpoint stuck detector monitors Wait plus all four smalltalk strike/parry commands without treating smalltalk as idle elsewhere. SuN1Sh1nE Profile_004 Savegame_032 replay-007 clears Soldier198's frame-776 missing MoveOk relaunch and matches exact EOF; both task-36 controls remain exact EOF.
- #247 completed: recomputed falling-arrow trajectories preserve Original mbDisappear and preflag one-waypoint HOLE endpoints before the optional far-edge extension; disappearing projectiles bypass the landing snap, while non-falling endpoint arrows remain active for Hourglass. SuN1Sh1nE Profile_004 Savegame_017 replay-009 matches exact EOF, and Savegame_024 replay-013 clears the combined frame-137 retirement regression; both task-36 controls remain exact EOF.
- #249 completed: LinkSource again requires Original's source-to-docking IsReachableGrid check and no longer retries authorized narrow-pocket sources with collision checks disabled. Nescafe Profile_001 Restart replay-005 clears Soldier205's frame-792 invented path and advances to independent frame-892 SeekPointSelection RNG work; both task-36 controls remain exact EOF.
- #248 completed: lethal Human WAIT translation preserves the stamped Upright posture when its emergency-fall arm delegates the follow-up to the base Actor translator, and dead/unconscious actors execute their selected current order without Rust's source-invented allowlist. linux2 Profile_002 ExQuickSave replay-009 clears the connected frame-941/958/959 animation chain and advances to an independent Civilian68 AI frontier after frame 1059; both task-36 controls remain exact EOF.
- #251 completed: IsDetecting180Degrees checks the target's raw active flag rather than whether it can fight, allowing an active unconscious target to reach Original's geometry and LOS path. SuN1Sh1nE Profile_004 Savegame_016 replay-002 clears frame 1225 and advances to an independent PC132 EatCmd/Wait frontier after frame 1522; both task-36 controls remain exact EOF.
- #252 completed: active-ability owner selection matches self-heal's canonical Healing order; Original's Eating substitution remains an execution-time sprite choice rather than a translated sequence order. SuN1Sh1nE Profile_004 Savegame_013 replay-003 and both task-36 controls match exact EOF.
- #250 completed: ClearPatrol/RemoveAllSubordinates directly invokes each member's virtual ReturnToDuty, matching Original ForceReturnToDuty and bypassing StartThink's script-lock refusal. linux2 Profile_002 ExQuickSave replay-009 and both task-36 controls match exact EOF.
- #254 completed: postponing a selected ability clears only its exact sequence/element-owned Rust mirror so resumed translation can install fresh orders. SuN1Sh1nE Profile_004 Savegame_016 replay-002 clears the frame-1522 Eat/Wait frontier and advances to an independent Soldier91 turn frontier after frame 1529; both task-36 controls remain exact EOF.
- #253 completed: RunningUpright stamps Upright/MovingFast for every PerformSeek/PerformMotion result, matching Original's unconditional SetStates before its motion-state switch. SuN1Sh1nE Profile_004 Savegame_016 replay-004 clears frame 1364 and advances to an independent Soldier92 movement frontier after frame 1501; both task-36 controls remain exact EOF.
- #255 completed: one-point patrol paths store initial facing through Original's unit-vector then ASPECT_RATIO re-binning round-trip, so sector 3 correctly returns as sector 2 and authors the required post Turn. linux3 Profile_003 Savegame_009 replay-010 clears its frame-18346 extra bored RNG draws and advances to an independent visibility-query frontier after frame 18347; both task-36 controls remain exact EOF.
- #258 completed without a Rust change: Cyrdach Profile_156 Savegame_023 replay-004 omits the same source-required deployment-zone entry as Task73 despite byte-identical movement endpoints and exact FLOAT crossing determinants. The stale trace is retired pending deterministic Original recapture.
- #256 completed: pending-Halt GoTo projection preserves the exact selected default Wait element that Original Stop skips, using required live sequence metadata across filtered and direct AI callbacks. SuN1Sh1nE Profile_004 Savegame_016 replay-002 and both task-36 controls match exact EOF.
- #257 completed: the shared one-point route branch dispatches ReturnToDuty virtually for soldiers, synchronously running Enemy InitializePatrol and its resume before later caller work. linux3 Profile_003 Savegame_009 replay-010 and both task-36 controls match exact EOF.
- #259 completed: identity-matched terminal Tie validates Original's raw 40-unit map boundary before facing, while terminal Hit retains its distinct face-before-abort behavior. linux3 Profile_001 Savegame_035 replay-015 matches exact EOF.
- #260 completed: subordinate patrol delegation now calls the live chief DispatchStimulusToWholePatrol synchronously and uses its actual boolean result, so an empty chief returns control to the subordinate's local handler.
- #261 completed: PushAside uses only Original's authored rectangular victim scan and no longer recovers an out-of-half-width antagonist radially. SuN1Sh1nE Profile_004 Savegame_024 replay-012 clears frame 325 and advances to an independent detection frontier after frame 753.
- #262 completed: a redundant EnterAttentiveMode that terminates during Soldier::Translate skips Actor::Instruct's accepted motion/order epilogue, preserving the terminal motion edge.
- #263 completed: BeingWeakSword/BeingStunnedSword initialization synchronously closes each soldier opponent's EVENT_ADVERSARY_WEAK Think while preserving older deferred detection FIFO entries.
- #264 completed: a successful patrol-chief dispatch closes the complete direct actor owner boundary before returning. SuN1Sh1nE Profile_004 Savegame_016 replay-011 clears the Task260-exposed frame-373 animation regression and advances to an independent RNG frontier after frame 1379.
- #266 completed: StopMovement's in-place NewID reseed preserves Start/InProgress/Done rather than inventing successor advancement. nicouzouf Profile_001 Savegame_049 replay-009 clears frame 329 and advances to an independent PC127 motion frontier after frame 901.
- #267 completed: terminal generated PC stop-transition cleanup preserves a resolved external orientation goal while clearing movement ownership. nicouzouf Profile_001 Savegame_047 replay-003 matches exact EOF.
- #265 completed without a Rust change: SuN1Sh1nE Profile_004 Savegame_024 replay-012's frame-753 follow cone contradicts the restored Original source and August 9 binary, both of which immediately focus S111 and reject S112 exactly as Rust does. The August 1 trace is retired pending deterministic current-Original recapture.
- #268 completed: genuine lateral/circle strike termination clears both the executable sweep and the serialized human victim list, preventing a fresh strike from rehydrating stale victims before its action point. SuN1Sh1nE Profile_004 Savegame_016 replay-011 clears the frame-1379 extra damage/provoke RNG triplet and matches exact EOF.
- #269 completed: ReceiveSwordDamage on an already grounded posture terminates while still selected, preserving the current Execute motion edge and Original's dead-rider exception instead of entering the accepted-empty stamp path. nicouzouf Profile_001 Savegame_049 replay-009 clears PC127's frame-901 Start/InProgress frontier and matches exact EOF.
- #270 completed: pending Halt(PREFERENCE) projection consults required live selected-element priority and exposes idle only when Original Stop can interrupt it. nicouzouf Profile_001 Savegame_039 replay-006 clears the frame-221 Injury/ExtractingArrowBow shortcut regression and advances to an independent frame-252 smalltalk frontier; two sibling controls clear the same family.
- #271 completed: a point seek launches its retained post-seek sequence only when the actor actually reaches the authored seek sector. A Stop transition ending short no longer starts stale attached work; the representative matches exact EOF.
- #273 completed: a moving-fast `EVENT_VIEW` tail resumed after synchronous `HeyFolksLookThere` retains its logical enclosing `Think` through the engine-owned `GoNear` route drain. The same inaccessible cross-topology route now raises `EVENT_COULDNT_REACHPOINT` at Original's EndThink boundary instead of discarding the deferred failure. SuN1Sh1nE Profile_004 Savegame_034 replay-002 clears both soldiers' frame-830 reaction-running frontier and advances to an independent PC motion-state mismatch after frame 875.
- #274 completed: `AttackingApproachingNewEnemy` now applies Original's stretched 3D `SquareDistance` to literal live positions while retaining the target's planning position for `GoNear`. The frame-113 frontier clears and advances to an independent frame-117 mismatch.
- #277 completed: ordinary/high bow-release initialization validity now runs for every human owner at the exact selected-order Execute boundary, and a failed guard early-returns from that owner slot before any stale specialized or generic Execute driver can run. Only a shot actually made Impossible loses its matching active-shot driver; held shoot-list entries also follow Original's post-transition terminal-state rejection and priority ordering. The frame-529 bow frontier clears and advances to an independent frame-1038 mismatch, while the full-health Eat regression at frame 274 also clears.
- #278 completed: retained shield geometry is refreshed only at Original's explicit `UpdateShield` sites, and `RHSightObstacle::UpdateBox` normalizes the already aspect-compressed facing vector before deriving its perpendicular. nicouzouf Profile_001 Savegame_020 replay-006 clears the frame-584 invented shield parry and advances to an unrelated projectile frontier after frame 587.
- #282 completed: reciprocal swordfight re-entry recomputes relative fighting ability only for a side whose opponent list actually gained an entry, preserving the already-engaged opponent's cached strength. SuN1Sh1nE Profile_004 Savegame_036 replay-005 matches exact EOF on merged commit `ca0803f61`.
- #283 completed: active-shot PC equip START restores the Bow action through Original's synchronous selection callback. Targeted non-NoAction messages first collapse a multi-selection to their addressed PC, preserving the other PC's retained shot and the exact restitution/action ordering. The former frame-723/panic family clears and advances to an independent frame-948 mismatch.
- #284 completed: shield movement selection distinguishes the PC stamped-state override from the base actor's live action-state rewrite, and authors the logical shield movement without an invented sprite/lift gate. The frame-702 frontier clears and advances to an independent frame-1946 mismatch.
- #285 completed: `SetAlwaysAttentive` promotes a green live view alert even when the independent music alert is already yellow, and treats a resolved soldier without an actor entity as invalid state. The representative clears its target and advances to an independent frame-784 mismatch.
- #286 completed: ladder-fall translation retains the existing layer goal while authoring its landing target, matching Original's separation of topology and movement goals. The frame-349 frontier clears, and the representative matches exact EOF on merged commit `ca0803f61`.
- #287 completed: an otherwise-lethal piercing hit against a PC already in an amulet coma preserves life and coma instead of repeating the wound/death path, while concussion work and sublethal damage retain their ordinary behavior. The frame-714 coma/lethal frontier clears and advances to an independent Projectile143 active-only mismatch after frame 720.
- #288 completed: blocked terminal sword movement becomes Impossible without inventing a `WaitingSword` action-state transition; Original's unhandled ABORTED sword-movement arm retains the live moving-sword state. The representative matches exact EOF.
- #289 completed: water/hole resolution is scoped to the exact terminal obstacle, bare ground alone searches global material sectors, and a target-hit arrow retains its stopped snapshot until the following refresh. Focused raised-platform, exact-hole, and leftover-countdown controls pass; the representative matches exact EOF.
- #290 completed: `FleeingHiding` handles `EVENT_TIMER` through the enemy virtual `ReturnToDuty` path instead of the shared base expected-event handler. The frame-1234 frontier clears; nicouzouf Profile_001 Savegame_022 replays 009 and 015 both match exact EOF on current validation.
- #291 completed: recorded interactions strictly preflight their actor and target identities, store the replacement quick-action titbit with its running state, stop recording, and do not also launch the interaction live. The frame-448 frontier clears, and the representative matches exact EOF on merged commit `ca0803f61`.
- #292 completed: ordered `ActorEffects` prefixes now isolate caller-tail self stimuli and cross-NPC actions while recursively settling earlier actor work. This preserves Original's `Focus(NULL) → Halt → EventReachPoint → Halt → WakeUp` ordering instead of allowing the event to register WakeUp before the restored older Halt interrupts it. nicouzouf Profile_001 Savegame_051 replay-015 clears the frame-1050 WakeUp frontier and advances to an independent Soldier59 attentive-mode frontier after frame 1067; the focused owner-FIFO regression passes.
- #293 completed without a Rust change: nicouzouf Profile_001 Savegame_065 replay-008 records Projectile98 on layer/sector 0 after frame 674, but its exact terminal landing footprint `(647,509)` lies inside both Croisement01's current layer-0 area/sector 0 and non-area motion obstacle index 39/sector 40. Current Original `ComputeTrajectory` clears the sector and returns on that obstacle before assigning a layer, yielding layer `0xFFFF`/sector null exactly as Rust does. This fourth member of the Task89 source/assets-inconsistent projectile-membership family is retired pending deterministic current-Original recapture.
- #295 completed: push-aside strike victim geometry now starts from Original's literal 16-sector `GetDirectionVector` table, preserving its ordinary `ASPECT_RATIO` Y scale without applying the angular helper's +0.1-radian sector-roundtrip nudge. The nudge had rotated Soldier110's narrow strike-A rectangle enough to admit a Royalist just outside its side boundary and reject the proposal as friendly fire. SuN1Sh1nE Profile_004 Savegame_024 replay-010 clears the frame-353 special-strike frontier and advances to an independent strike-completion/opponent frontier after frame 373; the focused boundary regression passes.
- #296 completed: Healing revalidates Original's strict target identity, type, state, and 40-unit range in the DONE arm immediately before applying healing or FX and consuming a plant. The representative matches exact EOF.
- #297 completed without a Rust change: SuN1Sh1nE Profile_004 Savegame_036 replay-004 first diverges after frame 1062 only because fresh Bonus321 exposes Original's uninitialized `mpointOldPosition.mZ` (`0x6b0c9c51`) while Rust safely initializes old position to zero. The trace is retired pending deterministic Original initialization and recapture.
- #299 completed: a redundant `RaiseShield` that terminates during Human translation exposes an owner-action Skip barrier, preserving the preceding Execute motion edge rather than publishing an accepted-Instruct stamp. The representative matches exact EOF.
- #302 completed: ordinary `RaiseShield` no longer eagerly stamps an actor Upright; an authored stand-up transition owns that posture change, while the instant-raise path remains immediate. The representative matches exact EOF.
- #303 completed: `BattleDecisions` derives `bAlertingSoldierNear` from the friends already admitted to Original's `mlistUs` instead of repeating their 360-degree visibility queries in the LookForHelp arm. nicouzouf Profile_001 Savegame_041 replay-015 clears the frame-1038 visibility-query frontier and advances to an independent PC HitCmd/MoveOk frontier after frame 1078; both Task 36 schema-14 controls remain exact EOF.
- #304 completed: falling-arrow endpoint retirement now follows Original's literal 3D old/current-position comparison instead of the separately serialized sprite-position caches, preserving a live endpoint through Refresh until the following Projectile Hourglass stops flight and applies its ordinary landing state. nicouzouf Profile_001 Savegame_020 replay-006 clears the frame-587 Projectile107 active/elevation/movement frontier and advances to an independent Soldier72 attentive-mode frontier after frame 1278; SuN1Sh1nE Profile_004 Savegame_016 replay-006 clears the frame-720 Projectile143 active frontier and matches exact EOF. The focused endpoint regression and parity-profile build pass.
- #305 completed: straight sword-strike DONE range checks now use Original's literal stored 3D `GetPosition` norm instead of projected map distance. linux3 Profile_003 Savegame_046 replay-002 clears PC87's frame-948 false `ReceiveSwordDamage` interruption and advances to an independent sword-damage/provoke RNG frontier after frame 1444; the focused cross-elevation regression passes.
- #306 completed: elevated-obstacle projectile intersection preserves Original `IsReachableImpactBounce`'s shipped bottom-plane denominator typo, mixing the origin's top-relative height with the destination's bottom-relative height only on the nonvertical underside-crossing path. linux3 Profile_003 Savegame_046 replay-008 clears Projectile92's frame-1946 trajectory frontier and matches exact EOF.
- #307 completed: NPC `IsLastRealAction` now follows Original's raw sequence following pointer without requiring the next element to have the same owner. A manager-owned Timer following Soldier45's completed `MoveOk` therefore suppresses its condolence `EVENT_REACHPOINT`, preserving `DefaultGotoRoute` instead of entering a spurious route turn. nicouzouf Profile_001 Savegame_065 replay-015 clears the frame-784 state/action/direction frontier and advances to an independent Soldier55 active-only mismatch after frame 836.
- #308 completed: completed `RHMOVE_MAP` cleanup classifies the actor against the inclusive authored map bounding box rather than the fast grid's four loader-padding rows. nicouzouf Profile_001 Savegame_065 replay-015 clears Soldier55's frame-836 active-state frontier and remains exact through frame 1283, where it reaches an independent sound-manager pending-request panic; the authored-edge/padded-row regression passes.
- #309 completed: a final jump landing retains `Terminated` when `Proceed` returns no next order, while a genuinely installed synchronous successor still projects `InProgress`. SuN1Sh1nE Profile_004 Savegame_034 replay-002 clears PC241's frame-875 motion-state frontier and matches exact EOF; all three focused jump-landing controls pass.
- #310 completed: `ForgetAttentiveMode` preserves the script-owned forced-attentive latch and, after `SetState`'s earlier attentive transition launch, applies its later attentive/will-be-attentive clears in Original statement order. nicouzouf Profile_001 Savegame_051 replay-015 clears Soldier59's frame-1067 frontier and advances to an independent RNG frontier after frame 1182; Savegame_020 replay-006 clears Soldier72's downstream frame-1278 frontier and matches exact EOF.
- #311 completed: an already-swordfighting `EVENT_GOTHIT` uses Original's direct `EnterSwordFight(attacker, false)` boundary, immediately adds the new principal relationship, and authors the reciprocal attacker-owned Enter transition instead of deferring a victim-owned Engage request. SuN1Sh1nE Profile_004 Savegame_024 replay-010 clears the frame-373 strike/opponent frontier and advances through the related frame-377 initiative ordering to an independent projectile-damage frontier after frame 431.
- #314 completed: PC swordfight entry always delivers Original's targeted `MSG_SELECT_ACTION(NO_ACTION)` before `DisableAllActionsTemp`, including when another PC is selected, so the temporary action snapshot cannot retain and restore a pre-fight Bow action. nicouzouf Profile_001 Savegame_065 replay-015 clears the frame-1283 pending-speech panic and advances to an independent RNG frontier after frame 1343; the focused unselected-PC control and parity-profile build pass.
- #315 completed: `QuitSwordFight` removes every reciprocal relationship through the authoritative `DeleteOpponent` path, so a survivor with remaining opponents resets smalltalk initiative according to the logical opponent list even while its action state is temporarily non-sword. nicouzouf Profile_001 Savegame_051 replay-015 clears the frame-1182 extra smalltalk RNG draw and matches exact EOF.
- #318 completed: detection sharpness now selects its 20/200 multiplier from Original's persisted `mViewParameters.bLeanOut` state rather than current posture. That flag can remain true after an actor becomes Upright when another path has already replaced `EYES_LOOK_DOWNWARDS`; Rust already preserved the sticky state as `NpcData.view_lean_out` but ignored it in both Enemy and non-Enemy detection. nicouzouf Profile_001 Savegame_039 replay-008 clears Soldier29's frame-122 maximal-visibility mismatch (`1779` versus `177`) and advances to an independent frontier after frame 437; the focused sticky-flag control and parity-profile build pass.
- #319 completed: route-source adaptation now treats an active `PassDoor` as Original's live `GetDoor()` only until the first `PassingDoor` callback. After that callback clears the door, commands authored during the remaining far-side walk use the actor's live position and sector instead of the completed gate's endpoint. nicouzouf Profile_001 Savegame_069 replay-011 clears PC245's frame-184 door-53/door-52 route split and matches exact EOF; both focused pre/post-callback controls and the parity-profile build pass.
- #320 completed without a Rust change: SuN1Sh1nE Profile_004 Savegame_024 replay-010 records a frame-431 non-shield arrow ricochet from Soldier44, but the current runner's live and profile-derived weapon identities both resolve Archer03 to weapon 13, whose authoritative fullgame_linux CPF piercing protection is zero. Shooter Soldier101 and victim Soldier44 are opposing camps, no target or shield owns the collision point, and every recorded frame RNG draw is nonzero modulo 101, so current Original `IsHurtableByArrow` must damage rather than ricochet. The exact source/data-inconsistent trace is retired pending deterministic current-Original recapture.
- #323 completed: `AlertOfficer` preserves Original's shipped release behavior when the selected officer is already a Friend detectable. Original's uniqueness check is only a debug assert before an unconditional append, so Soldier138 legitimately retains a second type-3 entry for Soldier97. A targeted append effect reproduces that boundary without relaxing ordinary detectable deduplication. linux3 Profile_001 Savegame_014 clears its sole frame-17481 detectable-length mismatch and matches exact EOF; both focused policy controls and the parity-profile build pass.
- #332 completed without a Rust change: all 36 nicouzouf Profile_001 Savegame_053/Savegame_059 schema-12 random-save records first diverge after frame 441 on one missing opaque query from Soldier45's eyes `(909,392,45)` to `(956.9902954101563,404.00244140625,0)`. Upright Soldier51's simulation detection point is instead at Z 45, and Soldier45's MissedFriend cadence is closed at modified frame 517. The zero-height endpoint belongs to the selected-view detection polygon's night-light-sector rendering path, which schema 12 recorded but current Original correctly suppresses through `RHParity::PresentationScope`. These presentation-contaminated records are retired pending schema-14 recapture; no render-only query was added to Rust simulation and legitimate simulation visibility comparison remains strict.
- #334 completed: every ordinary `RHElementTarget::MouseClicked` command now uses the existing direct target route: zero-tolerance `AppendMoveToSequence` movement followed by Turn and the interaction. The route covers exactly Search, UseLever, HitTarget, HandleTarget, TakeTarget, and Pay; human interactions retain their generic entity seeks. The complete six-command regression preserves saved wait time after Hourglass, and the human Search control retains tolerance 13 and wait time 25. nicouzouf Profile_001 Savegame_045 replay-007 clears frame 320 and advances to an independent frontier after frame 1140; Savegame_037 replay-015 clears frame 616 and advances to an independent frontier after frame 870. The parity-profile build passes.
- #335 completed: `AlertSoldiers` sorts accepted soldiers by Original's stretched literal 3D `SquareDistance` before assigning formation slots, rather than comparing projected map X/Y alone. nicouzouf Profile_001 Savegame_037 replay-003 clears the frame-420 swapped path-goal ownership between elevated Soldier53 and ground-level Soldier54 and matches exact EOF; the representative distance-order regression and parity-profile build pass.
- #338 completed: repeated directed panic now classifies the remembered center with Original's aspect-scaled actor facing vector before deciding whether to restart panic. The recorded direction-5 fixture flips from behind under Rust's former unscaled dot product to in front under `GetDirectionVector()` semantics, while a mirrored-center control remains behind; the focused regression is integrated in `031676ffc`.
- #341 completed: repeated directed panic now preserves an existing FleeingPanic/FleeingRunToDoor state, alert, and larger run count until the engine-owned door/search boundary decides Original's `bNewPanic`. In the no-door continuation, only the new-panic arm performs `SetState`, assigns Red alert and `runs + 1`, and fires `EVENT_REACHPOINT`; the repeated no-door arm remains an upgrade-by-maximum run-count operation. nicouzouf Profile_001 Savegame_065 replay-015 clears Soldier53's frame-1370 Red-versus-Yellow alert frontier and matches exact EOF through final frame 1528, all 1500 recorded frames. The three focused producer/new/repeated panic-boundary regressions and optimized parity-profile build pass.
- #343 completed: AI-facing PC scans now preserve Original's AddElement registry order; AI `Position(entity)` uses one door-first/carried-PC resolver at the evaluating owner boundary; and BattleDecisions' nearer-friend gate uses the selected target's exact literal 3D SquareDistance with source-consistent AI Position operands. The downstream phalanx split came from comparing raw floating-point MaxNorm distances: Original narrows each candidate to `UWORD`, tying both at 191 and preserving PC171 before PC174. linux3 Profile_003 Restart replay-004 clears frame 973 and advances independently to PC173's direction-goal-only frontier after frame 1150 (Original 1, Rust 14). Profile_003 Savegame_046 replay-003 and both task-36 schema-14 replay-009 controls match exact EOF; all five focused regressions pass.
- #344 completed: PushAside warning and live-hit rectangles now derive their facing axis from Original's literal 16-sector vector with `ASPECT_RATIO`, rather than an unscaled unit-circle vector. In nicouzouf Profile_001 Savegame_039 replay-008, profile-17 strike A dispatches to PushAside despite the straight-sword animation name; the corrected side projection admits Soldier91 inside Soldier110's 10-unit half-width and restores the missing frame-596 `ReceiveSwordDamage` RNG triplet. The focused rectangle regression and parity-profile build pass. The trace clears frame 596 and advances 877 frames to an independent Soldier81 movement frontier after frame 1473 (97.614% of 1509 frames).
- #345 completed: reactive `ConsiderToBeginParade` now closes Original's synchronous `StopAll`/Halt boundary before authoring its replacement GoTo. The direct owner drain holds aside pre-existing foreign condolence cards while closing owner and newly causal work, then restores the foreign FIFO; the replacement step-back uses a complete pre-Halt live context and preserves sword/rider flags. The focused foreign-backlog/Halt and rider/non-rider reactive controls pass, as does the optimized parity build. SuN1Sh1nE Profile_004 Savegame_024 replay-004 clears Soldier54's frame-584 Freezing/Wait/Swordfight frontier; with the separately archived #360 geometry correction, the Tasks345+360 validation runner first diverged independently after frame 585 on Soldier110: direction Original 8/Rust 14, direction_goal Original 8/Rust 14, and sprite_row Original 712/Rust 830.
- #350 completed without a Rust change: linux3 Profile_001 Savegame_038 schema-12 first diverges after frame 54408 because Original records five extra opaque queries from Soldier186's eyes `(1428,1164.0009765625,135.00100708007812)` to nearby night-light sector barycentres. Removing that five-query prefix leaves all eleven simulation queries bit-for-bit identical to Rust in geometry, result, and order. The prefix is owned by the selected-view `ShowDetectionPolygon` path through `RHShadowPolygon::DisplayDebug` and `ComputeViewRadius`; current Original wraps that rendering path in `RHParity::PresentationScope`, so `RHFastFindGrid::IsReachable` correctly suppresses its presentation visibility records. The contaminated schema-12 record is retired pending schema-14 recapture; Rust simulation visibility remains unchanged.
- #353 completed: projectile human collision now retains the last eligible human in Original's actor order. The `break` after both a primary belt hit and a leaning-eye retry exits only the posture switch, so the surrounding `marrayActors` scan continues and later candidates replace earlier ones. SuN1Sh1nE Profile_004 Savegame_034 replay-001 clears Projectile249's frame-940 Soldier114/Soldier116 arrow-damage swap and matches exact EOF through all 1500 recorded frames at final frame 1599. Both focused actor-order regressions and the optimized parity-profile build pass.
- #355 completed: orphaned non-forced sword movement now preserves Original's complete virtual `Stop(Injury)` callback order. `StopMovement` synchronously delivers the selected movement's condolence card before base `SequenceElement::Stop` traverses the linked Face/Turn; that Turn closes before `QuitSwordfight` is registered and `EVENT_QUIT_SWORDFIGHT` is delivered, while Actor Hourglass marks the captured movement Impossible only after Execute returns Aborted. Unrelated pending owner work remains intact and Quit cannot inherit the stopped Turn's cross link. All 15 focused tests pass. Commit `2a6a0449dde878bf499bcf7c4971ec54ce7c1c5d` clears nicouzouf Profile_001 Savegame_039 replay-001's Soldier81 post-frame-117 direction-goal frontier and advances to an independent frame-199 RNG overdraw.
- #360 completed: `ProposeGoodStepBackGoal` now applies its supplied aspect ratio to the retreat norm, direction classification, and sector vector, matching Original's explicit `SWORDFIGHT_ASPECT_RATIO` call from `ConsiderToBeginParade` instead of silently using ordinary isometric geometry. SuN1Sh1nE Profile_004 Savegame_024 replay-004 clears Soldier54's frame-584/frame-585 step-back movement frontier; the Tasks345+360 validation runner first diverged independently after frame 585 on Soldier110: direction Original 8/Rust 14, direction_goal Original 8/Rust 14, and sprite_row Original 712/Rust 830. All four focused step-back tests and the optimized parity build pass.
- #365 completed: `ReconsiderEnemyApproach` now resolves both `Position(pFriend)` and `Position(pFriend->GetPrimaryTarget())` through Original AI `Position` semantics, including the committed endpoint of an active door pass. In nicouzouf Profile_001 Savegame_069 replay-001, PC244 and PC247 are both passing door 53 directly before frame 444, so Original resolves them to the same point-in and cannot satisfy its strict target-exchange inequality; Rust had used PC244's interpolated actor position, swapped Soldier197 from PC247 to PC244, and emitted the wrong genuine `EnterSwordFight` visibility destination. Finalized commit `00e15d031`, building on the common resolver from `21d20b27a338d7e813f720ee6210465bcc0a94cb`, clears that frontier; the focused both-operands regression passes, and the optimized runner matches exact EOF through all 750 recorded simulation frames at final frame 770.
- #367 completed: corpse-intersection state now closes at each live actor owner boundary, matching Original `RHElementActorHuman::SetPosture`'s synchronous `UpdateIntersectingCorpses` call before the next creation slot can sample repulsive state. A lying-to-upright transition therefore clears the actor's small-radius and anti-collision-ignore flags before later movement. Commit `02d6255b4` retains the post-walk full scan for posture writes outside actor envelopes. nicouzouf Profile_001 Savegame_075 replay-008 schema-14 clears Soldier140's original post-frame-480 movement/direction/visibility frontier and advances to an independent divergence after frame 1027, not exact EOF. The focused owner-boundary regression and full `robin_engine` test gate pass.
- #378 completed: retained lateral/circle victim-angle state now remains dormant while a replacement PushAside owns Execute, matching Original's weapon-kind dispatch instead of presenting an interrupted strike-F sweep's terminal direction during strike A. Commit `bbb8daf52` limits IN_PROGRESS sweep advancement to Lateral and the four half/full-circle kinds; the focused replacement regression also confirms that PushAside still queues its own damage. SuN1Sh1nE Profile_004 Savegame_024 replay-004 clears the prior divergence after frame 585 and advances 326 frames to a distinct RNG-consumption mismatch during Original frame 911, not exact EOF.
- #404 completed: generic/non-movement actor Execute now closes Original's base `CheckForLineCrossing` boundary before motion-result handling. The unified active `LINE_CROSS` list preserves elevation duplicate and old-position filtering, and its multi-line arm runs `UpdateRoll`/`ComputeIncrementAll` even when every candidate is non-elevation. Commit `9f3ccfa94` clears nicouzouf Profile_001 Savegame_041 replay-007's PC125 map-increment divergence after frame 823; combined with the independently source-proven translated-damage facing fix in `613d3d092`, the representative matches exact EOF. Focused coverage exercises candidate identity/filtering plus one-versus-multiple and cached-versus-uncached corpse-placement crossings. Savegame_045 replay-014 and replay-003 remain at their independent post-frame-1298 PC99 and post-frame-1191 Projectile121 frontiers.
- #406 completed: exact-goal movement transitions now refresh target-leading state after recomputing their unchanged position, matching Original's nonzero animation-distance block. Original multiplies that distance by the already-zero goal increment and clears the forecast; Rust retained PC126's previous running forecast and incorrectly led Projectile130 away from its stationary belt point. Commit `18a158250` clears linux3 Profile_002 Restart replay-004's six projectile-vector differences after frame 974 and matches exact EOF through all 1,272 recorded frames at final frame 1272. The focused exact-goal forecast regression and all 72 movement tests pass, the optimized parity build succeeds, and same-save replay-009 remains exact EOF.
- #407 completed: `GetNearestFreeShieldBearer` now scans the complete same-camp soldier registry, matching Original's global scan without an `IsAbleToFight` gate. Inactive/script-locked Soldier62 remains eligible while running to its phalanx slot and is nearer than active Soldier60; Rust's former able-only nearby-fighter scan omitted it and sent archer Soldier66 to the wrong cover point. Commit `f7322ef74` and the focused inactive-bearer regression clear nicouzouf Profile_001 Savegame_045 replay-014's frame-1054 path-goal frontier and advance to an independent Soldier62 `list_them` divergence after frame 1162. The optimized parity build passes; same-save replay-003 remains unchanged at its independent frame-1191 Projectile121 lifecycle frontier, so neither replay is exact EOF.
- #420 completed: archer proximity and step-back geometry now use Original AI `Position` semantics rather than raw fighter feet. In nicouzouf Profile_001 Savegame_075 replay-008, door-passing PC346's raw `(1352,500)` position becomes door 33's committed point-out `(1352,512)`, causing Soldier191 to retreat as Original does after frame 1027. Commit `70277f26e` covers both `ArcherIsToNearToEnemy` and the reselected ArcherStepBack target; the focused regression and all 167 `ai_enemy` tests pass, the optimized parity build succeeds, and the schema-14 representative matches exact EOF through all 1,500 recorded frames at final frame 1514.
- #453 completed: all direct reaction orders authored by the human damage translators now retain Original's `bComputeDirection=false`, including generic/mobile, arrow/stone, sword, hit, death/knockout, ladder, push, and both shoulder-partner paths; the separate `TranslateRoll` order remains direction-enabled. Commit `613d3d092` passes the 47/47 focused damage/melee set and dedicated command regressions. Combined with Task404's restored post-Execute crossing boundary in `9f3ccfa94`, a fresh optimized runner matches exact EOF for schema-12 nicouzouf Profile_001 Savegame_041 replay-007 and nearby replay-006; both report `final_frame=1511` and `frame_count=1500`.
- #456 completed: Pay now follows Original's complete `PAYING` lifecycle across commits `bfd9a8114` and `d0f4c93ab`. Translation only installs the order; first Execute revalidates, samples the antagonist's live facing, turns the PC opposite, and emits `HERO_GIVE_MONEY` exactly once. Genuine animation DONE revalidates again, aborting before effects when invalid; valid completion launches `ActivateMoney`/`ReceivePurse` before deducting `BEGGAR_SALARY`. The corrected three-frame lifecycle regression covers both validation boundaries and settled valid/invalid completion, all three focused `pay_` tests pass, and a fresh optimized runner matches schema-14 linux3 Profile_003 Restart replay-004 through exact EOF across all 1,500 recorded frames and 5,072 RNG draws.
- #463 completed: generated PassDoor movements now retain the path-local traversal direction that Original copies from `RHGate::mbDirect` in `RHSequenceElementMovement::SetGate`. AI `Position(actor)` therefore resolves a selected direct pass to the inside endpoint and an indirect pass to the outside endpoint, instead of treating both as indirect. Commit `dc09f516d` and the focused two-polarity gate-builder regression clear schema-14 linux2 Profile_002 Savegame_002 replay-009's frame-516 patrol-coordinate/close-point frontier; the optimized runner advances to the trace's pre-existing independent frame-711 Civilian65 path goal/tolerance divergence, not exact EOF. Schema-14 Cyrdach Profile_156 Savegame_001 replay-009 remains exact EOF on the same runner.
- #472 completed: phalanx list rebuilding still traverses an installed inactive right neighbour, but now applies Original's viewer-active short circuit before either 360-degree or 180-degree geometry and LOS. In nicouzouf Profile_001 Savegame_045 replay-014, inactive Soldier62 had produced five extra Rust visibility queries to PCs 103, 104, and 105 after frame 902. Commit `ce5bcdf38` and the focused inactive-viewer regression clear that query frontier and the former frame-1162 Soldier62 `list_them` frontier; a fresh optimized runner advances to an independent ten-field PC99 melee/command divergence after frame 1298, where Original executes `SwordstrikeThrustA`/animation 67 and Rust remains in `MoveOk`/animation 304. Same-save replay-003 remains unchanged at its independent frame-1191 Projectile121 lifecycle frontier, so neither replay is exact EOF.
- #484 completed: arrow trajectory integration now preserves Original's WATER-owned `mbDive` lifecycle independently from HOLE disappearance and retains that flag when a later falling-arrow ricochet recomputes a dry endpoint. Commit `efe8f043c3` follows `RHElementProjectile::ComputeTrajectory` and terminal `Hourglass`: a dive retires before `HitObstacle`, avoiding the ordinary bare-ground `0.001` snap, while retained dive state still owns the terminal plouf presentation. The focused water and falling-arrow suites pass 23/23 and 4/4. Old schema-12 nicouzouf Profile_001 Savegame_045 replay-003 clears its five-field Projectile121 frontier after frame 1191 and advances independently to a five-field Soldier58 AI/detection divergence after frame 1291, not exact EOF; same-save replay-002 remains unchanged at its independent frame-1394 Projectile118 membership frontier. Savegame_041 replay-007 remains exact EOF across all 1,500 recorded frames.
- #488 completed without a Rust change: old schema-12 SuN1Sh1nE Profile_004 Savegame_017 replay-013 records Projectile182 outside all membership after frame 1165, although its bit-exact ballistic inputs produce terminal bare-ground footprint `(1536.70849609375,419.43267822265625)`. The trace's own active motion geometry places the footprint and its entire grid cell strictly inside layer-0 area/sector 0, no non-area motion sector contains it, and the current Leicester proto has no solid sight obstacle overlapping the terminal segment. Current Original `ComputeTrajectory` must therefore assign layer/sector 0 exactly as Rust does. The stale record is retired pending deterministic recapture. Task186's unrelated schema-14 linux2 Profile_002 Savegame_017 replay-013 move-to-wait control remains retained and exact EOF.
- #497 completed without a Rust change: old schema-12 nicouzouf Profile_001 Savegame_045 replay-014 reaches its current independent ten-field PC99 melee/command frontier after frame 1298, but the causative frame-1296-to-1297 `sword_strike` record serializes command A and `with_seek=true` without the resolved seek distance. Original's simple-click branch emits command A with 90% of the weapon's generic maximum distance, while the THRUST_A gesture emits the same recorded command with 90% of thrust A's maximum (`original-code/RHengine.cpp:15709-15730`, `15755-15778`, `15829-15861`; `original-code/RHSword.cpp:247-252`). The current recorder includes that resolved distance (`original-code/RHParity.cpp:1803-1810`), but this archived record cannot prove which input branch owned the seek; choosing either compatibility fallback would be speculative and could corrupt the opposite class of old traces. The single input-incomplete trace is retired pending deterministic modern recapture, with no trace-specific gameplay rule. The separately retained schema-14 linux3 Profile_001 Savegame_045 replay-014 matches exact EOF on the current optimized runner.
- #500 completed: `AlertSoldier` now measures candidates with Original's isometric MaxNorm formula, stretching map Y by `INVERSE_ASPECT_RATIO` before taking the maximum and narrowing to `ULONG`, rather than using raw map-space Chebyshev distance. At schema-14 linux2 Profile_002 Savegame_002 replay-009's frame-711 frontier, that formula selects Soldier187 over Soldier141 and exactly explains Original's cross-sector door-99 point-in path goal `(1371,2249)` with tolerance 0. Commit `f31e42a77` and the focused candidate-order regression clear the frontier; the optimized runner matches exact EOF through all 710 recorded frames at final frame 1186, and schema-14 Cyrdach Profile_156 Savegame_001 replay-009 remains exact EOF.
- #501 completed: future sword-strike records now carry the exact input-resolved seek tolerance, preserving Original's distinct simple-click generic maximum and A-E gesture-specific maximum through live input, parity decoding, player commands, and macro capture/replay. Nested Original commit `dd2e1c762` records the scalar only for same-sector seeks; outer commit `036f6ea95` consumes it verbatim, defaults missing historical fields to the prior strike-specific behavior, normalizes direct strikes to no distance, and advances the native trace cache to v55. Focused command, macro, trace, sector-topology, and live-resolution tests pass together with the parity-example check. This forward schema fix does not clear the provenance-incomplete old trace retired separately by Task497, and makes no old replay or EOF claim.
- #507 completed: combat `ReadyForTakeOff` now preserves Original's cached 3D takeoff point across installation of the landing obstacle and accumulates its one authored world-space vector before re-projecting map Y. Rust's eager obstacle update had replaced Soldier114's takeoff elevation with the landing plane after frame 1581; separately accumulating projected Y and Z then left a one-ULP Y mismatch after frame 1584. Commit `9913b66cd` covers both pushed and hit flights while retaining GroundPlane behavior. Both elevated-goal focused regressions and the optimized parity build pass; SuN1Sh1nE Profile_004 Savegame_034 replay-010 and adjacent replay-009 both match exact EOF.
- #527 completed: RiderCharging now closes Original's ordinary `PerformMotion(RUN)` destination boundary after committing its frame step: an undeviated zero-tolerance arrival snaps to the goal and returns `TERMINATED`, line-crossing callbacks still observe the terminating entry order, and Actor Hourglass advances it only afterward if no callback replaced it. Integrated commit `2338dc115` clears linux2 Profile_002 Savegame_018 replay-009's Soldier83 frame-8749 position/goal/animation/substate frontier and matches exact EOF; schema-14 Savegame_017 replay-013 remains exact EOF. The callback-order/snap/advance regression and all 21 rider-charge tests pass.
- #533 completed: changing pathfinder motion obstacles now toggle their owning fast-grid sector together with every perimeter line, matching Original `RHPathFinder::SetStateArea` and preventing inactive obstacle polygons from erasing a valid projectile landing area. Commit `5361c9f00` synchronizes fresh initialization, runtime patches, legacy patch reconstruction, and saved pathfinder state with strict sector bindings. All 18 pathfinder tests, both graph-state preflight tests, and the focused legacy-grid adoption test pass. Old schema-12 nicouzouf Profile_001 Savegame_010 replay-012 clears Projectile106's layer/sector-only frontier after frame 1142 and matches exact EOF across 1,500 frames (11 through 1510, 2,594 RNG draws); adjacent replay-011 also matches exact EOF across 1,500 frames and 2,190 RNG draws. This dry inactive-sector fix is distinct from Task484's water-dive lifecycle.
- #538 completed: death cleanup now preserves exactly the seven `Todo` commands Original dead Human `Instruct` still admits—five ordinary damage commands, Wait, and GetKilledAtBottom—while interrupting unrelated owner work. Commit `9e7c4fb0f` restores a simultaneous second sword hit and its exact protection/provoke RNG triplet in schema-12 linux2 Profile_002 Continue replay-004, clearing the frame-8319 RNG panic and advancing to a new PC137 lifecycle frontier after frame 8320: Original retains `DyingSword`/`ReceiveSwordDamage`, while Rust exposes animation 283/`Wait`. The full 3,032-test library suite and exact-whitelist/simultaneous-hit regressions pass; schema-14 Cyrdach Profile_156 Savegame_001 replay-009 remains exact EOF.
- #539 completed: a retained circle now runs Original's victim effect with the current replacement strike on every non-exact-action-point `IN_PROGRESS` Execute call, while leaving sprite facing and retained sweep angles unchanged until the exact action-done frame and counter. Commit `ddabda8ef` restores strike I's right-to-left damage/protection/provoke path for Soldier197's retained strike-H victim after frame 1397. The multi-frame and zero-forecast focused regressions pass within the 6/6 `interrupted_` set, and a fresh optimized runner matches SuN1Sh1nE Profile_004 Savegame_032 replays 012 and 011 through exact EOF. Replay-007 is not claimed as a control because it independently diverges after frame 331 in detection/visibility/direction state.
- #540 completed: Enemy return-to-duty continuation now restores Original virtual `SetState`'s shield-pair constraint when an archer leaves the seven allowed bow substates. In old schema-12 nicouzouf Profile_001 Savegame_045 replay-003, Soldier64 returned to `DefaultGotoRoute` after frame 1278 but Rust retained `shield_bearer_before_me=58` and Soldier58's reciprocal `archer_behind_me=64`; Soldier58 therefore remained in shield protection at its frame-1291 timer instead of entering `GetBattleOverview`. Commit `e553adb78` uses the existing reciprocal updater at the owner boundary. The focused reciprocal-clear/next-timer regression passes together with all six return-to-duty tests, and a fresh optimized runner matches the representative through exact EOF. Savegame_041 replay-007 remains exact EOF on the same runner.
- #541 completed: elevated combat domino sweeps now use Original's literal world-ground X/Y positions and full world X/Y flight increment instead of projected map coordinates. In old schema-12 linux2 Profile_002 Savegame_042 replay-004 after frame 7130, Soldier185 and Soldier182 were within Original's 15-unit world-ground radius (squared distance about 186.69), but projecting their differing elevations into map Y produced squared distance about 304.88 and made Rust omit the concussion-3 `ReceiveHitDamage`; the extra Original Civilian108 visibility query and Soldiers182-184 alert/AI split were downstream. Commit `9704d0bef` follows `RHElementActor::ApplyDominoEffect` and `GetPositionGround` (`original-code/RHelementactor.cpp:318-376`; `original-code/RHElement.h:322`). All six `domino_` tests pass, including an elevated world-versus-map boundary and the existing flat controls, and a fresh optimized runner matches Save042 replays 004 and 003 through exact EOF. Task80's earlier owner-slot timing fix remains distinct.
- #542 completed: lethal soldier cleanup now mirrors Original virtual `SetState(SLEEPING_FOREVER)` by synchronously detaching both combat neighbours, including reciprocal zero-clears that `EnemyAi::set_state` had already queued before death discards the outbox. Commit `37fea7ac4` clears nicouzouf Profile_001 Savegame_039 replay-007's former frame-406 Soldier90 goal/increment split on an isolated `2338dc115` build and advances 429 frames to an independent sprite-index panic during Original frame 835; Savegame_045 replay-010 remains exact EOF across 1,500 frames. All three focused death-relationship regressions pass. This is not a current-HEAD clearance claim: the later committed integration lineage independently diverges earlier after frame 143 on Soldier110 direction/direction-goal/sprite-row and cannot yet reach the Lane36 boundary.
- #543 completed: an already-dead human still runs Original `TranslateSwordDamage`; only the repeated `SetLifePoints`/Kill cascade is suppressed. Commit `f6fbd2674` lets the second of two same-frame cutting hits replace the first damage card with its own live `ReceiveSwordDamage`/`DyingSword` order while preserving grounded Task269 termination. The strengthened FIFO lifecycle regression and grounded control both pass, and a fresh optimized runner matches schema-12 linux2 Profile_002 Continue replay-004 and schema-14 Cyrdach Profile_156 Savegame_001 replay-009 through exact EOF.
- #544 completed: retained `EVENT_AFTER_SCRIPT_GO_ON` siblings now close one recursive Think and its engine-facing outbox before the next sibling begins, while preserving the outer StartThink/log first and its live tail/EndThink last (`original-code/RHartificialbonhomie.cpp:110-145,631-660`). Commit `233012233` restores Civilian37's first Panic no-door `EVENT_REACHPOINT` and its exact two direction/segment RNG draws (`original-code/RHartificialintelligence.cpp:1996,2030,3057-3073`), skips duplicate AfterScript markers only when reached, and preserves untouched suffixes when a sibling locks; follow-up `3c48d625b` truthfully provisions the deeper panic-boundary fixture exposed by that recursion. The Friendly order/RNG regression, Enemy lock/duplicate control, and full 3,038-test library suite pass. A fresh optimized runner matches `parity-random-save-replays-60s-15x/traces/Savegame_SuN1Sh1nE/Profile_004/Savegame_013/replay-002-session-0001.jsonl.zst` and Task36 control `parity-random-save-replays-60s-15x-schema14/traces/Savegame_Nescafe/Profile_001/Savegame_001/replay-009-session-0001.jsonl.zst` through exact EOF.
- #545 completed: an aligned in-place transition now preserves the stable anti-vibration direction count exactly as Original's zero-difference `TurnAntiVibration` return does. Commit `14bbf2ba2` prevents frame 1189 from clearing PC108's retained count 2, so its new goal 9 rotates direction 6 to 7 on frame 1192 instead of delaying a frame. Both focused `in_place_transition` tests pass, and authoritative optimized runner SHA-256 `a302025592f5769ebc0869b3983cfe9dcb647f97b800c244de2af73c1f55233d` matches Cyrdach Profile_156 Savegame_015 replays 005 and 004 through exact EOF.
- #546 completed: ordinary entity seeks now test Original's live same-sector interaction radius before an expired stale-route refresh, while the PC shield-seek arm retains its source-specific refresh-first order. In linux3 Profile_001 Savegame_001 replay-012, PC126 was already within Soldier63's 32-unit seek radius after frame 3240, but Rust refreshed the old destination and exposed `MoveOk` with wait 25 instead of launching `HitCmd`. Commit `0d828d811` restores the post-seek handoff before refresh; the focused expired-stale-route Hit regression passes, and authoritative optimized runner SHA-256 `a302025592f5769ebc0869b3983cfe9dcb647f97b800c244de2af73c1f55233d` matches same-save replays 012, 011, and 001 through exact EOF.
- #547 completed: falling-arrow presentation now follows Original's complete `SortForDisplay` result, including the FX-mask phase that can extract a deeper non-animation ahead of a shallower arrow before `RHElementArrow::Refresh` consumes its global `(rand() % 3) + 3` draw (`original-code/RHengine.cpp:7724-7755,8032-8120`; `original-code/RHElementArrow.cpp:318-337`). Commit `5cabafcf3` replaces the arrow-only world-Y sort with the existing authoritative full display order; the focused FX-reversal/RNG-owner regression passes 1/1. Optimized runner SHA-256 `a302025592f5769ebc0869b3983cfe9dcb647f97b800c244de2af73c1f55233d` matches nicouzouf Profile_001 Savegame_037 replays 015 and 012 through exact EOF. The committed scope is exactly `engine/tick.rs` and `engine/tests/lifecycle.rs`.
- #548 completed: a terminal true-circle strike now presents Original's retained current angle at the exact action-done frame/counter before `PerformAction` progresses the sprite, while the current Execute call still owns strike kind, direction, and animation. The source-faithful forward correction in mixed commit `0d250c7ad` is covered by terminal-progression and interrupted-replacement regressions, both passing 1/1; that commit also contains unrelated Task546 documentation because of a staging race. Authoritative combined runner `/home/phire/robinhood/target/parity/examples/original_parity_replay` (base HEAD `0d250c7ad440`, frozen diff `5b099cf99b0f`, SHA-256 `226d83c5deac0a89007d5fc1661cd5bb188d7d4c4908076a525486650679b6de`, size 65,888,107 bytes, mtime `2026-08-11 17:18:37.685 +0200`) clears nicouzouf Profile_001 Savegame_039 replay-007's frame-143 facing/row split and Task542's former frame-406 boundary, reaching an independent sprite-index panic while replaying Original frame 835. Savegame_045 replay-010 remains exact EOF across all 1,500 frames.
- #549 completed with representative clearance explicitly unobservable: circle-strike warning tolerance now converts the CPF rotation degrees through Original's `RHSword::GetStrikeRotationAngle` radians boundary and truncates the complete extension to `UWORD` before range addition. Commit `4d5e481ef` restores the two moving defenders omitted at the frozen Save024 replay-004 frame-911 RNG frontier; the focused collector regression passes 1/1 and both task-36 schema-14 controls match exact EOF on runner `226d83c5…`. Current integration stops the representative earlier at independent frame 752, so no f911 clearance or downstream advance is claimed.
- #551 completed: accelerating the lazy tail of a high-wall door pass now follows Original `PostProcessPath`'s posture-first transition choice. A crouched PC finishing the authored `TransitionCrouchingUp` therefore receives `TransitionWalkingCrouchedRunningUpright`, not the waiting-upright transition selected solely from action state. Commit `0261e4ab0` clears linux3 Profile_001 Savegame_000 replay-002's sole PC126 animation frontier after frame 6496; the focused lazy-tail regression passes 1/1, and authoritative optimized runner SHA-256 `226d83c5deac0a89007d5fc1661cd5bb188d7d4c4908076a525486650679b6de` matches replays 002 and 001 through exact EOF.
- #552 completed: enemy patrol-coordinate movement now reaches Original's virtual `RHArtificialMalignity::SetState` tail after `StopAll`, so Default patrol state clears alert to Green and authors `LeaveAttentiveMode` before the replacement `GoTo` (`original-code/RHartificialintelligence.cpp:7136-7158,7772-7835`; pure-virtual declaration `original-code/RHartificialintelligence.h:1222`; enemy override `original-code/RHartificialmalignity.h:236`; implementation `original-code/RHartificialmalignity.cpp:9145-9160,9223-9275,9402-9413,9544`). Commit `f766104e2` separates shared patrol geometry from owner effects and defers only movement behind an attentive transition that actually launches. The focused walk/run/no-change regression passes 1/1, and authoritative combined runner SHA-256 `226d83c5deac0a89007d5fc1661cd5bb188d7d4c4908076a525486650679b6de` matches linux2 Profile_002 ExQuickSave replays 011 and 010 through exact EOF, clearing the representative's former frame-1312 Soldiers144/145 command/alert boundary.
- #553 completed proactively: Friendly/Bonhomie patrol-coordinate movement now follows Original's common `StopAll` -> virtual `SetState` -> `GoTo` ordering, preserving Bonhomie's Green-alert, state-log, and synchronous script-callback effects before replacement movement (`original-code/RHartificialintelligence.cpp:7772-7835`; `original-code/RHartificialintelligence.h:1222`; `original-code/RHartificialbonhomie.h:37`; `original-code/RHartificialbonhomie.cpp:1158-1238`). Commit `6874580ad` keeps shared patrol geometry while restoring the Friendly owner boundary. The focused patrol-coordinate batch passes 4/4: two new Friendly cases cover changed-state Walk/Run ordering and unchanged-substate Bonhomie effects, alongside the existing Enemy and real-chief controls. No dedicated failing representative was assigned and no frontier clearance is claimed; authoritative runner SHA-256 `2f9737073af91081b893dcc5ee0fbdfe6a5117e25de88dbcdeffc2d73e07affc` conservatively matches nicouzouf Profile_001 Savegame_041 replay-007 through exact EOF as a non-regression control.
- #554 completed: legacy `INFO_NOISE` stimuli now decode Original's exceptional v22+ X/Y/level/sector position payload instead of the generic X/Y/sector/level order used by adjacent position-bearing variants (`original-code/RHartificialintelligence.cpp:231-293`). The former decoder turned the representative's authored sector 27/layer 0 noise into sector 0/layer 27, preventing projection to elevation 220 and producing four wrong direction goals after frame 27777. Commit `2a7845609` adds the Noise-only decoder; its byte-level Noise-plus-Hint regression passes 1/1. Authoritative runner SHA-256 `dd55dcb5c492daa93566385694b3673a8b5d35bd10fd1c429df9b59ba9d28575` clears linux3 Profile_001 Savegame_018 replay-001's Soldiers130-133 direction-goal family and advances 879 frames to a distinct frame-28656 PC183 LowerBow/Wait lifecycle mismatch; same-save replays 007 and 014 match exact EOF.
- #555 completed: replacement half-circle START warnings now mirror Original's hidden geometry side effect. `GetPossibleVictimsOfHalfCircleSwordStrike` rewrites the shared initial/current/final angles while forecasting a temporary warning list, so commit `65de61f0d` rebases an interrupted sweep's retained victim FIFO to the replacement half-circle geometry without changing full-circle START behavior. The focused real START-to-IN_PROGRESS regression passes 1/1. Authoritative runner SHA-256 `2f9737073af91081b893dcc5ee0fbdfe6a5117e25de88dbcdeffc2d73e07affc` clears SuN Profile_004 Save024 replay-004's frame-752 RNG overdraw and the previously hidden frame-911 frontier, then reaches a new later/distinct frame-912 Soldier111 `ai.substate` mismatch; both task-36 schema-14 controls match exact EOF.
- #556 completed: blocked movement teardown now preserves the action state authored by Original's movement Execute switch while still clearing the path/tracker/blocked box and marking the captured element `Impossible`. Walking has no ABORTED state arm and therefore retains `Moving`; Running still publishes its unconditional `MovingFast`. Commit `55f1654ad` clears linux3 Profile_003 Savegame_055 replay-004's sole Soldier182 action-state frontier after frame 38028. The focused walking/running/sword set passes 3/3, and authoritative optimized runner SHA-256 `dd55dcb5c492daa93566385694b3673a8b5d35bd10fd1c429df9b59ba9d28575` matches replays 004 and 003 through exact EOF.
- #557 completed: mission-team add/remove natives now preserve Original's exact live `RHPCDescription` identity instead of collapsing distinct peasants that share a character profile. PC107's deployment-zone exit therefore removes description 11 without deleting PC119's description-32 team slot, so Sherwood's `CheckDeploymentZone` retains equal team/occupant counts and does not invent a move for PC119. Commit `ce8825fd0` validates live `campaign_description_index` identity while retaining the raw-profile fallback only when no live actor exists. The focused identity/corruption/fallback set passes 3/3, and authoritative optimized runner SHA-256 `b19a04b6e907901ccfb3cc76d8b3f42b7b6b75538b957d42c0a5450c2762c7d5` matches linux3 Profile_003 Savegame_031 replays 007 and 006 through exact EOF across all 1,500 recorded frames.
- External parity 13 completed: a replacement lateral strike's START warning forecast now mirrors Original's writes to the human-owned initial, current, and final sweep angles while preserving an interrupted strike's victim FIFO. Ordinary fresh strikes still defer real sweep initialization to their authored action boundary. Branch commit `31aeb6a538c4465e149d30885f5a1a186bacc2af` was integrated as `49c160ba7e045f6063531c7335a6b9670c77671a`.
- External parity 14 completed without a Rust change: `Savegame_SuN1Sh1nE/Profile_004/Savegame_021/replay-006-session-0001.jsonl.zst` predates Original's typed `NearbyCiviliansPanic` stimulus. Its panic center and escape-door choice depended on pointer or stale union bytes where the civilian handler expected a position; current Original and Rust use the caller's concrete position. The trace is retired pending deterministic recapture. Branch commit `b5a23c10039a677747146a63f0aff07d718add80` was integrated as `912d9a3bab281f845dea5890d2f66f17e6600b18`.
- External parity 18 completed: swordfight initiative transfer now belongs only to the logical sword-movement action that reaches Human Execute's `RHMOTION_START` arm. Broader retained sword visual context, an ordinary successor, and a raw sprite START hidden by `PerformSeek` do not own that side effect. Branch commit `e901d800bb8618b431092b64a3f72505ddf3c38e` was integrated as `8091bcfcf114f812d584fab367dd13c3e418f928`.
- #339 completed without a Rust change: all 15 authoritative Cyrdach Profile_156 schema-14 traces already match exact EOF, including Savegame_001 replay-001 through frame 1701 and the strict task-36 replay-009 control. No compatibility change or retirement was needed.
- #340 completed: `AlertOfficer` route failure now remains owned by the deferred LookForHelp continuation until the enclosing BattleDecisions flow consumes it, clears the latch, falls back to Cassos, and emits exactly one final decision log. Nescafe Profile_001 replay-015 and replay-014 match exact EOF; focused success and failure-order regressions pass.
- #558 completed without a Rust change: Nescafe Profile_003 Savegame_001 replay-002's frame-941 LeaveAttentiveMode/Turn boundary requires a transient `WAITING_ALERTED` Execute-entry identity that the loaded owner graph does not preserve. Three narrow projections failed to advance the real replay and were reverted; no heuristic compatibility state or retirement was added.
- #342 completed without a Rust change: SuN1Sh1nE Profile_004 Savegame_001 replay-005 depends on Original's unrecorded process-global `RHMessenger::mbRecordingMacro` value surviving an in-process load. Schema 14 contains neither the singleton nor its transition, so Rust cannot reconstruct the repeated-select_pc bow gate without inventing trace-specific state.
- #559 completed: swordfight opponent exit now matches the shipped Original release fallback when a legacy load exposes a debug-invalid non-sword movement: WalkingWithSword maps to WalkingUpright and every other action maps to RunningUpright. The linux Profile_005 replay-013 representative and both Profile_011 controls match exact EOF.
- #560 completed: civilian `AlertSoldier` ranking now uses both actors' raw 3-D body positions, stretched world Y, Original MaxNorm, and integer truncation rather than projected or gate-snapped planning geometry. linux2 Profile_002 Savegame_002 replay-009 and replay-008 match exact EOF; stretched-distance, gate-transit, and nonzero-Z regressions pass.
- #561 completed: `GridLine::intersects_bbox` now uses Original's strict endpoint rejection and f32 infinite-line corner determinants instead of `geo::Rect::intersects`, restoring a near-corner movement authorization contact. linux3 Profile_001 Savegame_022 replay-005, a fresh Original recapture, and replay-001 match exact EOF; all fast-find-grid tests pass.
- #568 completed without a Rust change: linux3 Profile_003 Savegame_072 replay-004 creates Bonus638 during PC343's `DropAmmo` path and immediately exposes Original's uninitialized `mpointOldPosition.mZ` bits (`0xf6e5e850`). Original's Bonus/PositionInterface constructors and `CopyPositionMapEtc` never initialize that old-position field before parity observes it, while Rust correctly initializes it to zero. The one undefined-oracle trace is retired; same-save replays 001, 002, 003, 008, 009, 011, and 014 remain exact-EOF controls.
- #346 completed: postponed non-entry sword strikes B-I translate against their authored human antagonist even if it dies before release, while fresh ThrustA retains Original's `CanEnterSwordfightWith` death gate. nicouzouf Savegame_075 replay-015, replay-013, and the replay-012 control match exact EOF; missing required targets now fail with identity instead of fake coordinates.
- #347 completed: `HeyFolksLookThere` prechecks now compare the caller and friends through raw 3-D `RHElement::GetPosition` geometry, sharing the canonical unsnapped AI body point introduced by #560. randomguy Profile_004 Restart replay-007 and replay-008 match exact EOF; the door-transit regression passes.
- #348 completed: legacy AI adoption now clears only Rust's runtime completion-owner marker while preserving all three serialized completion flags, matching Original's nonserialized Think recursion ownership. Both Nescafe Profile_002 state failures match exact EOF. Ten nicouzouf schema-12 captures that match every recorded frame but lack a clean `rng_suffix` terminator are retired as incomplete rather than counted as simulation failures.

Standing clusters not yet tasked: actor.animation 133 (top cluster, resweep-db67cc978 classification), direction_goal 76, position_goal_map.x 72, motion_state 70, MoveOk→MoveWaiting 57, BoredAnimationChoice 56, AiRandomValueRectangle 55, path_events.length 54, RuntimeBuildingExitWait 51, visibility_queries.length 45 (+43 ai.list_them etc.). Full breakdown: `output/parity-audits/resweep-db67cc978/classification.json`.

## Batch 20 — 2026-08-15 session (resume point)

Frozen runner in use for measurement is still `07872e94` (audit dir
`output/parity-audits/batch19-head07872e94-nestedd7792d55-preflight/`). That sweep measured
**204 remaining non-EOF** traces (`remaining-failures-204.snapshot`, sha256
2ba7e64819c94e39e4578c405ffbc07d6dd227c7026c0046ce5e9e216fa8cf13). Since then main has gained
11 behaviour fixes clearing **33 traces to exact EOF** (not yet re-measured by a sweep):

| commit | root cause | traces |
|---|---|---|
| 588b063a8 | rider-charge scalars rounded once by `operator*=`, not per component | 3 |
| 60327ada4 | clear `mbDeviated` on aligned transition start (was count-priming) | 5 |
| 2f8e6b54a | NPC gaze trig evaluated in f64 like Original `GetRotated`/`Angle` | 6 |
| 198f7a6da | collision candidates grouped; break at first impacting group | 2 |
| f1767fe51 | `AssignPath` native keeps `mbSpecialAction` (2nd overload) | 2 |
| 3c1bbd9ae | cross-sector swordfight occupancy gate runs without a jump line; Interrupted + CASCADE_NEXT_LEVEL | 6 |
| 2b24f1094 | `TRANSITION_UNEQUIP_BOW` also unselects the PC Bow action | 3 |
| ab4274027 | `ReconsiderEnemyApproach` recomputes the jump line live after retarget | 1 |
| f0c371b84 | `EndThink` keeps its frame open across completion cascades | 2 |
| 906cd1cf3 | dead path requests still consume the per-frame result slot | 1 |
| 4ac810388 | virtual `Kill` runs synchronously inside piercing damage | 2 |

Diagnostics added: `84520e433` `PARITY_DEBUG_RNG_SITE_MAP` (learns Original callsite-offset -> Rust
site-label pairs while streams agree, then names the draw Rust SKIPPED), `ee0ad7b62`
`[RECONSIDER_STIMULUS]`, `PARITY_DEBUG_PATH_BARRIER` (in 906cd1cf3), `PARITY_DEBUG_BAD_EXPERIENCE`.

### NEXT ACTION: freeze a runner and sweep — and do NOT sweep failures-only this time
Three merges touch shared core paths, so a failures-only manifest is blind to the regressions they
could cause. Include a sample (>= 300) of currently-PASSING traces as a regression guard:
- `198f7a6da` rewrote 465 lines of `sight_obstacle.rs` (`IsReachableImpact` semantics), used beyond projectiles
- `f0c371b84` changes Think recursion depth for ALL AI, reviving two previously-dead depth-gated behaviours
- `906cd1cf3` changes path-request scheduling for every actor
- `9f4284669` replaces "last AI move intent wins" with FIFO in `launch_ai_move`, so every `GoTo` of one
  Think becomes its own sequence (Original `ReconsiderSwordfightObservation` deliberately falls through
  with no `return`, issuing a step-back GoTo *and* a chase GoNear — `RHartificialmalignity.cpp:15502-15570`).
  It also REMOVES `OrderRuntime::validate_invariants`' duplicate-owner rejection, which encoded the wrong
  model, and inverts that invariant's unit test. Affects every AI movement; removing an invariant means
  the sweep is the first broad check that nothing depended on it.
- `ee0ad7b62` **is mislabelled**: its message says "Trace swordfight reconsider stimulus" (diagnostics
  only) but it also rewrites `calculate_opponent_nearest_to_rene` (`ai_enemy/util.rs`) to resolve
  Maurice's opponents through the full fighter registry instead of the 500-unit `IsAbleToFight`-filtered
  `nearby_fighters` snapshot. Faithful to `RHArtificialMalignity::CalculateOpponentOfMauriceWhoIsNearestToRene`
  (`original-code/RHartificialmalignity.cpp:13949`, whose caller at `:13692` asserts non-null), and it
  advanced three Savegame_024/039 frontiers — but it went in unlabelled and unvalidated, so treat it as
  an untested behavioural change when reading batch-20 sweep results.

### Corrections to long-standing campaign lore (both verified this session)
- **Substate mapping**: C++ `RHsubstate` and Rust `ai::model::Substate` align **1:1**, both 256 entries from 0.
  The old "C++ = Rust index + 1" note was FALSE (fixed at line ~412). A parser that ignores Rust's explicit
  `= N` discriminants undercounts and appears to confirm the old claim — do not be fooled by it.
- **RNG cluster labels are artifacts**: `classify_parity_failures.py` keys on the first RNG site of the frame.
  A full triage of the 15-trace "ambient idle" bucket found NOT ONE member caused by idle/bored/16th-frame
  code; it decomposed into ~9 unrelated families. Cluster RNG traces by the state delta, not the site label.

### Environment constraints
- The Original binary CANNOT be rebuilt here: CMake >= 4.0 is satisfiable via pip/uv, but the 32-bit
  toolchain is missing (`cannot find -lgcc` for `-m32`). Prebuilt `original-code/build/native-full/robin`
  and `robin-schema14-capture` DO run, and gdb against them is a proven technique — but no new
  Original-side instrumentation is possible. `addr2line` mis-symbolizes against that prebuilt `robin`
  (it predates current instrumentation); use `PARITY_DEBUG_RNG_SITE_MAP` instead.
- Disk is the binding constraint: each agent worktree reaches 6-17 GB (debug + release targets). Twelve
  concurrent agents once filled a 436 GB disk and corrupted a worktree's git metadata. Remove each
  worktree right after merging; keep a >12 GB floor.

### Capture campaign (separate from the fix wave)
`parity-save-replays/60s-random-input/schema14-seed1000000-20260814/` — target 243 saves x 10 replays =
2430; at checkpoint **850/2430**, running at nice/ionice-idle. It self-pauses by writing `.capture.pause`
when free disk drops under its 30 GB floor + 9 GB/job reservation; delete that file to resume. Completed
captures are compressed under `traces/`; `.capture-attempts/` is disposable scratch (it once held 29 GB).

### Agents in flight at checkpoint (8) — worktrees under .claude/worktrees/
seek flood (16) · light-sector `ComputeViewRadius` raycasts (7+2) · 
seek-refresh extra movement element (6) · detection/alert escalation (4) · Wait-completion-vs-interruption
(~20, largest open lead) · plus two finished-and-merged. Their branches are `worktree-agent-<id>`;
check `git rev-list --count main..<branch>` before assuming work is unmerged.
**UNREVIEWED**: branch `worktree-agent-a0a0cb7befe82f927` carries `e447d2b50` "Charge a weak enemy on the
AI's stretched 3D distance" committed AFTER its report was merged — it is unvalidated by the coordinator
and must be reviewed/validated before merging.

### The "+-4 sector rotation" cluster is DISPROVEN — re-split it (agent returned no fix, 2026-08-15)
A dedicated agent examined 3 of the 14 members in depth and found Rust computes **exactly the same
sector the Original computes** in every one; the divergence is always *whether/when the turn is issued*,
never the sector math. Lead (a) confirmed: `vector_to_sector_0_to_15_with_aspect` is a faithful port.
Lead (b) is a dead end: `RHartificialintelligence.cpp:1670` and `:2001` are faithfully ported and are not
on any examined member's path. Do not re-open this as a rotation bug. Correct split:
- **One real sub-bug, 5 traces — `anim 140 vs 50`**: Savegame_071 r003 (Soldier104), Savegame_032 r006
  (Soldier87), Savegame_030 r005/r006/r011 (Soldier240). In substate 97
  (`SeekingSoldierGiveAlertingReportToOfficerPoint`) Rust `PointTo`s at entry+100 while Original does it at
  entry+150 (measured on three independent saves). The direction Rust picks is the one the Original later
  uses, so `PointTo`'s sector math is right. Ruled out: AI lock, `IsFrozenAll`, skipped sim frames,
  `mSubstateAtLastTimerLaunch`. **Best candidate:** `RHElementActor::Instruct`
  (`original-code/RHelementactor.cpp:1333+`) postpones the `PointTo` TURN element against the looping
  `WaitingAlerted` wait via `DecidePriorities -> RHPRIORITY_DECISION_POSTPONE`, so the turn starts only at
  the wait's next completion boundary; Rust instructs it immediately (installed order flips to `Turning`
  at exactly +100). Testing that should clear all five at once.
- **Separate, 1 trace** — Savegame_022 r012 (Civilian60, f1413): the interaction's `TurnElement` never
  reaches the actor. Rust routes every AI Turning intent through the always-deferred arm
  (`engine/movement.rs:11715` -> `launch_turn_sequence_deferred_no_transitions`), while Original `FaceTo`
  (`RHartificialintelligence.cpp:2739-2779`) `Halt()`s and `LaunchSequence` **synchronously**. That
  defer-everything asymmetry is the prime suspect.
- **Separate, 1 trace** — Savegame_071 r015 (Pc342, f5392): Original's `direction_goal` legitimately
  oscillates 3<->7 every frame while walking (7 when position advanced, 3 when not); Rust is one frame out
  of phase. Transient direction-goal write in the anti-collision/deviation path
  (`RHpositioninterface.cpp:1570/1574/2071/2109/2118`) — adjacent to `60327ada4`.
- **Unclassified, 7 traces** (not examined; three-for-three heterogeneity means do NOT assume a shared
  cause): Savegame_000 r001, Savegame_051 r004, Savegame_008 r013, Savegame_040 r001/r010/r011,
  Savegame_024 r014, Savegame_055 r001/r012, Savegame_038 r004/r015.

### Dead code / latent defects found but deliberately NOT fixed (no failing trace yet)
Same `IsAbleToFight` vs `IsOutOfOrder`/`IsDead` confusion as the fix in `8b6ed1e21`. `IsAbleToFight` is
the combat-readiness virtual and is hard-coded false for every civilian, so it is wrong in BOTH
directions wherever the Original tests body condition:
- `ai_enemy/substate_handlers.rs:5100` — `SeekingTakingNet`/`EVENT_DONE` uses
  `!view.is_able_to_fight && !view.stuck_under_net`; Original is `IsDead() || IsUnconscious()`
  (`original-code/RHartificialmalignity.cpp:2237`). Civilians always satisfy the Rust form, so a
  recovered civilian under a removed net is re-examined instead of returning to duty.
- `ai_enemy/mod.rs:2618` — `chase_childs` skips candidates with `!view.is_able_to_fight` while its own
  comment says it filters `!is_dead && !is_unconscious`. Because civilians are never able-to-fight,
  **`chase_childs` can never find a suspect — the apple-chase behaviour is dead code in the port.**
  This will not show up in any replay whose capture never exercised apple-chasing; it needs a targeted
  test or a purpose-recorded trace.

### C vs C++ math overloads — a whole class of precision bugs (learned the hard way, 2026-08-15)
`2f8e6b54a` "Evaluate NPC gaze trig in double like the Original" was **wrong**, and was corrected by
`f6f8bd886`. The reasoning error: in C, `cos(float)` promotes its argument to `double`; in **C++**
`<math.h>` provides overloads, so `cos(float)` binds to the **float** overload. Disassembly of
`original-code/build/native-full/robin-schema14-capture` settles it — `SBGeoVector2D::GetRotated`
(`sblibng/SBGeoVector2D.cpp:413-422`) emits `call sincosf` then `mulss/mulss/subss`, i.e. **all single
precision**; `operator*` (:151-154) and `Det` (:158-161) likewise; only `Angle` (:431-464) goes double
(`divss; cvtss2sd; call atan; cvtsd2ss`), matching its explicit `(double)` cast. The build is SSE, so
there is no x87 excess precision either. **Do not infer float width from the C++ source alone — read
the disassembly of the capture binary.** Note the 6 traces `2f8e6b54a` cleared were fixed by its exact
`marraySectorX/Y` table change, not by the f64 rotation; all 6 still pass after the correction.

### Retirement candidates: the corpus is STALE for 30s Savegame_023 (3 traces)
30s `Savegame_linux3/Profile_001/Savegame_023` replay-001 (f54711), replay-002 (f54350),
replay-003 (f54212). Re-running today's Original binary on the same save and seed reproduces each
recording and then diverges **from the recording**: at seed 2, f54350, today's Original emits **1**
visibility query — the same as Rust — while the recording has 2. Rust now agrees with the current
reference; the recording does not. These are re-record-or-retire candidates, not Rust bugs. Verify the
same way before spending any more effort on them.

### Missing feature (not a parity bug): RHShadowPolygon night/fog light modulation
`shadow_polygon.rs` (engine and host side) implements no light modulation and no `is_reachable`. The
Original's `rhshadowpolygon.cpp:14288-14320` selects SHADOW sectors at the **projection area's** obstacle
layer (not the target's) and `:14349` calls `IsReachable(mptViewer, pSectorLight->GetBarycentre3D(),
SIGHTOBSTACLE_OPAQUE)`. gdb confirms a layer-2 `GetSectors(SECTOR_SHADOW)` at f54350 whose box contains
sector 189's barycentre (1035.3,1433.4) with **no preceding `ComputeViewRadius`** — so the layer-2 scan
never came from a dropped target obstacle. This is the correct form of the old sub-cause (a); the
"cadence" sub-cause (b) is **disproved** — no cadence change was needed for any member.

### Schema-12 corpus: VALID as an oracle (earlier "CRITICAL" claim in this file was WRONG)
An earlier revision of this section claimed the schema-12 corpus could not be trusted because today's
Original cannot reproduce it. **That overstated the case and was disproven by a dedicated investigation
(2026-08-16). Treat all schema-12 traces as fully trustworthy repros.**
- The schema-12 recorder already tagged audio draws `domain:"audio"`, on a callsite set **disjoint** from
  the simulation set (nicouzouf/039 r001: 6627 simulation draws over 43 callsites, 646 audio over 5, zero
  overlap). The runner consumes **only** `TraceRngDomain::Simulation`
  (`original_parity_replay.rs:548-564`, `:3900-3901`).
- So the correct statement is narrow: *a schema-12 trace cannot be re-verified by re-running Original past
  its first audio draw, but it remains a faithful record, because the runner filters the audio domain.*
  In 12/12 old-vs-fresh pairs the first difference was exactly an audio-domain draw present in the old
  trace and absent in the new one, with zero pre-audio drift — i.e. no build or asset staleness at all.
- Decisive evidence the recordings are right and Rust is wrong: for two sampled failures today's Original
  **independently reproduced the old recording's exact divergence entity and frame** — nicouzouf/037 r008
  Pc78 at f653/654, and linux3/040 r001 Soldier255 at f28701/28702.
- Reverse check: three currently-PASSING schema-12 traces each FAIL on a fresh capture of the same save at
  the same site family / same actor. They pass because their trajectory misses the bug, not because of a
  shared stale expectation (the runner has no schema-conditional comparison). Across 16 fresh captures,
  8 pass / 8 fail — the corpus if anything **under**-reports defects.
- Population evidence: 209 schema-12 traces pass bit-exactly in batch-20; schema-12's pass rate (76.3%,
  209/274) is **higher** than schema-14's (72.3%, 138/191); 63% of schema-12 failures are in a failure
  class that also occurs in other corpora; and all 65 come from saves that also have passing schema-12
  traces, which recorder corruption would not do seed-selectively.
- **Recommendation adopted: treat the 65 schema-12 failures as fixable port defects. Do not re-record or
  retire them as a class.** Retire only individually on a *proven* recorder defect (presentation-
  contaminated visibility queries, absent schema-14 fields, missing `rng_suffix`) via
  `docs/PARITY_RETIRED_TRACES.txt`. Re-recording would cost ~65 x 3-6 min and LOSE coverage: the
  audio-free trajectory dodges the bug in 7 of 13 sampled cases.
- The one proven schema-12 recorder defect (missing `PresentationScope`, i.e. render visibility queries
  leaking into the oracle) shows up as `state:visibility_queries.length` and accounts for exactly the 3
  known-stale 30s Savegame_023 traces — and **zero** of the 65.
- Fresh-capture recipe (all 16 save sha256s verified against each trace header's `initial_save`):
  `env ROBINHOOD_DATA_DIR=<repo>/datadirs/fullgame_linux SDL_AUDIODRIVER=dummy SDL_VIDEODRIVER=dummy LD_LIBRARY_PATH=<repo>/lib32:<repo>/lib32/pulseaudio <repo>/original-code/build/native-full/robin-schema14-capture -PARITYSAVE <save> -PARITYTRACE <out>.jsonl -PARITYSEED 1 -PARITYFRAMES 1500 -PARITYRANDOMINPUT <header.random_input_seed>`

### Task-4 "early-battle melee micro-gate" cluster: framing DISPROVEN, re-cluster it
All 22 members were re-measured on merged main: **2 now pass, 6 advanced, 14 unchanged.** The two
earliest frontiers (f207 / f231) were NOT the root of the other twenty — they moved without moving
anything else. Do not treat this as one family again. The 14 unchanged members should be re-clustered by
their own signatures: `DefaultPostLook` at f488/f503, `MeleeInitiative`+`SmalltalkStrikeSide` at
f517/f616, `SwordDamageProtection` at f1314, and five field-divergence members (f670 `ai.list_them`,
f792 `actor.animation`, f912 `ai.substate`, f1129 `path_events[0].goal.bits`, f1433 `PassDoor` vs
`MoveOk`) that are not melee-gate bugs at all.
Also disproven with Original ground truth (condolation stream from the prebuilt schema14 binary, which
retains an unconditional `fprintf` at `original-code/RHelementactornpc.cpp:6487`): over 400 frames /
1667 condolations, `RHCOMMAND_WAIT` was **510/510 `RHSEQ_INTERRUPTED`, zero TERMINATED**. The
`EVENT_DONE`-on-TERMINATED arm (`:6587-6595`) is dead in a swordfight because a swordfighting actor's
WAIT becomes `RHANIMATION_WAITING_SWORD`, whose Execute returns `IN_PROGRESS` unconditionally
(`RHelementactorhuman.cpp:3610-3628`). Rust already matches (`engine/soldier_helpers.rs:1196-1200`).

### Open lead: parade heartbeat timer rings 10 frames late in Rust
Residual f315 divergence on nicouzouf Savegame_039 replay-001. `SUBSTATE_ATTACKING_SWORDFIGHT_PARADE` is
left only on `EVENT_TIMER` (`RHartificialmalignity.cpp:4404-4421`), armed once at `:4419` with
`GetFramesFromStartTillActionDone(GetAnimation()) + 10`. Rust arms it to ring at universal frame 326;
the Original rings at 316. The formula is NOT the bug: `sprite.rs:1987` is statement-for-statement
`original-code/RHsprite.cpp:1283-1305`, and the animation<->strike round trip is an identity on A..I.
Remaining candidates: a second, later `ConsiderToBeginParade` from a different hitter that Rust drops, or
an `EVENT_ENTER_SWORDFIGHT` (`RHartificialmalignity.cpp:6485-6493`) Rust never delivers. Use the
`PARITY_DEBUG_PARADE_TIMER` diagnostic merged in `dca5d4789`.

### Two C++ behaviours Rust does not model (deliberately NOT changed — unreachable/unprovable today)
1. Missing `break` after `case SUBSTATE_ATTACKING_MOVING_AROUND_OLD_ENEMY:`
   (`RHartificialmalignity.cpp:4372-4383`) falls through into the QUITTING_SWORDFIGHT body; Rust returns.
   Provably unreachable now: it only bites on `EVENT_TIMER`, and `Think` discards an `EVENT_TIMER` whose
   `mSubstateAtLastTimerLaunch` differs from the current substate, while nothing arms a timer in
   substate 173. Changing it would be an unvalidatable no-op with non-zero risk.
2. Rust's `attacking_quitting_swordfight` calls `end_swordfight()` unconditionally in the sword-action
   arm; the Original guards it with `if( GetCommand() != RHCOMMAND_QUIT_SWORDFIGHT )`
   (`RHartificialmalignity.cpp:4392-4397`). Needs an actor-command field on `AiContext` first.

### Highest-value unassigned leads
1. **Wait-completion-vs-interruption** (dispatched): at f231 Original draws 1, Rust draws 4 because Rust
   raises a spurious `EVENT_DONE` on a `Wait` the Original interrupts with a same-frame reactive parry.
   Sits under ~20 traces in the two hottest battle saves.
2. **Civilian on-post turn** (2 traces, clean): linux3/P3 Savegame_074 r001/r002 — Civilian 62
   `DefaultGotoPostTurn`(8) vs Rust `DefaultOnPost`(11), extra `AiRandomValueRectangle` before `MacroRand`.
3. **Melee mirror pair**: nicouzouf 069 r010 (strike lands in Rust only) and 037 r005 (lands in Original
   only) — plausibly one root cause seen from both sides.
4. **1-ulp `movement_map.x`**: Savegame_029 r014 (f4866), r010 (f5185), Savegame_032 r002.
5. **Increment seeded from post-move position**: Nescafe P2 Savegame_000 r015 f1531 —
   `normalize(goal - pos@1530)` reproduces Original `0xbebd747b` bit-exactly.
6. **Bow-shot initial velocity**: Savegame_029 r001 f12292 — same XY bearing, different speed/pitch;
   `ComputeInitialThrowVelocity` (`RHelementprojectile.cpp:161-207`) picks a different apex flight time.
7. **`EnemyAi::end_think` triple latch**: queues all three completion latches; Original's `StartThink`
   (`RHartificialintelligence.cpp:982-984`) clears all three so at most one can fire. No failing trace yet.
8. `RuntimeBuildingExitWait` cohort (3) — same missing C++ site `RHsequence.cpp:484`, three different
   upstream causes; campaign #75 flagged it capture-required.

## Wave loop (the process to continue)

1. Merge reported agent branches into main (verify suite 2629/0 + debug build).
2. When agents free up / finish: assign next task from board, or spawn fresh (`Agent` tool, `isolation: "worktree"`, prompt template: symlink setup + TaskGet + debug-only + narrow validation + faithful-C++ rules — copy any recent spawn prompt from this file's history).
3. After a batch of merges: rsync source to remote, release-build, freeze runner as `original_parity_replay-<commit>`, make manifest, launch 11-shard tmux sweep (`scripts/run_parity_release_sweep.sh CORPUS AUDIT RUNNER SHARD SHARDS`, wrapper `run-parity-*.sh` pattern), watcher on SWEEP-DONE, rsync back, classify, update ledger, dispatch.
4. **Next sweep (14) must be FULL-UNIVERSE** — run `scripts/build_parity_full_snapshot.sh <audit>/traces.snapshot`; it includes every current corpus, excludes schema-11 (`parity-save-replays/`), and replaces old captures listed in `docs/PARITY_RETIRED_SAVES.txt` with their schema-14 recordings. The passing set must be re-verified because of the #36 regression.

## Key mechanics reference (hard-won, do not relearn)

- Remote sweep: `ssh atlasbio-robin-cpu4`; corpora + datadirs + original-code + rust-src at `~/robinhood/`; build `cd ~/robinhood/rust-src && cargo build --release --example original_parity_replay`; sweep script reads `<audit>/traces.snapshot`, writes per-trace `status/<key>.status` (0 pass, 1 state-div, 101 rng-panic, 124 timeout) and `logs/<key>.log`; key = trace path with `/`→`__`.
- classify_parity_failures.py groups by first divergence boundary; output classification.json.
- Substates in dumps: C++ `RHsubstate` and Rust `ai::model::Substate` align **1:1** — both are 256 entries starting at `START_SLEEPING_SUBSTATES`/`StartSleepingSubstates = 0`, and e.g. 155 = RUNNING_TO_ENEMY, 208 = RIDER_CHARGING_APPROACHING, 250 = RUN_TO_AVENGER_ON_ROOF on both sides (only 114/201/202 differ in spelling). The earlier "C++ enum = Rust index + 1" note in this file was WRONG and caused misreadings of dumps; verified 2026-08-15 by parsing both enums with explicit discriminants honored. Aim previews burn creation-order in Original, not in Rust (see memory project_creation_order_preview_gaps).
- RNG contract: Rust consumes recorded draw VALUES; "replay exhausted" = Rust drew MORE than Original; extra/missing draws → compare gate conditions at the named site.
- `--dump-jsonl` ~500KB/frame — narrow windows only, delete after.
- Entity mapping in dumps: original pc:N ↔ Rust Pc(PcId(N−1)) sometimes (creation-order shift) — verify entity identity before trusting a dump diff (bit fix-endfacing twice).
- Debug replays under multi-agent load: 25-85 min for 15k+ frame traces; timeout 1800-3600s.
- The Bash tool auto-spills long output; never filter cargo.

## Recovery checklist for the NEW session (do these in order)

1. Read this file fully. Also load memories (MEMORY.md points here).
2. Recreate the open task board from `docs/parity-task-archive/*.json` (open items: #9, #20, #21, #23, #24, #28, #31, #34–#40; the JSON descriptions carry the accumulated analysis — copy them verbatim into new TaskCreate calls).
3. **First dispatch: task #36 (seek-RNG regression)** — spawn a fresh worktree agent with the task JSON's content; 4 shallow repros make it a fast bisect over `git log --oneline --merges db67cc978..78a792ed6`.
4. Spawn agents for #20 (attach wip patch as head-start), #21 (ditto), #39, and further board items as slots allow. Spawn template: Agent tool with isolation:"worktree"; prompt = symlink setup block (original-code, datadirs, corpora → absolute paths into this repo) + task text + rules (debug builds only; never --release/clippy/filtered cargo; faithful C++ only, no invented guards; validate 2-4 repros + 2-3 short controls; cargo fmt; commit on own branch; report worktree+branch+root cause+validation).
5. After merging a batch: release build + freeze runner + **FULL-UNIVERSE re-sweep 14** via `scripts/build_parity_full_snapshot.sh`. Failure-only sweeps are banned until a clean post-#36 full-universe result verifies the passing set.
6. Continue the wave loop (previous section) until 100% parity. Do not stop.
